# Worklist slice: (Z) Sections to trim to a pointer (other docs own them)

From `worklist.tsv` (1 rows). `class` is one of
verified/doc-edit/wrong/missing/trim/delta/code-bug?. The `MANUAL`
text is the report's quote; its line numbers are the OLD (2324-line)
revision and are hints only.

## A154 (trim)
source: `mechanisms.md`

- MANUAL: MANUAL: lines 1835–1943 (SDK APIs + `onIdle`)
- CODE: doc: `docs/WIRE.md` "Idle work"/SDK contract; AGENTS.md restates it
- FIX: FIX: shrink to the three-SDK table + pointer.

