[
 {
  "id": "A035",
  "slice": "Context window",
  "status": "apply",
  "reason": "the MANUAL names two fields that exist nowhere in the code: the status event carries a nested cumulative `cache {prompt, read, hitRate}`, and the conversation header keeps `cachePrompt`/`cacheRead`/`cacheHitRate` | UI parenthetical corrected with A112's evidence (web UI: numbers, not a percentage). | Record corrected: the applied text drops this row's process note and re-wraps the parenthetical (the note was drafting apparatus, not MANUAL prose).",
  "evidence": "docs/MANUAL.md:733-738 vs core/conversation.nim:2240-2242 (statusEv[\"cache\"] = {prompt, read, hitRate}) and :467-472 (header cachePrompt/cacheRead/cacheHitRate); `grep -rn cacheHitTokens core/ components/ ui/` → only components/hooks/README.md:35 (wrong too)",
  "old_string": "  When the provider reports cached input (`prompt_tokens_details.cached_tokens`),\n  the status event also carries `cacheHitTokens` and `cacheHitRatio` — with\n  the frozen prompt prefix most prompt tokens should be cached after the\n  first request, so a low ratio is a signal worth noticing (the web UI shows\n  `⚡ NN% cached` per message; the TUI status line shows a `⚡ NN% cached`\n  chip).",
  "new_string": "  When the provider reports cached input (`prompt_tokens_details.cached_tokens`),\n  the status event also carries the conversation's cumulative cache split as\n  `cache {prompt, read, hitRate}` (summed prompt and cached tokens, ratio in\n  percent); the conversation header keeps the same numbers as `cachePrompt`,\n  `cacheRead` and `cacheHitRate`. With the frozen prompt prefix most prompt\n  tokens should be cached after the first request, so a low ratio is a signal\n  worth noticing (the web UI shows `⚡ <cached>/<prompt> cached` per message and\n  the cumulative split in `/info`; the TUI shows a `cache NN%` chip in its\n  header and the same split in `/status`)."
 },
 {
  "id": "A036",
  "slice": "Context window",
  "status": "already",
  "reason": "the MANUAL is right today: it claims only that the four `reset:*` reasons are the intentional prompt-prefix rebuilds, and that holds. The extra reasons the row lists either do not rebuild the prefix (`warn:threshold` core/conversation.nim:913, `compact:failed|declined|invalid|stale` :1575-1629) or are not `ev.session.context` events at all — `context-overflow` is published on `ev.session.retry` (:2114 via onEvent(\"retry\", …), subject built at core/conversation.nim:2913-2915), so the sentence the row proposes would introduce a new error",
  "evidence": "docs/MANUAL.md:787-790; core/conversation.nim:840/856 (reset:prune/trim), 1337 (reset:tools), 1722 (reset:compact), 913 (warn:threshold), 1575-1629 (compact:*), 2114 (reason context-overflow on the *retry* event)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A037",
  "slice": "Context window",
  "status": "already",
  "reason": "already fixed by an earlier pass (present in the current MANUAL, §Progressive tool discovery / §Model): the exposure table is followed by \"A hidden tool that also carries `x-harness.runner: true` is exempt from a subagent's frozen tool allowlist — how replaceable runner machinery (compaction, recall) reaches a child whose toolset was frozen before it existed\"",
  "evidence": "docs/MANUAL.md:1729; code unchanged: core/dispatch.nim:1455-1473",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A039",
  "slice": "Context window",
  "status": "already",
  "reason": "verified in §Context window (the canonical/projection bullet): accurate as written — `context_recall` resolves exactly canonical/spill/checkpoint refs and its `mode: search` greps the whole canonical history",
  "evidence": "components/recall/main.nim:6-15,137-217,253; docs/MANUAL.md:782-789",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A040",
  "slice": "Context window",
  "status": "already",
  "reason": "verified in §Context window (first bullet): the effective window really is resolved by hidden `llm_resolve` before each turn",
  "evidence": "core/conversation.nim:985 (dispatchToolCall(\"llm_resolve\", …, 10_000)); docs/MANUAL.md:683-690",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A041",
  "slice": "Context window",
  "status": "already",
  "reason": "verified in §Context window (per-session controls bullet): maxCalls is clamped 1-500 on the first call and every dispatch attempt increments the counter before dispatch",
  "evidence": "core/conversation.nim:2657-2663 (mc >= 1 and mc <= 500), :2403, :2426 and :2450 (`inc toolCallsMade  # every dispatch attempt counts`); docs/MANUAL.md:712-715",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A155",
  "slice": "Context window",
  "status": "skip",
  "reason": "duplicate of A035 — identical quote, identical site: one edit, carried by the lower id (A035) so the batch cannot anchor two edits at one sentence",
  "evidence": "docs/MANUAL.md:733-738 (same anchor as A035)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A156",
  "slice": "Context window",
  "status": "skip",
  "reason": "duplicate of A036 — same sentence, same finding; no edit is needed (A036 carries the code evidence that the extra reasons are decisions, not prefix rebuilds)",
  "evidence": "docs/MANUAL.md:787-790 (same anchor as A036)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A157",
  "slice": "Context window",
  "status": "already",
  "reason": "delta row, re-verified in §Context window: the prune → compactor → trim ladder, the per-turn calibration offset, the derived output reserve and `trimThrough` all match the code, so there is no delta to write",
  "evidence": "core/conversation.nim:595-612 (ctxWarnRatio 0.75, ctxTrimRatio 0.9, ctxOutputReserve 16384, NIF_CTX_RESERVE), :685-695 (trimThreshold = min(0.9·window, window−reserve)), :812-821 (contextTarget), docs/MANUAL.md:743-772, 800-808",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A161",
  "slice": "Context window",
  "status": "already",
  "reason": "the store kind table now carries every kind the row lists — profile, mcp, approval, spill, contextreceipt, compaction_input and context_projection all have rows (added by the store batch, commit 04100bb)",
  "evidence": "docs/MANUAL.md:2698-2710 (kind table rows for profile/approval/contextreceipt/compaction_input/context_projection/spill/mcp)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A368",
  "slice": "Context window",
  "status": "already",
  "reason": "delta row: 'git tools scope at the workspace repo' is accurate — components/git declares `x-harness.workspace {cwdField: \"repo\"}`, so core resolves the repo argument against the conversation workspace",
  "evidence": "components/git/main.nim:187,218,257,295,325 (`\"workspace\": {\"cwdField\": \"repo\"}`), core/dispatch.nim:1457-1475; docs/MANUAL.md:719-723",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A393",
  "slice": "Context window",
  "status": "apply",
  "reason": "the MANUAL documents `prompt_tokens_details.cached_tokens` as if every protocol reported it verbatim; the anthropic lane synthesizes prompt_tokens from input + cache read + cache creation and counts only reads as cached, and usage is attached only when the provider reported non-zero tokens",
  "evidence": "components/llm/anthropic.go:186-201, components/llm/codex.go:325-331 (Responses usage mapped), components/llm/main.go:1076-1087 (usage only when usageSeen and non-zero); docs/MANUAL.md:1064-1067",
  "old_string": "  blocks (consecutive tool results merge into one user message).",
  "new_string": "  blocks (consecutive tool results merge into one user message); its usage is\n  normalized for core's counters — `prompt_tokens` is input + cache reads +\n  cache writes, while only cache **reads** count as cached tokens (cache\n  writes are billed at write rates). Across all three protocols a `usage`\n  object accompanies the result only when the provider reported non-zero\n  tokens."
 },
 {
  "id": "A189",
  "slice": "Starting and stopping",
  "status": "apply",
  "reason": "accurate but thin: add the numbers a debugger needs — attach probes for ~10 s at 200 ms, the post-spawn wait is 20 s with that exact error, and the Nim SDK reaps a core it spawned earlier in the session",
  "evidence": "sdk/go/harness.go:121-142 (10 s / 200 ms probe), :163-182 (20 s, \"spawned core did not answer within 20s — check %s\"); sdk/niffler/sdk.nim:591-617 (reapSpawnedCore), :655-671 (same 20 s failure text); docs/MANUAL.md:2761-2767",
  "old_string": "  repo root is baked in at `make ui` time (ldflags), so the installed icon\n  works as well as the in-tree binary.",
  "new_string": "  repo root is baked in at `make ui` time (ldflags), so the installed icon\n  works as well as the in-tree binary.\n  The probes are patient: attaching retries for ~10 s at 200 ms, and a spawned\n  core must answer within 20 s or `ensureHarness` fails with `spawned core did\n  not answer within 20s — check <root>` (the Nim SDK first reaps a core it\n  spawned earlier in the same session, if it has exited)."
 },
 {
  "id": "A190",
  "slice": "Starting and stopping",
  "status": "apply",
  "reason": "the TUI's probe order is longer than documented ($NIF_ROOT-relative and cwd-relative `var/nats-url` too) and, unlike ensureHarness, it does no root check on the core it joins",
  "evidence": "~/git/niffler-tui/tui/main.go:2938-2953 (resolveNATSURL: NIF_NATS_URL, $NIF_ROOT/var/nats-url, ./var/nats-url, defaultNATSURL), tui/main.go:3080 (comp.Client = true), no CoreRoot probe anywhere in that file; docs/MANUAL.md:2768-2772",
  "old_string": "  `ensureHarness` and never spawn a harness: they probe for a live bus\n  (`NIF_NATS_URL` → `var/nats-url` → 127.0.0.1:4222), connect and register\n  `client: true` (so an autostarted core stays up while they run). Start the\n  harness first — desktop UI or `./var/bin/niffler`.",
  "new_string": "  `ensureHarness` and never spawn a harness: they probe for a live bus\n  (`NIF_NATS_URL` → `$NIF_ROOT/var/nats-url` → `./var/nats-url` →\n  127.0.0.1:4222), connect and register `client: true` (so an autostarted\n  core stays up while they run). Unlike `ensureHarness` they do **not** check\n  which root the answering core serves, so a TUI can join a foreign harness's\n  bus that the desktop UI would refuse. Start the harness first — desktop UI\n  or `./var/bin/niffler`."
 },
 {
  "id": "A191",
  "slice": "Starting and stopping",
  "status": "apply",
  "reason": "the idle/boot-grace numbers are right, but the count is a registration, not a lease: nothing in the catalog ages a client out (only reg.depart, a supervisor loss or remove drops it), so a SIGKILLed client pins an autostarted core — and core's approval gate, which denies when clientCount()==0",
  "evidence": "core/catalog.nim:795-799 (clientCount counts components with client:true), :607/787/818/823 (only depart/lost/removed drop a registration — no liveness sweep); core/approval.nim:197-212 (clientCount()==0 → deny); core/niffler.nim:659-691; docs/MANUAL.md:2777-2784",
  "old_string": "Interactive frontends register `\"client\": true` (the SDK's `interactive()`\n/ `Component.Client` marker). An **autostarted** core counts them: when the\nlast one departs it shuts down after `NIF_AUTOSTART_IDLE_S` (default 10s —",
  "new_string": "Interactive frontends register `\"client\": true` (the SDK's `interactive()`\n/ `Component.Client` marker). That mark is a registration, not a lease: a\nclient killed without `reg.depart` keeps an autostarted core up — and keeps\ncore believing a human is reachable for approvals — until the catalog drops\nit (the `ui` registry's 20 s lease is a separate clock, see\n[Clients and the UI registry](#clients-and-the-ui-registry)). An\n**autostarted** core counts them: when the last one departs it shuts down\nafter `NIF_AUTOSTART_IDLE_S` (default 10s —"
 },
 {
  "id": "A192",
  "slice": "Starting and stopping",
  "status": "apply",
  "reason": "accurate; add the two facts an operator needs — NIF_AUTOSTART=1 forces service mode even on a tty, and the shell keeps serving svc.core.call between keystrokes so a UI can attach to a tty-started core",
  "evidence": "core/niffler.nim:653 (if isatty(stdin) and not autostart), :661-664 (runAdminShell with pumpCoreCalls/cat.pump/sup.pump); core/tty.nim:6-8 (\"While waiting for input the shell keeps pumping the bus so UIs stay responsive\"), :260 (if pump != nil: pump()); docs/MANUAL.md:2773-2775",
  "old_string": "- **Terminal admin shell** — `./var/bin/niffler` directly, or\n  `./var/bin/niffler --minimal` for the three-component boot profile. A\n  manually started core never self-terminates; stop it with Ctrl-C / SIGTERM.",
  "new_string": "- **Terminal admin shell** — `./var/bin/niffler` directly, or\n  `./var/bin/niffler --minimal` for the three-component boot profile. A\n  manually started core never self-terminates; stop it with Ctrl-C / SIGTERM.\n  `NIF_AUTOSTART=1` in the environment overrides the shell — that core is\n  service mode even on a tty — and while it sits at the prompt it keeps\n  serving `svc.core.call`, so a UI can attach to a tty-started core."
 },
 {
  "id": "A193",
  "slice": "Starting and stopping",
  "status": "apply",
  "reason": "an explicit NIF_NATS_URL short-circuits ensureHarness in both SDKs (nothing is probed, nothing is spawned); NIF_NATS_SPAWN is documented only in the env table and the observe section, so the core-side twin is worth naming where the lifecycle is described",
  "evidence": "sdk/niffler/sdk.nim:611-617 and sdk/go/harness.go:121-123 (explicit NIF_NATS_URL returns before any probe); core/niffler.nim:343-353 (NIF_NATS_SPAWN=1 → spawnNats([\"-1\"]) — random port, never 4222); docs/MANUAL.md:2784",
  "old_string": "`NIF_ENSURE_ATTACH=0` makes `ensureHarness` spawn unconditionally (tests).",
  "new_string": "`NIF_ENSURE_ATTACH=0` makes `ensureHarness` spawn unconditionally (tests).\nAn explicit `NIF_NATS_URL` short-circuits the other way: the client attaches\nto exactly that bus and nothing is probed or spawned. (`NIF_NATS_SPAWN=1` is\nthe core-side twin — an isolated core-owned bus on a random port, never\n4222; see [Environment variables](#environment-variables).)"
 },
 {
  "id": "A194",
  "slice": "Starting and stopping",
  "status": "apply",
  "reason": "the kernel-level child cleanup is documented only indirectly (the Troubleshooting row for an orphaned nats-server); state it where lifecycle is explained, including that core itself is the deliberate exception",
  "evidence": "core/supervisor.nim:141-152 (setpriv --pdeathsig TERM when findExe(\"setpriv\") finds it), components/nats/main.go:15-18 + components/nats/pdeathsig_linux.go:17 (PR_SET_PDEATHSIG in nats-server main), sdk/go/pdeathsig_linux.go:17, sdk/niffler/sdk.nim:566, sdk/go/harness.go:158 (core: Setsid only, no PDEATHSIG); docs/MANUAL.md:2782-2784 (the paragraph this belongs to) — anchor is the `## Common tasks` heading (docs/MANUAL.md:2786, unique)",
  "old_string": "## Common tasks\n",
  "new_string": "At the kernel level the same rule holds with one deliberate exception: core\nitself has no parent-death signal — an autostarted core must outlive the UI\nthat spawned it — while every child it starts does. Supervised children\n(components, session runners) are wrapped in `setpriv --pdeathsig TERM` when\nutil-linux's `setpriv` is on `PATH`, the SDKs set `PR_SET_PDEATHSIG` in the\ncomponents they spawn, and the bundled `nats-server` sets it in its own\n`main`, so nothing survives its harness even on SIGKILL. Off Linux (or\nwithout `setpriv`) both mechanisms are absent, which is how an orphaned bus\nhappens (see Troubleshooting).\n\n## Common tasks\n"
 },
 {
  "id": "A196",
  "slice": "Starting and stopping",
  "status": "already",
  "reason": "the scope the row asks for is now in the MANUAL: the restart-policy paragraph states that `never` or `on-failure` is per child, that spawn uses on-failure and session runners are always never (added by the batch-8 A188 pass), so the Troubleshooting row need not repeat it",
  "evidence": "docs/MANUAL.md:848-852 (Restart policy paragraph), :2848 (the symptom row); core/supervisor.nim:33-49 (parsePolicy), :200-212 (rpNever retires instead of restarting)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A197",
  "slice": "Starting and stopping",
  "status": "apply",
  "reason": "the registry is documented now (added by the batch-2 A149 pass); the remaining deltas are the `owner` op, the lazy sweep and the name collision between the hidden core tool `ui` and the Wails-bridge component `ui` — the last one is an easy reader trap",
  "evidence": "core/uireg.nim:1-20,65-124 (op set register|renew|release|claim|release_session|owner, sweep() on every op, no timer), core/catalog.nim:213-224 (hidden core tool named `ui`), core/tty.nim — and ui/bridge.go:25-26 (sdk.New(\"ui\", \"0.1.0\")); docs/MANUAL.md:218-221",
  "old_string": "Every frontend registers on the bus as a zero-tool component and keeps a\nlease alive: core's `ui` tool — `register`, `renew`, `release`, plus\n`claim`/`release_session` for per-conversation ownership — tracks which\nwindow owns which conversation. A lease lasts 20 s; display numbers\n(\"Niffler 1\", \"Niffler 2\") are monotonic for the harness's lifetime. This is",
  "new_string": "Every interactive frontend registers on the bus as a zero-tool component and\nkeeps a lease alive: core's hidden `ui` tool — `register`, `renew`,\n`release`, `owner`, plus `claim`/`release_session` for per-conversation\nownership — tracks which window owns which conversation (the component named\n`ui` is the desktop bridge, not this tool). A lease lasts 20 s, and expired\nleases are swept lazily on each request — no timer thread exists. Display\nnumbers (\"Niffler 1\", \"Niffler 2\") are monotonic for the harness's lifetime.\nThis is"
 },
 {
  "id": "A198",
  "slice": "Starting and stopping",
  "status": "already",
  "reason": "the row's premise is obsolete in the current tree: the desktop UI is no longer registry-free — each browser tab mints a \"ui-<hex>\" identity through Bridge.NewUiId, registers/renews every 5 s and claims its conversation like the TUI, so there is no asymmetry left to state; the MANUAL's 'every frontend registers … keeps a lease alive' is correct, and the tool/component name collision is covered by the A197 edit",
  "evidence": "ui/frontend/src/lib/uiRegistry.ts:1-20,49-60 (UI_LEASE_RENEW_MS = 5000, register/renew/claim/release via send(\"core\",\"ui\",…)), ui/bridge.go:197-209 (NewUiId \"ui-<12 hex>\"); ~/git/niffler-tui/tui/main.go:650-681 (same cadence for \"tui-<id>\"); docs/MANUAL.md:218-226",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A199",
  "slice": "Starting and stopping",
  "status": "already",
  "reason": "the identity handoff the row asks to document is in the MANUAL (TTL 120 s, keyed by bus + workspace, so the conversation and its \"Niffler N\" label survive /restart). The exit code 85 and the wrapper's re-run loop are niffler-tui internals — that client's own README is their home, not the harness MANUAL",
  "evidence": "~/git/niffler-tui/tui/handoff.go:32-37,84-111, tui/main.go:58-63 (restartExitCode = 85), tui/main.go:3179-3186 (restart → Exit(85) without release), tui/uirelease.go:29-38 (every other exit releases, 3 attempts); docs/MANUAL.md:224-226",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A200",
  "slice": "Starting and stopping",
  "status": "skip",
  "reason": "the roster's three facts live at their natural homes already (the tty is status-only in Session runners, console/cli are bus tools in The bus in one screen, cli/console rows in Shipped components), and the row's middle fact is imprecise for the current tree: the SPA does not speak NATS — it calls the Go bridge's Send/SendAs bindings, which are the bus citizen (and `make dev` stubs them in a browser). A second table would duplicate four documented spots with one partly-wrong premise",
  "evidence": "docs/MANUAL.md:210-214 (tty admin shell), :541-565 (console/cli), :81-82 (shipped-component rows), :2822-2823 (Wails/make dev); ui/frontend/src/nats.ts:1-8,24-38 (bridge().Send / SendAs — not NATS), ui/bridge.go:22-30 (component \"ui\", Client = true)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A201",
  "slice": "Starting and stopping",
  "status": "skip",
  "reason": "below the MANUAL's threshold: the sentence already names exactly the six commands (help/status/catalog/tools/sessions/exit), it sits ten lines above the new Clients and the UI registry subsection, and the `?`/`quit` aliases are shell sugar — a cross-link plus two aliases would churn prose without changing what a reader can do",
  "evidence": "core/tty.nim:19-26 (command table), :207-210 (\"help\",\"?\" and \"exit\",\"quit\"); docs/MANUAL.md:210-214, 216-226",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A202",
  "slice": "Starting and stopping",
  "status": "skip",
  "reason": "client-side timeouts (a 10-minute turn call, a 30 s status call, the sidebar reading store.list) are SPA implementation details, not bus contract; the load-bearing half — clients call `svc.core.call` with the `session` tool and nothing about a conversation is UI-private — is already stated in Session runners",
  "evidence": "ui/frontend/src/views/Chat.svelte:439 and ui/frontend/src/App.svelte:206 (timeouts), ui/frontend/src/views/Sessions.svelte:27,58 (store.list); docs/MANUAL.md:182-185 (\"Clients keep calling svc.core.call (tool `session`)\")",
  "old_string": "",
  "new_string": ""
 }
]