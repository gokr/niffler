[
  {
    "id": "A059",
    "slice": "Background processes (processes)",
    "status": "already",
    "reason": "Duplicate of the approvals-list row in mechanisms.md (the finding's own FIX is 'none [dup]'): the current table already matches the code exactly — process_start/process_kill carry approval: always, process_poll/process_list declare effect read, and the wait cap is 25 s inside the 30 s tool timeout. No MANUAL change needed.",
    "evidence": "docs/MANUAL.md:1458-1461 vs components/processes/main.nim:492-530 (approval: always on start/kill, effect: read on poll/list), MAX_WAIT_MS = 25_000 at main.nim:40, x-harness.timeoutMs 30000/20000",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A060",
    "slice": "Background processes (processes)",
    "status": "already",
    "reason": "Verified in the first wave and still true: the spool bullet states the 32 MiB cap, the tail truncation, the per-stream poll chunk default and the 32/50 registry bounds, all of which the current code implements verbatim.",
    "evidence": "docs/MANUAL.md:1466-1478 vs components/processes/main.nim:33-37 (MAX_LIVE = 32, KEEP_FINISHED = 50, SPOOL_CAP = 32*1024*1024), :50-56 (NIF_PROCESSES_POLL_CHUNK default 65536), :200-210 (tail truncation)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A061",
    "slice": "Background processes (processes)",
    "status": "already",
    "reason": "Verified in the first wave and still true: the exit-notice paragraph matches the code — the notice rides svc.session.<id>.steer with a {notice: …} payload, is sent exactly once per process, and carries process id, status and byte count rather than the output itself.",
    "evidence": "docs/MANUAL.md:1483-1493 vs components/processes/main.nim:443-471 (publishExitNotice: notified-once guard, svc.session.<id>.steer, %*{\"notice\": payload}), :473-482 (onIdle reaper)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A062",
    "slice": "Background processes (processes)",
    "status": "already",
    "reason": "Verified in the first wave and still true: the process_start row names the process group and stdin from /dev/null, exactly what the spawn does.",
    "evidence": "docs/MANUAL.md:1458 vs components/processes/main.nim:301-316 (wrapped command ending `< /dev/null`, fork, setpgid(0, 0) in the child, execvp)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A285",
    "slice": "Background processes (processes)",
    "status": "apply",
    "reason": "'no timeout applies' reads as if the whole bash call is unbounded. It is not: bash's background branch issues a 15 s `process_start` request (bash main.nim:142) inside the call's own dispatch budget, while the job itself outlives the call. Added that distinction; the surrounding paragraph already documents the [E_BACKGROUND] path.",
    "evidence": "components/bash/main.nim:139-160 (`c.request(\"processes\", \"process_start\", startArgs, 15000)`); docs/MANUAL.md:1496-1500",
    "old_string": "All four tools are on-demand (`discover`/`invoke`). The bash tool's\n`run_in_background` flag is a thin producer over this component: the call\nreturns the id at once (no timeout applies) and the transcript line points\nat `process_poll`/`process_kill`.",
    "new_string": "All four tools are on-demand (`discover`/`invoke`). The bash tool's\n`run_in_background` flag is a thin producer over this component: the call\nreturns the id at once — no timeout applies to the *job*, but the `bash`\ncall that starts it is still bounded (its dispatch budget, and the 15 s\n`process_start` request behind it) — and the transcript line points at\n`process_poll`/`process_kill`."
  },
  {
    "id": "A291",
    "slice": "Background processes (processes)",
    "status": "already",
    "reason": "The first wave found nothing to fix ('FIX: none — verified accurate') and the bash/processes interplay paragraph still matches the code, including the owning-session handoff, the exit notice and the [E_BACKGROUND] fallback.",
    "evidence": "docs/MANUAL.md:1496-1502 vs components/bash/main.nim:139-166 (session handed to the registry, payload text, [E_BACKGROUND] on failure)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A452",
    "slice": "Background processes (processes)",
    "status": "apply",
    "reason": "The process_kill row said only 'terminate the whole process group'; the escalation and its grace period are now stated, matching killEntry.",
    "evidence": "components/processes/main.nim:183-193 (killEntry: SIGTERM, sleep 300, SIGKILL, sleep 100); docs/MANUAL.md:1460",
    "old_string": "| `process_kill {id}` | Terminate the whole process group. Approval-gated |",
    "new_string": "| `process_kill {id}` | Terminate the whole process group — SIGTERM, 300 ms grace, then SIGKILL. Approval-gated |"
  },
  {
    "id": "A454",
    "slice": "Background processes (processes)",
    "status": "apply",
    "reason": "The owner-less case was described as 'a direct process_start, e.g. from cli'. Only bash hands an owner over (it is the single caller that passes `session`), so any other route — the model's own discover+invoke, cli, a script — has no exit notice. Widened.",
    "evidence": "components/processes/main.nim:491-503 and :508-521 (the process_start and process_kill schemas carry approval: always and no session flag; process_start has no x-harness.sessionId, `session` only arrives as an argument) and components/bash/main.nim:148-151 (only bash sets startArgs[\"session\"]); docs/MANUAL.md:1486-1489",
    "old_string": "  instead of `running` until asked. A process started without an owner\n  session (a direct `process_start`, e.g. from `cli`), or one whose\n  conversation runner has already retired, is announced to nobody — poll it.",
    "new_string": "  instead of `running` until asked. Only `bash run_in_background` hands the\n  owning conversation over (it is the one caller that passes `session`), so a\n  `process_start` reached any other way — the model's own `discover` +\n  `invoke`, `cli`, a script — is started without an owner session; a process\n  whose conversation runner has already retired is announced to nobody too.\n  Poll those."
  },
  {
    "id": "A455",
    "slice": "Background processes (processes)",
    "status": "apply",
    "reason": "'Processes die with the harness' overstates the crash case: a graceful stop kills every entry via onDrain, but a SIGKILLed component leaves the children running and the next start sweeps them (registry.json + /proc starttime). Reworded.",
    "evidence": "components/processes/main.nim:115-143 (bootSweep kills orphaned groups), :484-488 (onDrain -> killEntry for every entry); docs/MANUAL.md:1494-1498",
    "old_string": "- Crash-safe: children are process-group leaders, so a SIGKILLed component\n  leaves them running — `registry.json` (pid + /proc starttime, defeating\n  pid reuse) drives a boot sweep that kills orphans from a previous life\n  before serving. Processes die with the harness.",
    "new_string": "- Crash-safe: children are process-group leaders, so a SIGKILLed component\n  leaves them running — `registry.json` (pid + /proc starttime, defeating\n  pid reuse) drives a boot sweep that kills orphans from a previous life\n  before serving. They die when the `processes` component stops; if it is\n  killed instead, the next start sweeps what is left."
  },
  {
    "id": "A012",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The Layout table introduces components/ as 'one directory per component', but two directories are not bus citizens. Named both in the same row: components/nats (which builds the var/bin/nats-server core spawns — only mentioned later, in Monitoring) and components/ctxtest (compiled by the nested call tests, never shipped or spawned).",
    "evidence": "components/nats/main.go + components/ctxtest/{main.nim,sink.nim}; tests/t_fabric.nim:45-54 and tests/t_agent.nim:48-53 compile ctxtest into their sandbox; docs/MANUAL.md:38",
    "old_string": "| `components/` | shipped component sources — one directory per component (Nim, Go, TypeScript and one bash demo); the inventory is the [Shipped components](#shipped-components) table below, which is the part that has to stay current |",
    "new_string": "| `components/` | shipped component sources — one directory per component (Nim, Go, TypeScript and one bash demo); the inventory is the [Shipped components](#shipped-components) table below, which is the part that has to stay current. Two directories are not bus citizens: `components/nats` builds the `var/bin/nats-server` core spawns when a bus has to be started, and `components/ctxtest` is a fixture the nested-call tests (`t_fabric`, `t_agent`) compile for themselves |"
  },
  {
    "id": "A119",
    "slice": "Observation and logs",
    "status": "skip",
    "reason": "Same finding as A012 (mechanisms-full.md repeats the component-nats/ctxtest observation from mechanisms-obs.md); folded into A012's edit of the components/ row rather than a second edit of the same sentence.",
    "evidence": "docs/audit/2026-09-18/sections/observation-and-logs.md A119 vs A012",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A236",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The Boundary section states the positive half (observe the bus, not component internals) but never the negative one. Added a short 'Not an audit trail' paragraph to that section: observe is a bounded in-memory ring, logfile is explicitly best-effort, console renders and forgets, hooks record nothing, and the only durable approval artefacts are the client-written grant record and the source file core writes.",
    "evidence": "components/observe/main.nim:15-27 (DefaultRingMessages 2000, DefaultRingBytes 16 MiB, in-memory), components/console/main.nim:1-107 (subscribe and render), components/hooks/main.nim:70-83 (runs the command, keeps nothing), core/approval.nim:90 (var/approval-sources); docs/MANUAL.md:2224-2235, 627-633 (store kind approval is client-written)",
    "old_string": "Observation is an administrative capability. A bus capture can contain tool\narguments, model output, approvals, and data from every session. Niffler's\ncurrent trust model is a single trusted user/admin; do not expose the observe\nservice or capture directories to untrusted bus clients.",
    "new_string": "Observation is an administrative capability. A bus capture can contain tool\narguments, model output, approvals, and data from every session. Niffler's\ncurrent trust model is a single trusted user/admin; do not expose the observe\nservice or capture directories to untrusted bus clients.\n\n**Not an audit trail.** None of these components is a durable record of\ndecisions: `observe` keeps a bounded in-memory ring that dies with the\ncomponent, `logfile` is best-effort (at-most-once, `ev.log.>` by default),\n`console` prints and forgets, and `hooks` record nothing. The only durable\nartefacts of the approval gate are the client-written grant record (store\nkind `approval`) and the program source core writes to\n`var/approval-sources/<digest>.nim` — no request/verdict history exists."
  },
  {
    "id": "A238",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The observe table marks observe_dump/observe_send/observe_request as approval-gated but not observe_monitor, which is gated identically — the row reads as a plain read until the prose two paragraphs later. Added the marker.",
    "evidence": "components/observe/main.nim:714 (approval: always on observe_monitor), docs/MANUAL.md:2260 vs :2265-2267",
    "old_string": "| `observe_monitor` | Read nats-server connection/subscription counts and most-subscribed patterns |",
    "new_string": "| `observe_monitor` | Read nats-server connection/subscription counts and most-subscribed patterns; approval-gated — it borrows the operator's monitoring endpoint |"
  },
  {
    "id": "A239",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "The first wave recorded no fix ('FIX: none'): the claim that a direct svc.observe.call client is a trusted bus peer bypassing core policy matches the design — the gate lives in core's dispatch, and the component's own input/subject validation is an implementation detail not worth a MANUAL clause.",
    "evidence": "core/dispatch.nim approval gate vs components/observe/main.nim:366-392 (input/subject validation only); docs/MANUAL.md:2266-2270",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A240",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified in the first wave and still true: NIF_OBSERVE_MONITOR_URL and the per-request HTTP client for /subsz and /connz are exactly what the Monitoring section describes.",
    "evidence": "components/observe/main.nim:719 (NIF_OBSERVE_MONITOR_URL), :727-757 (fresh fetch per request, /subsz?subs=1&limit=1024 then /connz); docs/MANUAL.md:2429-2432",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A241",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The hooks section had no verification line at all, and tests/t_hooks.nim is named nowhere. Added one sentence — scoped to what the test actually covers (the env-to-subject mapping and stdin payload delivery; the finding's 'timeout behaviour' is NOT in that test, so it is not claimed).",
    "evidence": "tests/t_hooks.nim:29-64 (NIF_HOOKS_EVENTS/NIF_HOOKS_EV_SESSION_TURN, published ev.session.turn, asserts the payload reached the hook's stdin), Makefile:517 (test-hooks); docs/MANUAL.md:1168-1170",
    "old_string": "Hook stdout and stderr go to the component's own log, `var/logs/hooks.log`\n(the supervisor redirects child output there) — never into logfile's JSONL,\nwhich persists bus traffic only.",
    "new_string": "Hook stdout and stderr go to the component's own log, `var/logs/hooks.log`\n(the supervisor redirects child output there) — never into logfile's JSONL,\nwhich persists bus traffic only.\n\n`make test-hooks` (`tests/t_hooks.nim`) is the smoke test: it boots the\ncomponent with `NIF_HOOKS_EV_SESSION_TURN`, publishes an `ev.session.turn`\nand checks that the decoded payload reached the hook's stdin."
  },
  {
    "id": "A383",
    "slice": "component: llm",
    "status": "apply",
    "reason": "The shipped-components row named only the hidden `chat` tool. The component registers three hidden tools, none in a conversation's direct set — now named in the row with what each is for, closing the 'no tool inventory' gap without a new section (the deeper resolution/output detail lands in the Context window bullets and the Model catalog paragraph, this batch).",
    "evidence": "components/llm/main.go:1140-1152 (llm_resolve: hidden+runner+10000 ms), :1154-1165 (llm_models_source: hidden only, x-models-source v1 priority 150), :1167-1185 (chat: hidden+runner, timeoutMs from chatTimeoutMs at :1124-1131); docs/MANUAL.md:64",
    "old_string": "| `llm` | Go | required | streaming chat adapter (hidden `chat` tool; `ev.llm.token` deltas; cancellation) — protocols: OpenAI-compatible Chat Completions, OpenAI Codex (ChatGPT OAuth) Responses and Anthropic Messages; `llm-openai` in `components/llm-openai` is the minimal non-streaming example, swap it in via `manifest.yaml` |",
    "new_string": "| `llm` | Go | required | streaming chat adapter — three hidden tools, none of them in a conversation's direct set: `chat` (one inference, `ev.llm.token` deltas, cancellation; `runner`-exempt, timeout `NIF_LLM_TIMEOUT_MS`), `llm_resolve` (the credential-free resolution probe clients call) and `llm_models_source` (the `x-models-source` v1 live-id source the `models` catalog calls) — protocols: OpenAI-compatible Chat Completions, OpenAI Codex (ChatGPT OAuth) Responses and Anthropic Messages; `llm-openai` in `components/llm-openai` is the minimal non-streaming example, swap it in via `manifest.yaml` |"
  },
  {
    "id": "A384",
    "slice": "component: llm",
    "status": "apply",
    "reason": "llm_resolve reports more than the sentence claimed: output/outputSource, protocol, authType and hasKey are in the same result, and the probe fails outright when no provider credential resolves. Extended the bullet.",
    "evidence": "components/llm/main.go:1094-1116 (resolveHandler result keys), :266-300 (resolveProvider -> envProvider errors 'no API key'); docs/MANUAL.md:683-689",
    "old_string": "  fallback if `models` is removed. The result includes secret-free provider,\n  model, catalog and context provenance for interactive clients. See\n  [Model catalog](#model-catalog-models).",
    "new_string": "  fallback if `models` is removed. The result includes secret-free provider,\n  model, catalog, context and output provenance plus the provider's `protocol`,\n  `authType` and `hasKey` for interactive clients; it still needs a resolvable\n  provider, so with no stored and no environment credential it fails instead of\n  reporting a window. See [Model catalog](#model-catalog-models)."
  },
  {
    "id": "A385",
    "slice": "component: llm",
    "status": "apply",
    "reason": "The output window was documented nowhere (grep outputSource: no hits). Added a bullet next to the context-window one: catalog limit.output, else the deliberate 32768 default and why, per-call maxTokens only lowering it, and the Codex lane ignoring it.",
    "evidence": "components/llm/main.go:84-90 (defaultOutput = 32768 with its rationale), :156-176 (resolveOutputWindow: catalog then fallback), :500-503 (maxTokens only lowers), :512-518 (chatCodex call takes no output size) and codex.go:181-215 (codexRequest sets no max output); docs/MANUAL.md:683-691",
    "old_string": "- `session {sessionId, content?, model?, thinking?, title?, cwd?, profile?, discovery?, tools?, maxRounds?, maxCalls?, maxTokens?}` accepts a\n  conversation-scoped model override.",
    "new_string": "- The **output** window is resolved alongside the context window and comes\n  back as `output`/`outputSource`: the catalog's `model.limit.output`, else a\n  deliberate 32768 default — without an explicit cap the provider applies its\n  own server-side cap and truncates long answers mid-stream. A per-call\n  `maxTokens` only ever lowers the resolved value (the expert judge's tiny\n  verdicts), and the Codex lane ignores it entirely.\n\n- `session {sessionId, content?, model?, thinking?, title?, cwd?, profile?, discovery?, tools?, maxRounds?, maxCalls?, maxTokens?}` accepts a\n  conversation-scoped model override."
  },
  {
    "id": "A388",
    "slice": "component: llm",
    "status": "apply",
    "reason": "The max_completion_tokens/DeepSeek half is already in the MANUAL (applied as A176: §Output caps and `finish_reason`), so this row adds the two pieces that were missing — the Anthropic lane always receives the resolved window as `max_tokens`, and the Codex lane sends no cap at all — and folds A389's remaining Codex canonicalization clause into the same paragraph (one edit for both rows; see A389).",
    "evidence": "components/llm/main.go:816-822 (lengthCap: max_tokens only for provider name 'deepseek', else max_completion_tokens), anthropic.go:212 (max_tokens: outputSize), codex.go:180-215 (codexRequest sets no max output-token field), main.go:791-807 (canonicalFinish); docs/MANUAL.md:1069-1080",
    "old_string": "default (8K/64K/128K, by model) applies — send `max_tokens` for DeepSeek\nendpoints. How a stream ended is reported in `finish_reason`: `length` means the\noutput cap cut the reply short (`llm` logs a truncation warning), `tool_calls`\nmeans the model stopped to call tools, and Anthropic's `max_tokens` stop maps\nonto `length` (its `tool_use` stop maps onto `tool_calls`). Two OpenAI-compatible\nerror finishes, `aborted` and `insufficient_system_resource`, arrive with HTTP\n200 and are surfaced as a retryable stream error, not as a reply.",
    "new_string": "default (8K/64K/128K, by model) applies — send `max_tokens` for DeepSeek\nendpoints. The Anthropic lane always receives the resolved window as\n`max_tokens`; the Codex lane is told no cap at all. How a stream ended is\nreported in `finish_reason`: `length` means the output cap cut the reply short\n(`llm` logs a truncation warning), `tool_calls` means the model stopped to call\ntools, and Anthropic's `max_tokens` stop maps onto `length` (its `tool_use` stop\nmaps onto `tool_calls`); Codex's `max_output_tokens` finish and its\n`response.incomplete` event map onto `length` as well, while an unrecognized\nreason passes through unchanged for the log. Two OpenAI-compatible error\nfinishes, `aborted` and `insufficient_system_resource`, arrive with HTTP 200 and\nare surfaced as a retryable stream error, not as a reply."
  },
  {
    "id": "A389",
    "slice": "component: llm",
    "status": "skip",
    "reason": "Folded into A388's edit of the same paragraph: `length`/`tool_calls`, the Anthropic mapping and aborted/insufficient_system_resource as a retryable stream error were already documented in §Output caps and `finish_reason` (A176); the only missing sentences were the Codex `max_output_tokens`/`response.incomplete` mapping and the pass-through of unknown reasons, both appended by A388 rather than as a second overlapping edit.",
    "evidence": "components/llm/main.go:787-807 (canonicalFinish), :1038-1052 (finish_reason in the result, length warning), :774-785 (interrupted finishes); docs/MANUAL.md:1069-1080",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A390",
    "slice": "component: llm",
    "status": "apply",
    "reason": "The value set and the header round-trip were already documented; missing was that the field is only sent when non-empty (which is why an unsupporting provider never sees it) and how each lane spells it. Added. The finding's second half — a session-schema enum that lists only low/medium/high — no longer exists: core/catalog.nim:197 enumerates `max` and conversation.nim:2858 accepts it.",
    "evidence": "components/llm/main.go:432-447 (ReasoningEffort, only sent when non-empty), codex.go:210-213 (reasoning {effort, summary}), anthropic.go:234-236 (thinking + output_config.effort); core/catalog.nim:197-198; docs/MANUAL.md:696-701",
    "old_string": "  value that clears an earlier choice. It is forwarded to the provider as\n  `reasoning_effort` (the `llm` adapter maps it to each protocol's own field)\n  and carried on the conversation header.",
    "new_string": "  value that clears an earlier choice. It is forwarded to the provider as\n  `reasoning_effort` — and only when non-empty, so a provider that does not\n  support the field never sees one; the `llm` adapter maps the value onto each\n  protocol's own form (Codex `reasoning {effort, summary}`, Anthropic\n  `thinking` plus `output_config.effort`) — and carried on the conversation\n  header."
  },
  {
    "id": "A391",
    "slice": "component: llm",
    "status": "apply",
    "reason": "The cancellation sentence named only llm.cancel.<sessionId>. Added the two preconditions (armed only for a streamed call) and the cancelId override that lets auxiliary callers cancel independently.",
    "evidence": "components/llm/main.go:468-487 (subscription gated on args.Stream && cancelID != '', cancelID defaulting to sessionID), components/compaction/main.nim:275-289 (cancelId and sessionId both 'compaction.<sessionId>.<attemptId>'); core/conversation.nim:2016 (the turn's chat call always sets stream: true); docs/MANUAL.md:536-538",
    "old_string": "`ev.session.assistant` event always carries\nthe complete content, so a missed last frame heals itself. Abort an\nin-flight call by publishing to `llm.cancel.<sessionId>`.",
    "new_string": "`ev.session.assistant` event always carries\nthe complete content, so a missed last frame heals itself. Abort an\nin-flight call by publishing to `llm.cancel.<sessionId>` — or, when the caller\npassed a `cancelId`, to `llm.cancel.<cancelId>`: the subscription is armed\nonly for a streamed call (`stream: true`, which core always sets) and uses\n`cancelId` with the session id as its default. Auxiliary callers depend on\nthat separation — compaction cancels\n`llm.cancel.compaction.<sessionId>.<attemptId>` — so a user's turn stop can\nneither kill nor be killed by a summarization call."
  },
  {
    "id": "A392",
    "slice": "component: llm",
    "status": "apply",
    "reason": "The row's core claim is documented (A392's MANUAL half is right), but the paragraph's parenthetical was wrong about who suppresses token frames: only compaction passes `emitTokens: false`, the expert judge sets `stream: false` instead, and the hooks component never calls `chat` at all. Corrected.",
    "evidence": "components/llm/main.go:997-1003 (frame guard: emitTokens() && SessionID != ''), :447-449 (emitTokens defaults true), :923-945 (purpose logged with provider/model/effort/ttft/tok-s), codex.go:333-341 (emitLLMToken); only components/compaction/main.nim:286 sets emitTokens false, components/expert/main.nim:538-552 passes stream: false, components/hooks/main.nim has no chat call; docs/MANUAL.md:529-533",
    "old_string": "non-empty `sessionId` **and** leaves `emitTokens` on — auxiliary callers (the\ncompactor, hooks, the expert peer) pass `emitTokens: false` so their partial\noutput can never be mistaken for assistant text — and one frame is emitted per\nstream chunk that has content or reasoning.",
    "new_string": "non-empty `sessionId` **and** leaves `emitTokens` on — auxiliary callers\nstream internally for cancellation but keep their partial output out of the\nconversation (compaction passes `emitTokens: false`; the expert judge's call is\nnot even streamed, so nothing is emitted either way) — and one frame is emitted\nper stream chunk that has content or reasoning."
  },
  {
    "id": "A395",
    "slice": "component: llm",
    "status": "apply",
    "reason": "History repair (sanitizeMessages/repairToolArgs) was undocumented. Added a paragraph at the end of the streaming discussion: replayed assistant tool_calls arguments are repaired before they are sent, on text only.",
    "evidence": "components/llm/main.go:463 (sanitizeMessages called before resolving) and :681-760 (repairToolArgs, unterminated string/container closed, '{}' fallback); docs/MANUAL.md:538-540",
    "old_string": "`nats sub '>'` attached to the bus shows the harness thinking in real time.",
    "new_string": "History is replayed to the provider verbatim, which is why the adapter\nrepairs it on the way out: an assistant `tool_calls` payload left unterminated\nby a dropped stream (or a buggy writer) has its strings and containers closed,\nand an unsalvageable payload becomes `{}` — a strict backend rejects the whole\nrequest otherwise. The repair reads text only and never executes anything.\n\n`nats sub '>'` attached to the bus shows the harness thinking in real time."
  },
  {
    "id": "A396",
    "slice": "component: llm",
    "status": "apply",
    "reason": "The live-source paragraph described the plugin accurately but never named the tool it rides on. Named `llm_models_source` and added its bounds: one probe per provider+base URL key per 10 minutes, 8 s timeout, memory-only cache, GET {baseUrl}/models, Codex excluded, and the 'no live model data yet' error before the first successful probe.",
    "evidence": "components/llm/models_source.go:33-37 (liveProbeTTL 10 min, liveProbeTimeout 8 s), :57-95 (handler, in-memory patch, 'no live model data yet' at :91), :101-134 (probe scheduling per provider|baseURL key), :136-140 (liveModelsServingIDs excludes Codex), :145-188 (GET {baseUrl}/models); docs/MANUAL.md:2057-2062",
    "old_string": "tool probes an endpoint on demand with a stored or explicit credential (the\nconnect form), and the `llm` component registers an `x-models-source` plugin\n(priority 150) whose patch adds the ids each provider was observed serving\n(probed in the background after chats, 10-minute TTL) so the whole catalog\nconverges on what endpoints really list.",
    "new_string": "tool probes an endpoint on demand with a stored or explicit credential (the\nconnect form), and the `llm` component's hidden `llm_models_source` tool\nregisters as an `x-models-source` plugin (priority 150) whose patch adds the\nids each provider was observed serving. The probe behind it is one background\n`GET {baseUrl}/models` per catalog-provider + base-URL key every 10 minutes\n(8 s timeout; the ids live in memory only, so a restart forgets them until the\nnext chat, and the tool answers `no live model data yet` until a probe has\nsucceeded; the Codex lane is excluded because the ChatGPT backend exposes no\nsuch route) so the whole catalog converges on what endpoints really list."
  },
  {
    "id": "A397",
    "slice": "component: llm",
    "status": "already",
    "reason": "Fixed before this batch: the progressive-discovery model section now documents the flag — a hidden tool that also carries `x-harness.runner: true` is exempt from the frozen tool allowlist (added with the exposure-flags pass, ledger A114). Precision note, not a MANUAL defect: core's check reads the exemption off any session allowlist (a root session's `tools` list too) and additionally exempts `chat` plus the store quartet by name — a slightly wider claim than the sentence makes.",
    "evidence": "docs/MANUAL.md:1729; core/dispatch.nim:1544-1548 (schema runner+hidden exemption), :1536-1538 (chat, put/get/list/del exempt by name); components/llm/main.go:1140-1152 (runner: true on llm_resolve and chat), :1154-1165 (llm_models_source, hidden only)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A399",
    "slice": "component: llm",
    "status": "apply",
    "reason": "`accountId` was listed in the provider object but never explained. Gave it one clause next to `stripPrefix` (which is already documented): the ChatGPT account id the Codex headers carry, derived from the OAuth token when the record has none, with the failure text when neither exists.",
    "evidence": "components/llm/main.go:62-73 (provider struct: accountId, stripPrefix), main.go:508-512 + :669-674 (stripModelPrefix), codex.go:37-45 (accountID from the record else the JWT, else the error), codex.go:391-420 (codexAccountID reads chatgpt_account_id); docs/MANUAL.md:376",
    "old_string": "`authType` defaults to `api_key`; `stripPrefix` rewrites a namespaced id (`alibaba/glm-5.2` → `glm-5.2`) for gateways that route on the canonical id.",
    "new_string": "`authType` defaults to `api_key`; `stripPrefix` rewrites a namespaced id (`alibaba/glm-5.2` → `glm-5.2`) for gateways that route on the canonical id; `accountId` is the ChatGPT account id the Codex lane's headers carry, derived from the OAuth token when the record does not hold one (with neither, the call fails with `OpenAI Codex OAuth token has no ChatGPT account id; sign in again`)."
  }
]
