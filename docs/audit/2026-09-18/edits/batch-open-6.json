[
 {
  "id": "A107",
  "slice": "(X) Claims that are simply wrong",
  "status": "already",
  "reason": "The marker sentence no longer names `skills` and no longer claims one shared four-file set: it names only `lsp` and `repomap`, with their real build-file sets, and says `skills` uses fixed directories only — exactly the requested fix.",
  "evidence": "docs/MANUAL.md:337-342: \"The `lsp` component additionally treats build files (`go.mod`/`go.work`, `tsconfig.json`/`package.json`, `*.nimble`/`config.nims`, `Cargo.toml`) as repo *markers* — where to walk from, not configuration it parses, while `repomap` lists such marker files as bare entries in its map; `skills` uses fixed directories only.\" Applied by row A022 (edits/applied-ids.txt; applied.md \"A022 | precedence paragraph: markers are lsp/repomap only\"). Code: components/lsp/roots.nim:29-38 (per-language marker sets), components/repomap/main.nim:132-136 (`package.json`, `Cargo.toml`, `go.mod`, `config.nims` as bare map entries), components/skills/main.nim:189-220 (fixed directory list, no build-file walk).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A108",
  "slice": "(X) Claims that are simply wrong",
  "status": "already",
  "reason": "The unenforced \"≤100 servers per harness\" claim is gone; the MCP chapter states no server-count ceiling.",
  "evidence": "`grep -n 'per harness\\|servers per\\|≤100' docs/MANUAL.md` → no match; the old text was MANUAL:1116-1118 of a7a8d25 (\"… `bridge` reserved; ≤100 servers per harness\") and components/mcp/main.go has no such check (registry ops page with `limit` default 10 / max 20, main.go:296-302). Removed by batch-8 (applied.md: \"mcp (`mcp_search`, the unenforced `≤100` claim removed, deferred tool sets)\").",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A109",
  "slice": "(X) Claims that are simply wrong",
  "status": "already",
  "reason": "The `/limit` bullet now splits the three soft limits instead of reading as if all were checked before every dispatch.",
  "evidence": "docs/MANUAL.md:646-648: \"Seconds is checked before every tool dispatch (one `bash` call can outlast a whole round); rounds and tokens are checked before the next LLM round.\" Fixed in commit 04100bb (`git log -L 644,650:docs/MANUAL.md` shows the old \"(checked before every tool dispatch, not only between rounds)\" replaced). Code: core/conversation.nim:2190-2196 (seconds checked on the dispatch path) vs :1839-1858 and :2403 (round and call budgets at round boundaries).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A112",
  "slice": "(X) Claims that are simply wrong",
  "status": "already",
  "reason": "Superseded in the same pass: A035 (batch-open-2) rewrites this very paragraph, and its UI parenthetical was corrected with this row's evidence (web UI renders `cached/prompt` numbers, TUI chip reads `cache NN%`). Anchor no longer applies after A035 lands. Original finding: `ev.session.status` nests its cache economics under `cache {prompt, read, hitRate}`; `cacheHitTokens`/`cacheHitRatio` exist nowhere. The UI parenthetical is wrong too: the web UI renders `⚡ <cached>/<prompt> cached` per message (numbers, not a percentage) and the TUI's chip reads `cache NN%`.",
  "evidence": "core/conversation.nim:2239-2242 `statusEv[\"cache\"] = %*{\"prompt\": p.cachePrompt, \"read\": p.cacheRead, \"hitRate\": …}`; ui/frontend/src/lib/providers.ts:42-46 (`cache?: {prompt, read, hitRate}`, no cacheHit* fields); ui/frontend/src/views/Chat.svelte:742 renders `⚡ {…cached_tokens}/{…prompt_tokens} cached`; ~/git/niffler-tui/tui/context_status.go:117 with tui/i18n.go:221 (`\"runtime.cache\": \"cache {0}%\"`) and i18n.go:233 (`status.detailCache` = \"cache hits: {0} of {1} prompt ({2})\"). Stale sibling doc (NOT the MANUAL, reported only): components/hooks/README.md:35 still lists cacheHitTokens/cacheHitRatio.",
  "old_string": "  the status event also carries `cacheHitTokens` and `cacheHitRatio` — with\n  the frozen prompt prefix most prompt tokens should be cached after the\n  first request, so a low ratio is a signal worth noticing (the web UI shows\n  `⚡ NN% cached` per message; the TUI status line shows a `⚡ NN% cached`\n  chip).",
  "new_string": "  the status event also carries `cache {prompt, read, hitRate}` — with\n  the frozen prompt prefix most prompt tokens should be cached after the\n  first request, so a low ratio is a signal worth noticing (the web UI shows\n  `⚡ <cached>/<prompt> cached` per message and the cumulative split in\n  `/info`; the TUI shows a `cache NN%` chip in its header and the same split\n  in `/status`)."
 },
 {
  "id": "A113",
  "slice": "(X) Claims that are simply wrong",
  "status": "already",
  "reason": "This row is the aggregate of four content drifts; all four are fixed in the current tree, so nothing is left to correct here.",
  "evidence": "(1) approval-gated list: docs/MANUAL.md:573-581 now names `build`, `spawn`/`kill`/`remove`, `agent_ask`, `lsp_registry`, `mcp_add`/`mcp_edit`/`mcp_remove`/`mcp_refresh`, `process_start`/`process_kill`, `skill_install`/`skill_remove`, every `provider_*`/`observe_*` (the old list at a7a8d25:457-467 held 26 dotted names and missed these ten). (2) `/limit`: docs/MANUAL.md:608-610 now separates the 5-minute tool-approval window (`timeoutMs`, 300 s) from the 120 s keep-going window. (3) orphaned `nats-server`: docs/MANUAL.md:2847 (fixed in 04100bb). (4) web UI delete: ui/frontend/src/views/Sessions.svelte:55-65 sends core's `conversation_delete` instead of trimming store records (commit 7a7b724, recorded in docs/audit/2026-09-18/code-bugs.md).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A207",
  "slice": "Wrong or misleading claims (in this slice)",
  "status": "already",
  "reason": "The Troubleshooting row no longer says a SIGKILLed core is the only cause; it names the three real ones, matching the finding's fix verbatim in spirit.",
  "evidence": "docs/MANUAL.md:2847: \"| orphaned `nats-server` | a manually started `nats-server`, a non-Linux host (no PDEATHSIG to reap it), or a stale `var/nats-pid` left by SIGKILL — check the pid file (core verifies pid + comm, so a stale file is ignored), then `pkill -f nats-server` |\" — rewritten in commit 04100bb (`git log -L 2845,2848:docs/MANUAL.md`). Same mechanism the finding cites: components/nats/main.go:15-18, sdk/go/pdeathsig_linux.go:17.",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A208",
  "slice": "Wrong or misleading claims (in this slice)",
  "status": "already",
  "reason": "Superseded in the same pass: A191 (batch-open-2) states the same thing in this paragraph — the count is a `reg.publish` registration, not the 20 s `ui` lease, and a SIGKILLed client keeps an autostarted core (and core's approval reachability) up until the catalog drops it. Anchor no longer applies after A191 lands. Original finding: The shutdown paragraph still reads as though the 20 s `ui` lease decides liveness. It does not: the autostarted core counts catalog registrations carrying the `client` flag, and the ui registry (register/renew/release leases, per-conversation ownership) is a separate mechanism — say so in one sentence.",
  "evidence": "core/catalog.nim:795-799 `proc clientCount*` — \"Registered interactive frontends (reg.publish with \"client\": true). An autostarted core exits when this returns to zero\"; core/niffler.nim:677-686 uses only `cat.clientCount()` for the idle/boot-grace exit; the ui registry is core/uireg.nim (leases, 20 s) and is described separately at docs/MANUAL.md:216-227.",
  "old_string": "Interactive frontends register `\"client\": true` (the SDK's `interactive()`\n/ `Component.Client` marker). An **autostarted** core counts them: when the\nlast one departs it shuts down after `NIF_AUTOSTART_IDLE_S` (default 10s —",
  "new_string": "Interactive frontends register `\"client\": true` (the SDK's `interactive()`\n/ `Component.Client` marker). An **autostarted** core counts those\nregistrations — the flag on `reg.publish`, cleared by `reg.depart` or by\ncore's supervisor when the process dies; the 20 s `ui` lease of [Clients and\nthe UI registry](#clients-and-the-ui-registry) is separate bookkeeping\n(which window owns which conversation), not this count. When the last one\ndeparts it shuts down after `NIF_AUTOSTART_IDLE_S` (default 10s —"
 },
 {
  "id": "A260",
  "slice": "A. Documented but stale / wrong",
  "status": "already",
  "reason": "The env row no longer says \"when higher\" — it says a positive value wins over both defaults, which is what the code does (the env value is assigned last, unconditionally).",
  "evidence": "docs/MANUAL.md:434: \"| `NIF_MCP_PROBE_TIMEOUT_MS` | timeout for one real-connect probe in `mcp_add`/`mcp_edit`: when set (positive) it wins over both the 30 s default and the server's own `timeoutMs` | `30000` |\" — rewritten in commit 04100bb (`git log -L 434,434:docs/MANUAL.md`). Code: components/mcp/main.go:463-472 (`timeout := 30 * time.Second`; `cfg.TimeoutMs` only when > 30_000; then `NIF_MCP_PROBE_TIMEOUT_MS` overrides whenever ms > 0); the MCP chapter repeats it correctly at docs/MANUAL.md:1634 (\"Validation timeout: 30s or `timeoutMs` if higher (`NIF_MCP_PROBE_TIMEOUT_MS` overrides)\").",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A261",
  "slice": "A. Documented but stale / wrong",
  "status": "already",
  "reason": "The \"all engines enforce single-writer the same way\" sentence is gone; file-backed engines and the lock-free tidb engine are now distinguished, which is also what the section's own tidb bullet says.",
  "evidence": "docs/MANUAL.md:268-272: \"The file-backed engines (`sqlite`, `barrel`) enforce single-writer the same way: one process owns the file (flock; kernel-released on crash), everyone else speaks envelopes. `tidb` has no file to lock — the cluster is shared state by design, and row locks plus the rev counter arbitrate between harnesses.\" — rewritten in commit 04100bb (row A106). Code: components/store-sqlite/main.go:136 `acquireLock(dbPath + \".lock\")` (var/store.db.lock), components/store/main.nim:48 `let lockPath = path & \".lock\"` (var/barrel-db.lock), components/store-tidb/main.go:99-142 (no flock).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A263",
  "slice": "A. Documented but stale / wrong",
  "status": "already",
  "reason": "The \"identical in the Nim SDK, Go SDK and the UI bridge\" claim and the ambiguous load order are gone: the .env section now names the winner and splits the bridge out.",
  "evidence": "docs/MANUAL.md:463-467: \"Loading rules (same in the Nim, Go and TypeScript SDKs): existing shell environment **always wins** over `.env`; the SDKs load the current directory's `.env` first and the harness root's second, first definition of a key wins. The desktop UI bridge loads them in the opposite order (harness root, then cwd — `ui/bridge.go`), so there the root file wins.\" — applied as row A105 (04100bb). Code: sdk/niffler/sdk.nim:790 `loadDotEnv(\".env\", getEnv(\"NIF_ROOT\", \".\") / \".env\")`, sdk/go/component.go:414, sdk/ts/src/component.ts:242, sdk/dotenv.nim:56 `if key.len > 0 and not existsEnv(key)` (first definition wins), ui/bridge.go:71 `sdk.LoadDotEnv(filepath.Join(harnessRoot(), \".env\"), \".env\")`.",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A265",
  "slice": "A. Documented but stale / wrong",
  "status": "already",
  "reason": "The resolution source is stated where it matters: the skills source table says `<repo>/skills` is the checkout the binary was compiled in, with `$NIF_ROOT/skills` only as the relocated-deployment fallback — the requested fix, in the MANUAL's own words.",
  "evidence": "docs/MANUAL.md:945: \"| bundled | `<repo>/skills` — the checkout this binary was compiled in (a build-time path); `$NIF_ROOT/skills` is the fallback for relocated deployments, and `NIF_SKILLS_BUNDLED_DIR` overrides both — never removable |\" (applied as row A506, commit 04100bb; `git log -L 945,945:docs/MANUAL.md`). Code: components/skills/main.nim:185-198 `bundledSkillsDir` — `NIF_SKILLS_BUNDLED_DIR` → `currentSourcePath().parentDir.parentDir.parentDir / \"skills\"` → `root / \"skills\"`. The env row at docs/MANUAL.md:386 already links the same three-way resolution (\"replacing `<repo>/skills` and its `$NIF_ROOT/skills` fallback\").",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A019",
  "slice": "State and configuration",
  "status": "already",
  "reason": "The state-table Environment row now carries the inheritance caveat: a `.env` edit applies on respawn, but a variable exported in the shell that started core needs a harness restart.",
  "evidence": "docs/MANUAL.md:325: \"… | process lifetime — components read env once at boot, so a change needs `core.kill` + `core.spawn`. A variable *exported in the shell that started core* is inherited by every child and needs a harness restart instead |\" — the A019/A110/A264 edit recorded in applied.md and landed in 04100bb (the same wording as the hooks row at docs/MANUAL.md:430). Code: core/supervisor.nim:117-124 (`startChild`, children inherit the parent environment); children still re-read `.env` themselves at boot (sdk/niffler/sdk.nim:790, core/session.nim:37).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A023",
  "slice": "State and configuration",
  "status": "already",
  "reason": "Auditor class `verified` — the sentence the row quotes is still exactly what the code does; no change owed.",
  "evidence": "docs/MANUAL.md:318-320: \"chosen by lifetime: boot decisions are environment, identity/selection is the store, per-conversation choice is the conversation header, display is the browser, and everything derived is `var/`\"; the backing fields are the header rows at docs/MANUAL.md:326-328 (conversation header: model/modelOverride/thinking/profile/title/budgets), core/conversation.nim:2784-2786 (header fields) and core/dispatch.nim:394-402 (`profile` records). worklist.tsv class for A023 is `verified`.",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A460",
  "slice": "State and configuration",
  "status": "apply",
  "reason": "Half a line the state table was missing: where a background process's output actually lands (`pN.out`/`pN.err`), that those spools are swept at boot, and that ids keep counting up instead of restarting at `p1`. It is the map an operator debugs with.",
  "evidence": "components/processes/main.nim:293-296 — `let id = \"p\" & $gNextId` … `outPath = rootVarDir(\"processes\") / (id & \".out\")`, `errPath = … / (id & \".err\")`; :106-124 `bootSweep` adopts `nextId` from `registry.json` and kills orphans from the previous life; :130-141 deletes every `*.out`/`*.err` in `var/processes/` at boot. The state table currently says only \"`processes/` spools\" (docs/MANUAL.md:330), and the chapter's `process_start` row only \"spool files under `var/processes/`\" (docs/MANUAL.md:1458).",
  "old_string": "`nats-url`/`nats-pid` bus claiming, `processes/` spools, `repomap-tags/` per-file tags cache",
  "new_string": "`nats-url`/`nats-pid` bus claiming, `processes/` spools (`pN.out`/`pN.err` per start, wiped at boot; ids continue from the persisted counter instead of restarting at `p1`), `repomap-tags/` per-file tags cache"
 },
 {
  "id": "A083",
  "slice": "System prompt (systemprompt)",
  "status": "already",
  "reason": "Auditor class `verified`, and the re-check on the post-audit tree confirms every number: frozen-forever in the header, 500 ms probe then the 8 s budget only when the catalog says the component is registered, 200 KB truncation on both sides, agent pre-fetch.",
  "evidence": "docs/MANUAL.md:2171-2183 (\"Frozen per conversation\" / \"Fallback. Component absent, slow (500 ms probe, then an 8 s budget when the catalog says it is registered)\" / \"Cap. Answers are truncated at 200 KB (both sides)\" / \"Agent pre-fetch\"). Code: core/conversation.nim:67 `systemPromptTimeoutMs = 8_000`, :88-97 (`askSystemPrompt(ct, 500, …)` then the full budget only `if ct.cat.components.hasKey(\"systemprompt\")`), :99-101 (truncate at 200_000), :2606-2613 (header `systemPrompt` / the call's `systemPrompt` arg), components/systemprompt/main.nim:29 `maxPromptLen = 200_000`.",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A084",
  "slice": "System prompt (systemprompt)",
  "status": "already",
  "reason": "Auditor class `verified`; `baseprompt.txt` is still compiled in with `staticRead`, `$ROOT`-substituted, no runtime file dependency, so \"editing it is rebuild + respawn\" holds.",
  "evidence": "components/systemprompt/main.nim:36 `const basePrompt = staticRead(\"baseprompt.txt\")`; docs/MANUAL.md:2187-2191 (\"`components/systemprompt/baseprompt.txt` — the product prompt … `$ROOT`-substituted, baked into the binary at compile time via `staticRead`. Editing it is rebuild + respawn; there is no runtime file dependency.\").",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A085",
  "slice": "System prompt (systemprompt)",
  "status": "already",
  "reason": "Auditor class `doc-edit` with FIX \"none — this is the most accurate mechanism section in the file\": the context-file walk, shadow rule, ancestor order and `<workspace>` tail all still match the component.",
  "evidence": "docs/MANUAL.md:2194-2216 (candidate list and first-hit-wins, ancestor walk, worktree shadow rule, lazy `AGENTS.local.md` append, `<workspace>` tail only when cwd ≠ root). Code: components/systemprompt/main.nim:44-45 `candidates = [\"AGENTS.override.md\", \"AGENTS.md\", \"AGENTS.MD\", \"CLAUDE.md\", \"CLAUDE.MD\"]`, :46 `localCandidate = \"AGENTS.local.md\"` (additive, not shadowing), :51 (AGENTS.md shadows a CLAUDE.md beside it), :114-115 (worktree shadow rule), :248-270 (truncation marker).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A098",
  "slice": "Common tasks",
  "status": "already",
  "reason": "Auditor class `verified`; every command in the block still exists as a Makefile target, so the list needs nothing.",
  "evidence": "docs/MANUAL.md:2790-2807 block vs Makefile targets: install:374, uninstall:377, install-ui:383 (= ui-install:349), install-tui:385, install-lsp:661, test:467, test-server:472, test-ui:482, doctor:605, ram:399, down-here:420, clean:581, build:315. worklist.tsv class for A098 is `verified`.",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A099",
  "slice": "Common tasks",
  "status": "already",
  "reason": "Auditor class `doc-edit` with FIX \"none\"; re-reading the script confirms all three claims — PSS not RSS, membership by executable path, workload children of `bash` excluded.",
  "evidence": "docs/MANUAL.md:2825-2838 (\"Membership is by executable path (`*/var/bin/*`, `niffler-ui`), not the process tree … Read PSS, not RSS … Workload children of the `bash` tool (compilers, test binaries) are excluded by design.\") vs scripts/niffler-ram.sh:8 (membership rule: executable lives in a Niffler bin dir), :20-22 (RSS double-counts file-backed pages, PSS from smaps_rollup is the honest total; workload children of the bash tool excluded), Makefile:399-400 (`ram: bash scripts/niffler-ram.sh`).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A100",
  "slice": "Common tasks",
  "status": "already",
  "reason": "Auditor class `verified`; the headless-service-mode claim still matches the boot branch and the approval gate.",
  "evidence": "docs/MANUAL.md:2812-2815 (\"Headless service mode (no tty, for UIs/automation): `NIF_NATS_URL=... NIF_OPENAI_API_KEY=... ./var/bin/niffler < /dev/null` — serves `svc.core.call`; approval-requiring tools are denied unless a UI is attached or `NIF_AUTO_APPROVE=1`.\"). Code: core/niffler.nim:654-666 (`if isatty(stdin) and not autostart` → admin shell, else \"core: service mode (no tty) — serving svc.core.call\"), core/approval.nim:271-296 (`NIF_AUTO_APPROVE` bypass; tty fallback only when `clientCount() == 0`, otherwise `askUi` → deny with no client).",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A153",
  "slice": "Contents",
  "status": "skip",
  "reason": "A trim/restructure recommendation, not a wrong claim: the subject block is the MANUAL's one-screen map and already defers to WIRE.md as normative (\"Core speaks exactly one protocol: JSON envelopes over NATS (details in [WIRE.md](WIRE.md))\"); the audit files trim rows under the (Z) pass, which the parent explicitly deferred.",
  "evidence": "docs/MANUAL.md:478-569 — the section is 93 lines: a 43-line subject/payload block plus the streaming paragraph, the console/cli paragraphs; the WIRE.md pointer is its first sentence. applied.md, \"Deferred\": \"A017, A018, A152 — trim recommendations … This is the (Z) pass, not a wording fix\"; worklist.tsv gives A153 source group \"(Z) Sections to trim to a pointer (other docs own them)\". Cutting it to five lines here would also drop the `ev.*` inventory that the still-open observation rows (A257) want completed.",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A186",
  "slice": "Shipped components / Layout (",
  "status": "already",
  "reason": "Auditor class `doc-edit` with FIX \"no change\": the `core/` row still describes the session runner and the loop it drives correctly.",
  "evidence": "docs/MANUAL.md:37: \"| `core/` | the control plane: system harness (`niffler.nim`: bus bootstrap, supervisor, catalog, dispatch) + the session runner (`session.nim`) and the turn loop it drives (`conversation.nim` — the largest module — plus `compaction.nim`, `approval.nim`, `retry.nim`, `uireg.nim`, `tty.nim`) |\"; core/session.nim:1-15 (runner spawn/resume), core/conversation.nim (turn loop). The row was rewritten by the A003 edit applied in this pass.",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A266",
  "slice": "Could not determine",
  "status": "already",
  "reason": "The open question was \"does `dialog` count as shipped?\" — the manual no longer enumerates component sources under `components/` (it defers to the Shipped components table), and that table carries `dialog` with `—` in the Manifest column plus the exact lifecycle, so nothing is ambiguous or false.",
  "evidence": "docs/MANUAL.md:38 (\"shipped component sources — one directory per component (Nim, Go, TypeScript and one bash demo); the inventory is the [Shipped components](#shipped-components) table below\" — the old enumeration at a7a8d25:36 is gone, applied as A001/A141) and docs/MANUAL.md:86 (\"| `dialog` | bash | — | demo component written entirely in bash … Ships in `var/bin/dialog` (`make build`) but is **not autostarted**; spawn it with `spawn {name: \"dialog\", …}`\"). Code: no `dialog` entry in manifest.yaml; built by niffler.nimble:46 and Makefile:299-312.",
  "old_string": "",
  "new_string": ""
 }
]