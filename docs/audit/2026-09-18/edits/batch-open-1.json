[
 {
  "id": "A314",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The section documents the frame/judgment economics (MaxJudgmentsPerTurn, EvalCooldownMs, MaxActivities, MaxField, MaxReasoningTail, MaxMessage) but not the judge call's own cost knobs: the 1536-token output cap, reasoning_effort \"low\" and the 120 s timeout, all compile-time constants. One sentence at the end of the numbers paragraph fixes it, including the best-effort nature of the cap.",
  "evidence": "components/expert/main.nim:53 (ChatTimeoutMs = 120_000), :54 with its best-effort comment (:55-62) (JudgeMaxOutputTokens = 1536) and :63 (JudgeReasoningEffort = \"low\"); :538-548 builds chatArgs with maxTokens/reasoning_effort; the MANUAL never mentions JudgeMaxOutputTokens, JudgeReasoningEffort or ChatTimeoutMs",
  "old_string": "and caps an advisory message at 1 200 chars (`MaxMessage`).",
  "new_string": "and caps an advisory message at 1 200 chars (`MaxMessage`). The judge call\nitself is capped at 1 536 output tokens (`JudgeMaxOutputTokens`), asks for\n`reasoning_effort: \"low\"` (`JudgeReasoningEffort`) and times out after 120 s\n(`ChatTimeoutMs`) — all three are compile-time constants, so changing one means\nediting `components/expert/main.nim` and rebuilding. The token cap is\nbest-effort: a gateway may ignore it."
 },
 {
  "id": "A315",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The prefix is the component's main cost model, but the section never says how big it may get. Name the 80 % fill ratio, the 8 000-token observation reserve and the llm_resolve sizing at the phrase that introduces the prefix.",
  "evidence": "components/expert/main.nim:72 (PrefixFillRatio = 0.80) and :76 (PrefixObsReserveTokens = 8_000); :386-410 (buildKnowledge asks llm.llm_resolve for the judge model's context) with :411-413 (prefixBudgetTokens = int(ctx * PrefixFillRatio) - PrefixObsReserveTokens)",
  "old_string": "cache-stable knowledge prefix + one ephemeral observation, no tools) whether",
  "new_string": "cache-stable knowledge prefix — sized from `llm.llm_resolve` to 80 % of the\njudge's context window minus an 8 000-token reserve for the observation and the\nverdict, and cached per follow — plus one ephemeral observation, no tools)\nwhether"
 },
 {
  "id": "A316",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The section says \"knowledge prefix\" without saying what is in it, which is the component's trust boundary and the reason a steer can name a session-visible tool. One short paragraph after the design invariants names policy + reviewed bundled skills + the observed session's frozen exposure/allowlist + discover hints.",
  "evidence": "components/expert/main.nim:68 (SkillAllowlist) with :296-322 (skillKnowledge refuses any copy whose source is not \"bundled\"); :324-358 (fetchToolHints filters the catalog by the session's frozen direct set and allowlist); :360-384 (renderPrefix assembles policy + skills + live tools + on-demand hints + the allowlist line); :386-424 (buildKnowledge)",
  "old_string": "approval-gated work stays with the working session's human gate.",
  "new_string": "approval-gated work stays with the working session's human gate.\n\nThe prefix holds the component's own policy, the three reviewed bundled skills\n(`niffler-tools`, `niffler-fabric`, `niffler-harness` — a project or home skill\nthat shadows a bundled name is refused, so that allowlist is the trust\nboundary), then the observed session's own tool view: its frozen direct\nexposure and tool allowlist (read from `core.prompt_preview`) plus the on-demand\ntools it could `discover`. Never the global LLM toolset — that would overstate\nwhat an older or allowlisted session can actually call."
 },
 {
  "id": "A317",
  "slice": "component: expert",
  "status": "apply",
  "reason": "A reader looks for an `expert` row in the environment table and finds none; say in the section that there is no configuration of its own, and name the one variable that changes what an operator can see.",
  "evidence": "components/expert/main.nim has no getEnv/NIF_* read at all (the single match, :812, is a doc comment about NIF_LLM_PROVIDERS); the only knob over its output is the SDK log threshold sdk/niffler/sdk.nim:509-519 (NIF_LOG_LEVEL, default \"info\", suppresses lower levels before publishing), documented in the MANUAL env table at :398 and :2408",
  "old_string": "Design invariants: the working session never waits for the expert",
  "new_string": "The component has no configuration of its own: no `NIF_*` variable changes how\nit follows or judges (`NIF_LOG_LEVEL` only decides whether its own\n`ev.log.expert` lines are visible).\n\nDesign invariants: the working session never waits for the expert"
 },
 {
  "id": "A318",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The row documents the per-follow override but not what the judge runs on when it is omitted. One clause fixes it and states the cost intent.",
  "evidence": "components/expert/main.nim:538-548 (model/provider are added to chatArgs only when set, so the judge otherwise uses llm's default backend); :800-828 (expert_follow stores the per-follow model/provider); components/llm/main.go:374-382 (activeStoredProvider, re-read per chat call); components/provider/main.go:118-137 (environmentProvider)",
  "old_string": "`model`/`provider` override the judgment calls for that follow. Approval-gated. |",
  "new_string": "`model`/`provider` override the judgment calls for that follow; without them the\njudge runs on `llm`'s default backend (the active provider), which keeps judge\ncost off the worker's model. Approval-gated. |"
 },
 {
  "id": "A319",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The section names only two of the runner's seven rejection reasons. Add the one-advisory-per-turn limit and the exact-repeat rejection, which are what an operator sees when good advice is not folded.",
  "evidence": "core/dispatch.nim:1154 (pumpAdvise) with the rejection reasons at :1184-1200 — bad-envelope, no-active-turn, wrong-session, stale-turn, empty, duplicate, advisory-limit (:1199 rejects the turn's second advice, activeTurn.advisories >= 1)",
  "old_string": "(`stale-turn`/`no-active-turn`), never queued into the next turn. Accepted",
  "new_string": "(`stale-turn`/`no-active-turn`, plus `wrong-session`, `empty`, `duplicate` for an\nexact repeat of the last advice and `advisory-limit` once the turn already has\none), never queued into the next turn. Accepted"
 },
 {
  "id": "A320",
  "slice": "component: expert",
  "status": "apply",
  "reason": "\"high-confidence steers naming live, non-hidden tools\" hides half the gates. The added clause names the ones that silence most steers: normalization, session visibility, the message must name the tool, and the anti-repeat gate.",
  "evidence": "components/expert/main.nim:599-664 — confidence == high :600; MaxMessage cap :605; non-empty tools :609-612; normalization of backticks / component.tool :619-627; tool must be in liveTools|discoverable :630-634; tool must appear in the message :636-641; >=1 named tool not already in this turn's activities :650-664",
  "old_string": "the evidence warrants a steer. Only high-confidence steers naming live,\nnon-hidden tools are delivered, through the turn-bound\n`svc.session.<id>.advise` request/reply surface: the runner accepts advice",
  "new_string": "the evidence warrants a steer. Only high-confidence steers naming live,\nnon-hidden tools are delivered, and only if the gates hold: the steer carries a\nnon-empty `tools` list and a non-empty message within the `MaxMessage` cap, each\nname is normalized (backticks stripped, `component.tool` reduced to the tool),\nmust be visible to that session, must appear in the message text, and at least\none of them must be a tool the worker is not already using in this turn — a\nsteer naming only tools already in the frame is read as silence, not as a change\nof tool. Delivery is through the turn-bound\n`svc.session.<id>.advise` request/reply surface: the runner accepts advice"
 },
 {
  "id": "A321",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The section never says that nothing about a follow is durable. One clause in the invariants says it and names the single durable artifact.",
  "evidence": "components/expert/main.nim:222 (gFollows = initTable holds every frame, prefix and counter; the component makes no store request anywhere); core/conversation.nim:1086-1110 (drainAdvisories appends each accepted advice as a marked user message that the persister writes) with :248-286",
  "old_string": "transcript (every judgment is stateless); fail closed (any parse/validation/",
  "new_string": "transcript (every judgment is stateless) and nothing durable in the component\nitself — frames, prefixes and counters live in the process, so a restart drops\nevery follow and re-arming is explicit; the only stored artifact of an accepted\nsteer is the folded message record in the followed conversation); fail closed\n(any parse/validation/"
 },
 {
  "id": "A322",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The section says the component watches `ev.session.*` without naming what actually feeds the frame or when it is dropped. One parenthetical and one clause fix both.",
  "evidence": "components/expert/main.nim:709-793 (onSessionEvent — turn start resets the frame and refreshes knowledge :732-733; turn done clears it :737; toolcall start/done append activities capped at MaxActivities :747/:761; assistant text stored :777; the context branch trips an evaluation at >=80 % used :785; token deltas fill the reasoning tail :790)",
  "old_string": "`ev.session.*` events into a bounded per-session in-memory current-turn\nframe, and asks an LLM judge",
  "new_string": "`ev.session.*` events (toolcall start/done — the judgment trigger — turn\nstart/done, assistant text, the reasoning tail of token deltas, and a\ncontext-pressure trip at 80 % of the window) into a bounded per-session\nin-memory current-turn frame that is cleared at turn done, so no evidence ever\ncrosses a turn, and asks an LLM judge"
 },
 {
  "id": "A323",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The audit trail is the operator's only way to answer \"why was it silent?\". One sentence after the judge-lane sentence.",
  "evidence": "components/expert/main.nim:588-594 (every parsed judgment is logged with comp.log(\"info\", \"judgment\", ...) carrying action, reason, message and confidence — silence reasons included)",
  "old_string": "global: one judgment in flight, shared cooldown, per-session latest-state\ncoalescing.",
  "new_string": "global: one judgment in flight, shared cooldown, per-session latest-state\ncoalescing. Every parsed judgment — silence included — is published as an\n`ev.log.expert` line (action, reason, message), which is how an operator answers\n\"why was it silent?\"."
 },
 {
  "id": "A324",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The row lists the counters but not the rest of the payload the tool returns. Extend the row with the prefix diagnostics and the judge token totals.",
  "evidence": "components/expert/main.nim:874-906 (the per-follow reply carries knowledgeVersion, liveTools, skills, prefixChars, prefixBudgetTokens :895, the counters and tokens{prompt,cached,completion} :903-905); :908-925 (the aggregate, \"targets\" at :910)",
  "old_string": "With `session_id`: that follow's frame, knowledge version, and per-session counters (judgments, silences, steers, accepted, rejected, staleDrops, errors). Without: followed targets plus lifetime diagnostics. |",
  "new_string": "With `session_id`: that follow's frame, knowledge version, per-session counters (judgments, silences, steers, accepted, rejected, staleDrops, errors), the prefix diagnostics (`liveTools`, `skills`, `prefixChars`, `prefixBudgetTokens`) and judge token totals (`tokens {prompt, cached, completion}`). Without: followed targets plus lifetime diagnostics (the same counters and token totals, lifetime). |"
 },
 {
  "id": "A325",
  "slice": "component: expert",
  "status": "apply",
  "reason": "`expert_follow` is the only name in the on-demand orchestration bullet, but all four tools carry `x-harness.onDemand`. Naming the family is the fix.",
  "evidence": "components/expert/main.nim:831-832, 851, 871, 925 (x-harness onDemand set on each of the four tools); docs/MANUAL.md:1918 — \"Orchestration: `fabric`, the `agent_*` tools, `expert_follow`.\"",
  "old_string": "- Orchestration: `fabric`, the `agent_*` tools, `expert_follow`.",
  "new_string": "- Orchestration: `fabric`, the `agent_*` and `expert_*` tools."
 },
 {
  "id": "A326",
  "slice": "component: expert",
  "status": "apply",
  "reason": "All four tools are discover-only, so the model cannot call them directly; the section never says how the operator arms the component, nor that it stays inert otherwise. One parenthetical gives both paths.",
  "evidence": "components/expert/main.nim:37 (\"This component is inert until expert_follow names a target session\") and :831-832/851/871/925 (onDemand on all four tools); the cli spelling matches the MANUAL's own examples (docs/MANUAL.md:2149)",
  "old_string": "sessions concurrently — armed explicitly with `expert_follow {session_id}`\n(approval-gated, off by default) — watches each followed session's",
  "new_string": "sessions concurrently — armed explicitly with `expert_follow {session_id}`\n(approval-gated, off by default, and on demand, so the operator arms it with\n`discover` + `invoke` or\n`./var/bin/cli call expert_follow '{\"session_id\": \"conv-…\"}'`; the component is\ninert until then) — watches each followed session's"
 },
 {
  "id": "A327",
  "slice": "component: expert",
  "status": "already",
  "reason": "No expert-specific misstatement to fix: the Shipped components table's Manifest column reports the manifest's own flag (`optional` = `required: false`), and `expert` is autostarted exactly like every other optional service component (models, provider, edit, fabric, ...). `restart: on-failure` is the manifest-wide default for those entries, not an expert-specific fact, so a note here would have to be a table-wide convention.",
  "evidence": "manifest.yaml:233-239 (name: expert, autostart: true, required: false, restart: on-failure — the same shape as the `agent` entry just above and the `provider`/`models` entries); docs/MANUAL.md:74 reads `| `expert` | Nim | optional |`; the table already marks the genuinely-off components (hooks \"off by default\", dialog \"not autostarted\")",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A328",
  "slice": "component: expert",
  "status": "already",
  "reason": "The MANUAL-side gap this row names is closed: the tool table carries the `session_id` semantics for both optional-arg tools. What is genuinely thin is the components' own `- param:` doc comments, which live in code and reach the LLM through the schema — a code change, outside a MANUAL batch.",
  "evidence": "docs/MANUAL.md:2626 and :2628 (the expert_unfollow / expert_status rows state both branches explicitly); components/expert/main.nim:835-841 (expert_unfollow), :854-862 (expert_reload), :874-880 (expert_status) — doc comments without `- param:` lines",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A329",
  "slice": "component: expert",
  "status": "apply",
  "reason": "The runner publishes `reason` on the advice event (the judge's one-line justification) and the subject block omits it. Adding the field is the whole fix.",
  "evidence": "core/conversation.nim:1104-1106 (drainAdvisories emits {sessionId, turnId, source, content, reason}); components/expert/main.nim:475-486 (the advise payload that gets folded)",
  "old_string": "ev.session.advice      {sessionId, turnId?, source, content} an advisory was folded in",
  "new_string": "ev.session.advice      {sessionId, turnId?, source, content, reason} an advisory was\n                       folded in (`reason` is the judge's one-line justification)"
 },
 {
  "id": "A331",
  "slice": "component: expert",
  "status": "skip",
  "reason": "`bench/config.json` `expertJudge` is benchmark configuration, not user configuration: it belongs to the judge-tuning A/B (bench/README.md) and the MANUAL deliberately documents runtime knobs only. Documenting it would put benchmark internals in the reference manual.",
  "evidence": "bench/config.json:170 (expertJudge), bench/README.md:231; the MANUAL documents no bench knob at all and the component reads no environment variable (components/expert/main.nim)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A332",
  "slice": "component: expert",
  "status": "skip",
  "reason": "Belongs in docs/WIRE.md, not the MANUAL: the MANUAL already covers the advice fold and the event; the payload shape (`kind: \"advisor\"`, `source`, `knowledgeVersion`) is a wire detail and WIRE.md's advise entry is where the rest of the advise contract lives. Editing WIRE.md is outside this MANUAL-only batch.",
  "evidence": "docs/WIRE.md:87 (only the advise subject is listed there); components/expert/main.nim:475-486 (sendAdvise payload with kind/source/knowledgeVersion); docs/MANUAL.md:2611-2615 already describes the fold",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A465",
  "slice": "component: provider",
  "status": "apply",
  "reason": "Both credential-writing rows omit `stripPrefix`, which the components' own schemas expose (provider_add :340, provider_update :451) and which the UI toggles. Adding the arg to both signatures plus one clause saying what it does is the fix; the env-var row for NIF_LLM_PROVIDERS already explains the semantics for the table form.",
  "evidence": "components/provider/main.go:74 (StripPrefix field), :340 and :451 (schema properties of provider_add/provider_update), :390 and :523-525 (both handlers apply it); components/llm/main.go:492-496 strips the vendor/ prefix when sending; ui/frontend/src/App.svelte:199-205 toggles it through provider_update",
  "old_string": "| `provider_add {nickname, apiKey, protocol?, baseUrl?, model?, catalog?, context?, plugin?, active?}` | add or overwrite an API-key provider (upsert by nickname; `protocol`: `openai-chat` default or `anthropic`); the first provider — API-key or OAuth — becomes active automatically unless `active: false`; response is redacted |\n| `provider_update {nickname, apiKey?, protocol?, baseUrl?, model?, catalog?, context?, plugin?}` | hidden client API for partial updates; omitted API key is preserved |",
  "new_string": "| `provider_add {nickname, apiKey, protocol?, baseUrl?, model?, catalog?, context?, plugin?, stripPrefix?, active?}` | add or overwrite an API-key provider (upsert by nickname; `protocol`: `openai-chat` default or `anthropic`); the first provider — API-key or OAuth — becomes active automatically unless `active: false`; `stripPrefix` sends a namespaced model id without its `vendor/` prefix for gateways that route on the canonical id (e.g. `glm-5.2` for `alibaba/glm-5.2`); response is redacted |\n| `provider_update {nickname, apiKey?, protocol?, baseUrl?, model?, catalog?, context?, plugin?, stripPrefix?}` | hidden client API for partial updates; omitted API key is preserved |"
 },
 {
  "id": "A468",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The switch row and the resolution bullet describe the backend move but not what happens to a conversation's model pin — the highest-impact consequence of a switch, and a client-side contract (the UI and TUI both clear the pin; a bare bus call does not). One new bullet states it.",
  "evidence": "ui/frontend/src/App.svelte:169-192 (switchProvider drops the conversation's modelOverride when the switch moves the backend); var/plugins/niffler-tui@main/tui/controls.go:917 (providerActionChangedProvider) with its main_test.go:1069-1103 assertions (a switch clears the pin, re-selecting the already active provider does not); components/llm/main.go:374-382 re-reads the provider per chat call and nothing server-side rewrites the conversation header",
  "old_string": "- The `active` marker is a plain store doc (`{nickname, updatedAt}`) — written\n  with `expectRev` 0 — and a dangling or empty marker is deleted automatically\n  on the next read, so `provider_remove`/`provider_use_environment` need no\n  manual repair; `store` tools remain there for manual surgery if you want it.",
  "new_string": "- The `active` marker is a plain store doc (`{nickname, updatedAt}`) — written\n  with `expectRev` 0 — and a dangling or empty marker is deleted automatically\n  on the next read, so `provider_remove`/`provider_use_environment` need no\n  manual repair; `store` tools remain there for manual surgery if you want it.\n- A switch changes the backend, not the conversation: a conversation's pinned\n  `modelOverride` still belongs to the provider it was chosen under and may not\n  exist on the new one. Interactive clients therefore clear the pin when a\n  switch actually moves the backend (the web UI and the TUI do); a bare\n  `provider_switch` call leaves it in place, so a stale pin can fail the next\n  turn until the conversation's model is reset (`/model default` in the UI)."
 },
 {
  "id": "A469",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The MANUAL has no `/provider` documentation at all, though it documents `/model`, `/approvals`, `/limit` and `/compact`. The three moves map exactly onto the tools described in this section, so one sentence after the environment-fallback paragraph is the whole fix.",
  "evidence": "ui/frontend/src/lib/slash.ts:77-93 (command `provider`, subcommands environment/env and strip, alias `providers`, nickname sourced from provider.provider_list); ui/frontend/src/lib/slashDispatch.ts:123-136 (`/provider <nickname>` → provider_switch, provider-env → provider_use_environment, `provider strip [off]` → provider_update {stripPrefix}); ui/frontend/src/App.svelte:169-205 (the same three calls, plus the manager wrapping provider_add/update/remove and the OAuth flow)",
  "old_string": "`provider_use_environment` clears the active marker, it does not delete stored\nproviders.",
  "new_string": "`provider_use_environment` clears the active marker, it does not delete stored\nproviders.\n\nInteractive clients expose the same moves as slash commands: `/provider\n<nickname>` (alias `/providers`) switches the global backend, `/provider\nenvironment` (alias `/provider env`) comes back here, and `/provider strip\n[off]` toggles vendor-prefix stripping on the active provider; the UI's provider\nmanager wraps the same tools (`provider_add`/`provider_update`/`provider_remove`\nand the OAuth start/complete/cancel flow) behind the approval prompt."
 },
 {
  "id": "A470",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The row documents the behavior but not that the two argument shapes are alternatives, nor which one wins. The rest of the row (disk cache, stale-on-failure, errors returned) is already accurate. The tool's own schema description is separately wrong — it names two nonexistent modes, `providerListModel`/`providerExplicitModel` — and that is a code fix, not prose (recorded here, no code edited).",
  "evidence": "components/provider/main.go:747-755 (the tool schema — description at :749 names providerListModel/providerExplicitModel, the real properties nickname/baseUrl/apiKey/refresh at :751-753) against :765-799 (handler: nickname branch first, then baseUrl, else \"nickname or baseUrl required\" at :799); components/provider/models.go:80-90 (a probe failure serves the stale cache, else returns the error for the caller to fall back on)",
  "old_string": "| `provider_models {nickname?\\|baseUrl?, apiKey?, refresh?}` | model ids the provider's `/models` endpoint currently serves — a stored provider by nickname, or an explicit endpoint+key (the connect form, before the credential is saved). Disk-cached 5 min per endpoint (stale cache served when the probe fails); errors are returned to the caller so clients can fall back to the catalog |",
  "new_string": "| `provider_models {nickname?\\|baseUrl?, apiKey?, refresh?}` | model ids the provider's `/models` endpoint currently serves — a stored provider by nickname, or an explicit endpoint+key (the connect form, before the credential is saved); the two shapes are alternatives (`nickname` wins if both are given) and one of them is required. Disk-cached 5 min per endpoint (stale cache served when the probe fails); errors are returned to the caller so clients can fall back to the catalog |"
 },
 {
  "id": "A471",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The bullet reads as if `plugin` were wired up. It is stored and summarized metadata only: the component never resolves, validates or spawns the named component, and the switch event carries no plugin name — the plugin must find the switch itself. One clause makes that explicit without touching the rest of the bullet.",
  "evidence": "components/provider/main.go:69 (the field), :87 and :110 (only summarized), :339 and :450 (schema properties), :354 and :464 (handler args), :389 and :520-521 (stored), :981-1000 (the emitted events carry no plugin field)",
  "old_string": "  lookup, and `plugin` may name a component that hooks provider-specific\n  tools. On every switch the component publishes",
  "new_string": "  lookup, and `plugin` is informational metadata naming the component that owns\n  this provider's extra tools — `provider` neither starts nor validates it, so\n  the named component must be spawned separately and can only react on\n  `ev.provider.switch`. On every switch the component publishes"
 },
 {
  "id": "A475",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The Shipped policy hidden list names five provider tools but not the three OAuth ones, which are `x-harness.hidden` in the same way. Adding them keeps the list complete (the section's own provider table already marks them hidden).",
  "evidence": "components/provider/oauth.go:145, :168, :185 (x-harness {hidden: true} on provider_oauth_start/complete/cancel); docs/MANUAL.md:1023-1024 already calls them hidden, but the Shipped policy list at :1923-1928 omits them",
  "old_string": "`del`, LLM `chat`/`llm_resolve`, the systemprompt prompt, and the\ncredential-bearing provider tools (`provider_update`,\n`provider_use_environment`, `provider_status`, `provider_active`,\n`provider_get`). Store `put` is on demand (it carries",
  "new_string": "`del`, LLM `chat`/`llm_resolve`, the systemprompt prompt, and the\ncredential-bearing provider tools (`provider_update`,\n`provider_use_environment`, `provider_status`, `provider_active`,\n`provider_get`, `provider_oauth_start`, `provider_oauth_complete`,\n`provider_oauth_cancel`). Store `put` is on demand (it carries"
 },
 {
  "id": "A476",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The troubleshooting row sends every 401/403 to `NIF_OPENAI_API_KEY`, but with a stored provider active that key is not even used — its own key or refreshed OAuth token decides. The fix keeps the env fallback for the no-provider case.",
  "evidence": "components/provider/main.go:937-970 (effectiveProvider resolves the stored active provider first, the environment only as fallback) and components/provider/oauth.go:716-742 (ensureFresh refreshes a stored token before use); docs/MANUAL.md:367 already distinguishes a missing key from a rejected one",
  "old_string": "| llm error HTTP 401/403 | `NIF_OPENAI_API_KEY` missing or wrong — check `.env` and shell env |",
  "new_string": "| llm error HTTP 401/403 | check the active provider's key or token first (`provider_status` for a redacted view of what is in effect, `provider_list` for `expiresAt`); only with no stored provider active does `NIF_OPENAI_API_KEY` in `.env` or the shell environment decide |"
 },
 {
  "id": "A477",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The bullet describes the change event but not its `op` values, and a client that wants to react to a login or a token refresh has no way to know they are among them. Listing the ops is a parenthetical; the payload's secret-free nature is already stated.",
  "evidence": "components/provider/main.go:981-985 emits ev.provider.changed with only op/nickname/active/source/at; the ops: :430 (add/update), :532 (update), :578 (remove), :900 (import), :997 and :1015 (switch), components/provider/oauth.go:690 (login), :741 (refresh); the web UI reacts to the ev.provider. prefix (ui/frontend/src/App.svelte:552)",
  "old_string": "  enable or hide their tools. Every registry mutation also publishes the\n  secret-free `ev.provider.changed {op, nickname, active, source, at}` for\n  interactive clients to invalidate their provider/model views.",
  "new_string": "  enable or hide their tools. Every registry mutation also publishes the\n  secret-free `ev.provider.changed {op, nickname, active, source, at}` — `op` is\n  one of `add`, `update`, `switch`, `remove`, `import`, `login` or `refresh` —\n  for interactive clients to invalidate their provider/model views."
 },
 {
  "id": "A479",
  "slice": "component: provider",
  "status": "apply",
  "reason": "The protocols section lists the three wire protocols without saying that the API-key path can only use two of them. `provider_add` refuses `openai-codex` with an API key, so a reader who tries it gets an error the section never predicts. One sentence after the protocol list fixes it. (The related asymmetry — provider_update's schema still offers `openai-codex` and skips the stored-provider revalidation — is a code bug, flagged here, not edited.)",
  "evidence": "components/provider/main.go:375 (provider_add: \"protocol must be openai-chat or anthropic for API-key providers\"), :197-206 (validStoredProvider: authAPIKey && protocol != codex); the provider_add schema enum at :334 already offers only those two, while provider_update's enum at :445 still offers openai-codex and its handler (:528-540) never re-runs validStoredProvider",
  "old_string": "  blocks (consecutive tool results merge into one user message).",
  "new_string": "  blocks (consecutive tool results merge into one user message).\n\nAn API-key provider may use `openai-chat` or `anthropic` only: `openai-codex`\nrequires a ChatGPT subscription login (`provider_oauth_start`), and\n`provider_add` refuses the combination."
 },
 {
  "id": "A480",
  "slice": "component: provider",
  "status": "apply",
  "reason": "Nothing in the section says what an omitted field becomes, though the defaults decide whether a half-specified provider works at all. One paragraph after the exposure-flag paragraph states them.",
  "evidence": "components/provider/main.go:118-137 (environmentProvider defaults) and :138-180 (withDefaults: base URL by protocol/nickname, model by protocol, defaultModel = \"deepseek-chat\" at :39, catalog openai/anthropic for the two OAuth protocols)",
  "old_string": "`provider_models` runs under a 20 s timeout.",
  "new_string": "`provider_models` runs under a 20 s timeout.\n\nOmitted fields take protocol defaults: `openai-codex` infers the ChatGPT backend\nURL and `anthropic` the Anthropic one (under `openai-chat` a `deepseek` or\n`openai` nickname implies its own base URL), the model defaults to\n`deepseek-chat` (`openai-chat`), `gpt-5.4` (`openai-codex`) or\n`claude-sonnet-4-6` (`anthropic`), and the two OAuth protocols infer their\nmodels.dev catalog id (`openai`/`anthropic`)."
 }
]