[
 {
  "id": "A574",
  "slice": "The store",
  "status": "already",
  "reason": "verified — the MANUAL is exactly right today. The sentence is the other half of the write fence two sentences above it: a call carrying a live `__session` may only write `fabricprog` (everything else is `forbidden-kind`), while a call with an empty session — the direct bus callers named here — keeps full access on all three engines.",
  "evidence": "components/store/main.nim:100-106 (the fence, empty session bypasses it); components/cli/main.nim:112-114 (direct svc.store.call); components/store-sqlite/main.go:281-292",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A579",
  "slice": "component: console",
  "status": "apply",
  "reason": "the promise 'renders the wire traffic readably' overstates what the stream shows, and the two gaps are exactly what a reader needs while following a live install: a bare `reg.publish`/`reg.depart` payload (the registration object itself, not an envelope) prints as an empty-bodied `event <subject>` line, and every SDK reply prints with an empty tool name. Both facts are added where the promise is made; the same replacement covers A580, whose quote is that identical sentence.",
  "evidence": "components/console/main.nim:39-41 (call: subject + tool + args), :42-44 (result: env.tool, empty for every SDK reply — sdk/niffler/sdk.nim:725 + sdk/envelope.nim:72-73 send no tool), :48-56 (event renders env.payload only; a registration payload has no payload key, sdk/niffler/sdk.nim:530-539); `observe_subjects` exists (MANUAL:2412)",
  "old_string": "everything and renders the wire traffic readably: calls with tool + args,\nresults, errors, events, approvals — it is how you follow a live install\nor a stuck-tool call:",
  "new_string": "everything and renders the wire traffic readably: calls with subject + tool +\nargs, results, errors, events, approvals. Two limits are worth knowing: a\nbare `reg.publish`/`reg.depart` payload prints as an empty-bodied\n`event <subject>` line (use `observe_subjects` or `catalog` to see which\ncomponent came up), and every SDK result prints with an empty tool name —\ncorrelate it with the call line above. It is how you follow a live install\nor a stuck-tool call:"
 },
 {
  "id": "A580",
  "slice": "component: console",
  "status": "skip",
  "reason": "folded into A579: it quotes the very same sentence (subscribes to everything … renders the wire traffic readably) and asks for one of the two caveats A579 adds — that a registration/departure payload shows as a bare `event <subject>` with an empty body. A second edit would need the identical anchor.",
  "evidence": "components/console/main.nim:48-56 (env.payload only), :72-74 (reg.publish carries the fields at top level), sdk/niffler/sdk.nim:530-539",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A582",
  "slice": "component: console",
  "status": "code",
  "reason": "the renderer drops non-envelope messages on the floor: `render` prints `env.payload` for every `ekEvent`, and a `reg.publish`/`reg.depart` message *is* the registration object ({name, version, pid, tools}), so the most frequent boot traffic renders as a blank body. Code fix: when the decoded message is not an envelope, print the decoded top-level object (e.g. `event reg.publish  {name: \"bash\", pid: 1234}`) instead of an empty body, then the A579 caveat can go away again. Nothing else in the MANUAL needs changing.",
  "evidence": "components/console/main.nim:35-56 (render prints env.payload only), :72-74 (the raw registration payload), sdk/niffler/sdk.nim:530-535",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A583",
  "slice": "component: console",
  "status": "code",
  "reason": "results are unattributable on a busy bus: the renderer prints `env.tool`, and every SDK reply is built by `resultEnvelope(id, value)`, which carries no tool — so `result   → {…}` has neither a tool name nor the envelope id to correlate with the call above it. Code fix: echo the envelope id, or keep an id→tool map of the calls already rendered, so a result line can be attributed. This is a component change, not MANUAL wording (A579 documents the limit until it is fixed).",
  "evidence": "components/console/main.nim:42-44 (prints env.tool); sdk/niffler/sdk.nim:725 and sdk/envelope.nim:72-73 (replies carry no tool); the envelope id is present in the call and the reply",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A584",
  "slice": "component: console",
  "status": "apply",
  "reason": "the reconnect story is undocumented and user-visible: after the bus dies console goes quiet for the nats client's whole reconnect budget (the in-code comment says ~2 min, the audit measured ~3.7 min of silence before the loss line), then prints the loss line once and retries every 2 s while re-reading `var/nats-url` — which is why a harness that restarts on a new random port is picked up by itself. One sentence after the intro paragraph, before the example command.",
  "evidence": "components/console/main.nim:64-68 (discovery file re-read per attempt), :93-99 (loss line, then return), :101-107 (2 s retry loop); budget = natsnim conn.nim maxReconnects 60 × reconnectWait 2000 ms + dial timeouts",
  "old_string": "or a stuck-tool call:",
  "new_string": "or a stuck-tool call:\n\nWhen the bus dies, console goes quiet for the nats client's reconnect budget\n(tens of seconds to ~4 minutes), then prints its `console: bus connection\nlost` line and retries every 2 s, re-reading `var/nats-url` each time — a\nharness that restarts on a new random port is picked up automatically."
 },
 {
  "id": "A588",
  "slice": "component: console",
  "status": "apply",
  "reason": "the observe section claims a raw-node contract that its sibling does not share, and for anyone debugging a boot that contrast is the decision: `observe` keeps the bare registration payload, `console` prints an empty-bodied line for it. One clause in the sentence that makes the claim, with the cross-link to the section that qualifies it.",
  "evidence": "components/observe/main.nim:3, :278-281 (raw `>` tap keeps reg.publish/reg.depart objects intact) vs components/console/main.nim:48-56 (payload-only render); MANUAL:2402 (the sentence)",
  "old_string": "including unknown envelope fields and bare registration payloads.",
  "new_string": "including unknown envelope fields and bare registration payloads — unlike\nits sibling `console`, which renders envelopes only: a bare `reg.publish`\nprints there as an empty-bodied `event reg.publish` line (see\n[The bus in one screen](#the-bus-in-one-screen))."
 },
 {
  "id": "A622",
  "slice": "The store",
  "status": "apply",
  "reason": "the `compaction_input` row describes only the happy path. The record is transient by contract — the snapshot is deleted the moment the attempt settles, and pages orphaned by a crashed or timed-out attempt are swept after 600 s — which is the rule that makes `context_projection` the source of truth. One clause in that cell, not a new paragraph.",
  "evidence": "core/compaction.nim:26 (snapshotSweepSecs = 600.0); core/conversation.nim:1405-1418 (cleanupSnapshot), :2683",
  "old_string": "| `compaction_input` | `<convId>:<attemptId>` | the paged pre-compaction snapshot the compaction component verifies and the runner commits from (pages are `<id>:p<idx>`) |",
  "new_string": "| `compaction_input` | `<convId>:<attemptId>` | the paged pre-compaction snapshot the compaction component verifies and the runner commits from (pages are `<id>:p<idx>`). Transient: deleted as soon as the attempt settles, and pages orphaned by a crashed or timed-out attempt are swept after 600 s — nothing may treat it as durable (only `context_projection` is) |"
 },
 {
  "id": "A623",
  "slice": "The store",
  "status": "apply",
  "reason": "the row names three headline fields and hides the record. The detail is load-bearing — a single `expectRev` write on the previous generation, the durable `covered` range and `retained` ids the restart replays, the renderer id that makes a checkpoint reloadable by another compaction implementation — and this one replacement also discharges A643 (the recall resolver's `checkpoint` ref), which quotes the same cell.",
  "evidence": "core/compaction.nim:338-357 (buildProjectionRecord: version, generation, canonicalHigh, renderer, checkpoint, covered, retained, prunes, measurements, provenance); core/conversation.nim:1684-1693 (single expectRev put), :2677-2748 (reload); components/recall/main.nim:195-217 (a `checkpoint` ref reads this record's checkpoint+generation and refuses a superseded one)",
  "old_string": "| `context_projection` | `<convId>` | the committed context projection (cut, checkpoint, generation) a runner reuses after compaction |",
  "new_string": "| `context_projection` | `<convId>` | one document per conversation — the committed context projection (`version`, `generation`, `canonicalHigh`, `renderer`, the normalized `checkpoint`, the durable `covered` canonical range, the `retained` ids, `prunes`, `measurements`, `provenance`) a runner reuses after compaction. Written once with `expectRev` on the previous generation; it is the source of truth a restart rebuilds the provider view from, and the recall resolver reads its `checkpoint`/`generation` when a notice names a `checkpoint` ref |"
 },
 {
  "id": "A642",
  "slice": "The store",
  "status": "apply",
  "reason": "the `spill` row says what a spill is and nothing about how fragile the reference is: promotion is best-effort at append time (a failed promotion adds no ref line, because a missing ref never lies), a missing/empty/malformed spill document is refused loudly rather than answered as an empty success, and the prune gate re-verifies the document before pruning — so a broken spill can never cost the last copy. Added to the cell it belongs to.",
  "evidence": "core/conversation.nim:1296-1320 (promoteSpill, best-effort, warning on failure), :720-731 (prune gate re-verifies, keeps the original un-pruned); components/recall/main.nim:162-185 (missing and empty spills refused)",
  "old_string": "| `spill` | `<convId>:<n>` | an oversized tool result promoted out of the context window, addressable with `context_recall {\"ref\": {\"source\": \"spill\", \"id\": \"…\"}}` |",
  "new_string": "| `spill` | `<convId>:<n>` | an oversized tool result promoted out of the context window, addressable with `context_recall {\"ref\": {\"source\": \"spill\", \"id\": \"…\"}}`. Promotion is best-effort at append time (a failure keeps the temp-file pointer and adds no ref); a missing, empty or malformed spill document is refused loudly instead of answered as an empty success, and the prune gate re-verifies the document before pruning, so a broken spill can never cost the last copy |"
 },
 {
  "id": "A643",
  "slice": "The store",
  "status": "skip",
  "reason": "folded into A623 in this batch: both rows quote the same `context_projection` cell, so a second edit would need the identical anchor. A623's replacement states the fact A643 asks for — the recall resolver reads this record's `checkpoint` and `generation` when a notice names a `checkpoint` ref.",
  "evidence": "components/recall/main.nim:195-217 (a `checkpoint` ref resolves against the context_projection record's checkpoint+generation, and a superseded generation is refused); see A623 in this file",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A644",
  "slice": "System prompt (systemprompt)",
  "status": "apply",
  "reason": "the item-1 line points at baseprompt.txt but never says what the product prompt *does* for the rest of the system: it is the only place the model is taught the replaced-content recourse (pass the ref quoted in the notice verbatim), so a prune, spill or compaction notice is followable purely because that sentence exists. One appended sentence in the item, not a new subsection.",
  "evidence": "components/systemprompt/baseprompt.txt:43-48 ('the original is retrievable with context_recall — pass it the ref quoted in the notice verbatim'); the notices it refers to: core/conversation.nim:1314-1316 (spill), :739-741 (prune), the compaction checkpoint",
  "old_string": "there is no runtime file dependency.",
  "new_string": "there is no runtime file dependency.\n   The product prompt is also where the model is taught the replaced-content\n   recourse: pruned tool results, spilled command output and compaction\n   checkpoints are retrievable with `context_recall` by passing the ref quoted\n   in the notice verbatim — weaken that sentence and every trim, spill and\n   compaction notice stops being followed."
 },
 {
  "id": "A651",
  "slice": "Contents",
  "status": "apply",
  "reason": "the contents list gains the new chapter the grep audit asked for (A649, batch-open2-1, inserts `## Search (grep)` before `## Language servers (lsp)`); the bullet belongs right after `Fetch`, which the same report names as the model. If A649 is skipped this link dangles — the two rows are one finding and should land together.",
  "evidence": "docs/MANUAL.md:20 (the contents line); components/grep.md §4 (proposed `## Search (grep)` chapter before the lsp chapter); batch-open2-1.txt (A649)",
  "old_string": " · [Fetch](#fetch)",
  "new_string": " · [Fetch](#fetch) · [Search (`grep`)](#search-grep)"
 },
 {
  "id": "A730",
  "slice": "The store",
  "status": "skip",
  "reason": "duplicate row. This is the §Migrating-between-engines finding 'It reads **every document** from the source engine over the bus contract (so **any engine pair works, including TiDB**)' — owned, with the corrected wording, by A744 (a fixed 18-name probe list, so kinds outside it are silently dropped) and A745 (the source is always barrel), both in batch-open2-1. Repeating the fix here would need the same anchor twice.",
  "evidence": "tools/store_migrate.nim:260-268 (kindProbes), :279-300; batch-open2-1.txt (A744, A745)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A731",
  "slice": "The store",
  "status": "skip",
  "reason": "duplicate row of 'Migration refuses to overlay an existing target database', owned by A746 (batch-open2-1), which carries the corrected wording: the reachable refusal is the *source* check ('both store.db and barrel-db exist … ambiguous source'), and the `--to tidb` overlay check tests a marker file nothing ever creates.",
  "evidence": "tools/store_migrate.nim:290-299, :78-84; batch-open2-1.txt (A746)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A732",
  "slice": "The store",
  "status": "skip",
  "reason": "duplicate row of 'The same export/replay path moves data in **either direction**', owned by A747 (batch-open2-1: remove the sentence — measured, the source engine is always barrel, so sqlite→barrel dies 'root already uses sqlite').",
  "evidence": "tools/store_migrate.nim:283-294; batch-open2-1.txt (A747)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A751",
  "slice": "The store",
  "status": "code",
  "reason": "no prose fix exists for this one: `--force` is dead code. It is declared (:40), promised in the file header (:11) and in a comment (:289), parsed at :444-445 and read nowhere; the usage text does not list it. Code fix: honour it (overlay the target / tolerate a live store) or delete the field, the header sentence and the comment. The MANUAL should stay silent about `--force`, which is what A749 (batch-open2-1) asks for.",
  "evidence": "tools/store_migrate.nim:11, :40, :289, :444-445 (only the assignment; no reader)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A752",
  "slice": "The store",
  "status": "code",
  "reason": "migration silently loses whole kinds: `kindProbes()` is a fixed 18-name list that contains neither `spill` nor `contextreceipt`, and the per-kind verification only re-counts the kinds it discovered — a barrel root seeded with 5 kinds migrated as 'total: 3 documents read … verified: every kind matches the source count … done.' with exit 0. Code fix: extend the probe list and add a tripwire that fails the run when the discovered kind set disagrees with the probe list. The prose half is A744 (batch-open2-1), which corrects 'every document'.",
  "evidence": "tools/store_migrate.nim:260-268 (probe list), :270-283 (discoverKinds), :364-386 (verification re-counts discovered kinds only); live kinds: spill core/conversation.nim:1305, contextreceipt core/conversation.nim:304",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A758",
  "slice": "The store",
  "status": "apply",
  "reason": "the MANUAL documents root, engine and DSN but never says the engines carry no other knobs — the fact a reader hunting for a path, a pragma or a timeout setting needs. One paragraph closing the engine inventory; the only variables either Go engine reads are its root and (tidb) its DSN.",
  "evidence": "components/store/main.nim (only the SDK's NIF_ROOT/NIF_NATS_URL); components/store-sqlite/main.go:127; components/store-tidb/main.go:99-135",
  "old_string": "The file-backed engines (`sqlite`, `barrel`) enforce single-writer the same\nway:",
  "new_string": "Beyond root and engine selection the engines take no configuration: file\npaths, lock paths, pragmas, timeouts and pool sizes are code-resident\n(`NIF_ROOT` decides the root, `NIF_STORE_BACKEND` the engine,\n`NIF_STORE_TIDB_DSN` the cluster).\n\nThe file-backed engines (`sqlite`, `barrel`) enforce single-writer the same\nway:"
 },
 {
  "id": "A760",
  "slice": "The store",
  "status": "apply",
  "reason": "§The store contradicts §Shipped policy: `put` is on demand too, and it is the tool that carries `x-harness.sessionId` — the reason the write fence two sentences below can exist at all. Fix that one sentence instead of rewriting the paragraph.",
  "evidence": "components/store/main.nim:89 (`put`: onDemand + sessionId), :117/:129 (get/list onDemand), :171 (`del` hidden); the same flags in components/store-sqlite/main.go:264,358,404,498 and components/store-tidb/main.go:229,347,393,487; the MANUAL's own §Shipped policy already has it right (MANUAL:2066-2067)",
  "old_string": "`get` and `list` are on-demand tools; `del` is hidden — core deletes records,\nthe model cannot.",
  "new_string": "`put`, `get` and `list` are on-demand tools; `del` is hidden — core deletes\nrecords, the model cannot. `put` also carries `x-harness.sessionId`, which is\nwhat makes the write fence below possible."
 },
 {
  "id": "A761",
  "slice": "The store",
  "status": "apply",
  "reason": "one clause so the section stops implying the engines are tool-identical: the barrel engine also registers the hidden `selftest` — the SDK's own helper, the tool `/doctor` fans out to — while the two Go engines register the four tools only. A735 (batch-open2-1) states the same fact in the shipped-components row; different anchors, so neither edit depends on the other.",
  "evidence": "components/store/main.nim:181-232 (comp.selfTest roundtrip); sdk/niffler/sdk.nim:155-167 (hidden `selftest`, '/doctor fans out'); no selfTest helper in sdk/go (store-sqlite/main.go:106-117, store-tidb/main.go:79-90 register four tools); MANUAL:2949",
  "old_string": "(`put` accepts `expectRev` and fails with `rev-conflict` on mismatch).",
  "new_string": "(`put` accepts `expectRev` and fails with `rev-conflict` on mismatch). The\nbarrel engine also registers a hidden `selftest` tool — a real\nput/get/rev/list/`del` roundtrip that `/doctor` can call; the two SQL engines\nregister the four tools only."
 },
 {
  "id": "A762",
  "slice": "The store",
  "status": "apply",
  "reason": "the plaintext note is true for `provider` but reads as if the registry were the only stored secret: an `mcp` record keeps its `env`/`headers` values verbatim in the same file and only listings redact them. Extend the sentence in place — and say plainly that any copy of the database file is a copy of the credentials, since that is the whole protection.",
  "evidence": "components/provider/main.go:11,63,384 (apiKey plaintext); components/mcp/types.go:12,19-21 (Env/Headers stored verbatim); components/mcp/main.go:85-88 (only listings redact); MANUAL:1741 (`mcp_servers` redacts), :2194 (provider redaction)",
  "old_string": "LLM provider registry of the `provider` component. Credentials are stored in **plaintext** — the store file itself is the secret — and redaction happens in the tool responses only |",
  "new_string": "LLM provider registry of the `provider` component. Credentials are stored in **plaintext** — the store file itself is the secret — and redaction happens in the tool responses only (`provider_list`; `mcp_servers` likewise redacts the `mcp` records' `env`/`headers`). That covers both kinds' secrets, so any copy of `var/store.db` or `var/barrel-db` is a copy of them |"
 },
 {
  "id": "A763",
  "slice": "The store",
  "status": "apply",
  "reason": "the id is zero-padded to six digits, which is what makes key order message order — the reason the `after` cursor, resume and the compaction projection's covered range are ordered at all. Five words in the Id cell, no new row.",
  "evidence": "core/conversation.nim:266,271 (`convId & \":\" & align($seqNo, 6, '0')`); :1294-1296 (nextMsgKey uses the same padding for the spill ref)",
  "old_string": "| `message` | `<convId>:<seq>` | `{conversationId, role, content, ...}` |",
  "new_string": "| `message` | `<convId>:<seq>` (the sequence zero-padded to six digits — id order is message order) | `{conversationId, role, content, ...}` |"
 },
 {
  "id": "A764",
  "slice": "The store",
  "status": "already",
  "reason": "verified — the MANUAL is right today on both halves: the fence, the `forbidden-kind` code and the 'direct bus callers keep full access' asymmetry exist verbatim on all three engines, and no live session can write a harness-managed kind.",
  "evidence": "components/store/main.nim:100-106; components/store-sqlite/main.go:281-292; components/store-tidb/main.go:246-255; core/dispatch.nim:1650-1656",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A765",
  "slice": "The store",
  "status": "already",
  "reason": "verified — the backend paragraph is accurate for all three engines (sqlite the default at `var/store.db`, barrel behind `NIF_STORE_BACKEND=barrel`, tidb via the DSN with no flock); nothing to change.",
  "evidence": "core/niffler.nim:485-496 (engine → binary, default sqlite); components/store-sqlite/main.go:81,135; components/store/main.nim:58; components/store-tidb/main.go:71",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A766",
  "slice": "The store",
  "status": "already",
  "reason": "verified — `storeListAll` is really the whole-kind read path and it pages the exclusive `after` cursor to exhaustion behind a runaway guard; the MANUAL sentence is correct as written.",
  "evidence": "core/dispatch.nim:224-266 (paging loop + guard); call sites core/conversation.nim:399, core/dispatch.nim:451,695",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A767",
  "slice": "The store",
  "status": "code",
  "reason": "the barrel `put` handler does `db.set(docKey(kind, id), $value)` with no nil check; `$` on a nil JsonNode is a SIGSEGV on Nim 2.2.12 and the SDK pump catches CatchableError only, so `put {\"kind\":\"probe\",\"id\":\"novalue\"}` gets no reply and the process exits 139 (the supervisor hides it as a restart). Both Go engines already answer `put needs kind, id and value`. Code fix: guard `value == nil` in components/store/main.nim. No MANUAL wording is needed beyond A735's 'identical tools' qualifier.",
  "evidence": "components/store/main.nim:91-111 (no nil guard); components/store-sqlite/main.go:289-293 and components/store-tidb/main.go:254-258 (both guard, with a comment naming the barrel crash); sdk/niffler/sdk.nim:720-727 (pump catches CatchableError only)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A768",
  "slice": "The store",
  "status": "apply",
  "reason": "the MANUAL describes three engines but never says the store contract is one test run against each of them — including that the TiDB run SKIPs without a DSN. One closing paragraph of §Store engines, next to the engine inventory, rather than a new Testing paragraph that would split the fact from the engines it is about.",
  "evidence": "Makefile:491-503 (test-store, test-store-sqlite, test-store-tidb with the NIF_STORE_TIDB_DSN SKIP branch); tests/t_store.nim:15-22; tests/t_store_paging.nim:1-13",
  "old_string": "### Migrating between engines\n\n**Switching engines does not move data.**",
  "new_string": "The store contract is one test run against every engine: `make test-store`\n(the selected/default engine), `make test-store-sqlite`, `make test-store-tidb`\n(needs `NIF_STORE_TIDB_DSN`, otherwise it prints SKIP); `t_store_paging` pins\nthe `after`/`hasMore`/`nextAfter` cursor semantics that resume and migration\ndepend on.\n\n### Migrating between engines\n\n**Switching engines does not move data.**"
 },
 {
  "id": "A769",
  "slice": "The store",
  "status": "code",
  "reason": "tests/t_store_paging.nim tells the reader '`make test-store-paging` re-runs it against SQLite' — no such target exists (`grep -n store-paging Makefile` → 0 hits; the store targets are test-store, test-store-sqlite, test-store-tidb) and the test actually runs inside `make test-server` via the `tests/t_*.nim` wildcard. Code fix: correct the comment (or add the target). Nothing for the MANUAL.",
  "evidence": "tests/t_store_paging.nim:12; Makefile:441,447,491-503",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A773",
  "slice": "The store",
  "status": "skip",
  "reason": "summary row for the barrel `put` crash already decided as `code` under A767 in this batch (same finding, same file:line). It carries no CODE/FIX fields of its own and is not a MANUAL defect.",
  "evidence": "components/store/main.nim:110-111; see A767 in this file",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A774",
  "slice": "The store",
  "status": "skip",
  "reason": "summary row for the migration findings (one-directional, un-runnable on a migrated root, dead `--force`) that its own row-format counterparts own: A730–A732 in this batch fold to A744–A747, plus A751 for `--force`. It is prose from the report's priority list, not a separate MANUAL defect.",
  "evidence": "tools/store_migrate.nim:283-299, :444-445; the MANUAL sentences are MANUAL:337-339, :345-348 (owned by A744-A747)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A775",
  "slice": "The store",
  "status": "skip",
  "reason": "summary row for the plaintext-scope finding, folded into A762's edit of the same table cell (one replacement that names both kinds' secrets and the copy-of-the-file consequence).",
  "evidence": "components/mcp/types.go:19-21; see A762 in this file",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A776",
  "slice": "The store",
  "status": "skip",
  "reason": "umbrella row for three small drifts, each owned by its own row: `put` missing from §The store's on-demand sentence → A760 (this batch); the `var/` table row naming only `store.db` and neither lock file → A753 (batch-open2-8, State and configuration); NIF_STORE_BACKEND's unset-means-fallback (and the explicit-value demand) → A737 (batch-open2-1) and A755 (batch-open2-2). It has no anchor of its own.",
  "evidence": "MANUAL:365 (var/ row), :398 (NIF_STORE_BACKEND); batch-open2-8.txt (A753); batch-open2-1.txt (A737); batch-open2-2.txt (A755)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A788",
  "slice": "System prompt (systemprompt)",
  "status": "apply",
  "reason": "the claim is false: the product prompt is used verbatim — `staticRead(\"baseprompt.txt\")` into a `const`, assigned to `prompt` with no `%`/`replace`/`strformat` pass anywhere in the component (the harness root reaches the prompt only through the optional `<workspace>` tail, and a comment in the component explicitly forbids `$ROOT` in the frozen head, because it would break provider prompt-cache reuse). Fix the description too: the prompt covers change-scope discipline, tool-selection guidance and the docs pointer.",
  "evidence": "components/systemprompt/main.nim:36 (`staticRead`), :240 (`var prompt = basePrompt` — verbatim), :262-270 (root only via the `<workspace>` tail; the head must stay byte-stable), :234-243 (the comment naming $ROOT/cwd as forbidden in the head); baseprompt.txt:1-49",
  "old_string": "   (self-extension ladder, SDK examples, repo layout), `$ROOT`-substituted,\n   baked into the binary at compile time via `staticRead`.",
  "new_string": "   (change-scope discipline, tool-selection guidance, the docs pointer),\n   baked into the binary verbatim at compile time via `staticRead` — no\n   substitution and no template."
 },
 {
  "id": "A789",
  "slice": "System prompt (systemprompt)",
  "status": "apply",
  "reason": "two claims in this bullet are wrong and one is missing. The walk stops at the harness root, not at `/`: `stopAbove` is the root when the workspace is inside it (so the root is included and nothing above it is read), and a workspace *outside* the root walks its own directory only — which is what keeps a stray `AGENTS.md` in `$HOME` out of the prompt. Dedupe is by file identity (device:inode), not by path, so a symlink farm re-exposing the root cannot inject the same file twice.",
  "evidence": "components/systemprompt/main.nim:204 (`let stopAbove = if cwd == root or cwd.startsWith(root & \"/\"): root else: cwd`), :209-226 (the loop breaks at stopAbove), :74-84 and :217-219 (fileId = device:inode)",
  "old_string": "   - ancestor walk from the conversation's cwd up to `/`, harness root\n     first, deduplicated by path — nearer-to-cwd files appear later, so the\n     most specific instructions are the last thing the model reads;",
  "new_string": "   - ancestor walk from the conversation's cwd **up to the harness root\n     (inclusive)**, root first, deduplicated by file identity (device:inode —\n     a symlink farm cannot inject the same file twice); nearer-to-cwd files\n     appear later, so the most specific instructions are the last thing the\n     model reads. A workspace **outside** the harness root walks its own\n     directory only, so an unrelated `AGENTS.md` in `$HOME` never leaks into\n     the prompt;"
 },
 {
  "id": "A795",
  "slice": "System prompt (systemprompt)",
  "status": "already",
  "reason": "verified — both halves of the cap are exactly 200_000 bytes (the component truncates its own answer and core truncates whatever comes back), so 'truncated at 200 KB (both sides)' is right as written.",
  "evidence": "components/systemprompt/main.nim:29 (maxPromptLen = 200_000), :272-274; core/conversation.nim:96-98 (200_000 on the core side)",
  "old_string": "",
  "new_string": ""
 }
]
