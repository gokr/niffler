[
  {
    "id": "A008",
    "slice": "Layout of a running system",
    "status": "already",
    "reason": "Verified row (do not re-audit): the `grep` row still reads `optional (4 replicas)` and both facts hold in the current tree — the manifest replica count and the two tool flags are unchanged.",
    "evidence": "docs/MANUAL.md:76; manifest.yaml:122-124 (`replicas: 4`); components/grep/main.nim:52 (`parallel: true`, direct), :100 (`onDemand`)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A017",
    "slice": "Layout of a running system",
    "status": "skip",
    "reason": "Trim not applied: the three engine bullets are the MANUAL's only 'which engine, and why' reference for an operator and every claim in them is correct (the auditor's own fix line is 'keep, but see (Z)'); the Z-class preference would delete ~40 lines of operator-facing prose — including the tidb `No flock` contrast that A106/A015 just reconciled with the single-writer paragraph — with no replacement content to put in their place, while the same section already cites research/STORE_V2.md as the design record. That deletion is a scope call for the parent's trim pass, not a fact correction.",
    "evidence": "docs/MANUAL.md:228-279 (engine bullets + single-writer paragraph); docs/research/STORE_V2.md:112-170, 255-304 (the same rationale already recorded there: goose migrations, `modernc.org/sqlite`, barrel's two-key crash window, MEDIUMTEXT/utf8mb4_bin)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A018",
    "slice": "Layout of a running system",
    "status": "apply",
    "reason": "Trim applied with its replacement pointer: the four-invocation walkthrough becomes one sentence that names the real flag set (including `--to <engine>`, which the walkthrough omitted) and points at the tool's own `--help` plus research/STORE_V2.md. The six-line summary, the boot-refusal text above it and every fact after it stay; only the duplicated copy-paste block and the now-redundant second citation go.",
    "evidence": "docs/MANUAL.md:299-313 (paragraph + ```bash walkthrough); tools/store_migrate.nim:397-412 (the real flag set: `--root`, `--to`, `--dry-run`, `--scan`, `--all`); docs/research/STORE_V2.md:196-208, 274-283 ('Moving data between engines')",
    "old_string": "replays each into the fresh target, then verifies per-kind counts:\n\n```bash\nniffler-store-migrate --root ~/git/myharness      # migrate that root\nniffler-store-migrate --root ~/git/myharness --dry-run\nniffler-store-migrate --scan ~/git                # list un-migrated roots\nniffler-store-migrate --all ~/git                 # migrate all of them\n```\n\n`--scan` finds the top directory, sibling clones, and benchmark trees\n(`var/bench/**/niffler-root`). Migration refuses to overlay an existing\ntarget database; rollback is simply `NIF_STORE_BACKEND=barrel`, since the\nbarrel file is untouched. The same export/replay path moves data in either\ndirection (docs/research/STORE_V2.md \"Moving data between engines\").",
    "new_string": "replays each into the fresh target, then verifies per-kind counts. The flags\n(`--root`, `--to <engine>`, `--dry-run`, `--scan [<top>]`, `--all [<top>]`)\nare described by the tool's own `--help`; the design behind each stage is\n[research/STORE_V2.md](research/STORE_V2.md) \"Moving data between engines\".\n\n`--scan` finds the top directory, sibling clones, and benchmark trees\n(`var/bench/**/niffler-root`). Migration refuses to overlay an existing\ntarget database; rollback is simply `NIF_STORE_BACKEND=barrel`, since the\nbarrel file is untouched. The same export/replay path moves data in either\ndirection."
  },
  {
    "id": "A038",
    "slice": "Layout of a running system",
    "status": "already",
    "reason": "Verified row (do not re-audit): the replaceable compaction seam is documented as described — hidden + `runner` tool, `NIF_COMPACTION_*` bounds, runner-owned paged snapshot, the runner alone committing the projection.",
    "evidence": "docs/MANUAL.md:78 (`compaction` row), :405 (`NIF_COMPACTION_TOOL`), :770 ('the shipped `compaction_propose` is replaceable'); components/compaction/main.nim:216 (`hidden` + `runner: true`), no `storePut`/`storeDel` in the component (grep count 0)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A152",
    "slice": "Layout of a running system",
    "status": "skip",
    "reason": "Trim folded, not duplicated: its migrate-walkthrough half is exactly the text A018's edit trims into a pointer (same anchor — a second edit would collide), and its engine-bullet half stays per A017, which is the MANUAL's only engine-selection reference and is correct as written.",
    "evidence": "docs/MANUAL.md:228-313 (the two blocks A017/A018 cover); A018's edit above carries the only safely anchorable deletion",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A185",
    "slice": "Layout of a running system",
    "status": "already",
    "reason": "Verified row: the `var/bin/` row is accurate and unchanged — core, the session runner and the components are all built there by `make build`.",
    "evidence": "docs/MANUAL.md:43; Makefile:127-128 (`var/bin/session` from `core/session.nim`), :305 (`components-inner` builds it with the rest); core/conversation.nim:2881 (runner path)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A211",
    "slice": "Layout of a running system",
    "status": "already",
    "reason": "Fixed by batch-5b's A255 (applied): §Approvals now carries both halves the row asks for — core's own destructive tools are written with their bare names and the gating sentence says they are gated by name inside `handleCoreTool`, not through a schema, so `x-harness.approval` appears on component tools only.",
    "evidence": "docs/MANUAL.md:612-614 (\"Core's own destructive tools are named `spawn`, `kill`, `remove` and `conversation_delete`…\"); core/catalog.nim:68, 81, 89 (bare names); core/dispatch.nim:273-276 (gated by name before dispatch)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A257",
    "slice": "Layout of a running system",
    "status": "apply",
    "reason": "Applied as a pointer sentence rather than a longer block: the fenced list stays as it is, but it stops claiming to be the subject inventory and names the component-local families it omits, so a reader who sees `ev.fabric.*` or `ev.log.bash` on `nats sub '>'` knows why. Nothing is deleted and no subject is re-documented here (each family's chapter owns its detail).",
    "evidence": "docs/MANUAL.md:481-492 (the claim + the fenced block, which omits them); core/conversation.nim:2915 (`ev.session.` & kind — so `ev.session.map` exists too), components/lsp/main.nim:1286 (`ev.lsp.warm`), components/agent/main.nim:1012 (`ev.agent.done`), components/fabric/fabric.nim:292, 354, 770, 794 (`ev.fabric.*`), sdk/niffler/sdk.nim:528 and sdk/go/component.go:302 (`ev.log.<component>`); docs/WIRE.md:97-101 lists only a subset as well",
    "old_string": "Core speaks exactly one protocol: JSON envelopes over NATS (details in\n[WIRE.md](WIRE.md)). Subjects:",
    "new_string": "Core speaks exactly one protocol: JSON envelopes over NATS (details in\n[WIRE.md](WIRE.md)). Subjects — the backbone you meet while operating, not\nthe complete inventory (every component also publishes its own event\nfamilies: `ev.log.<component>` from the SDKs, `ev.lsp.warm`, `ev.agent.*`,\n`ev.fabric.*`):"
  },
  {
    "id": "A004",
    "slice": "Shipped components",
    "status": "already",
    "reason": "Already right in the current row: `warmup` is in the operation list and the two extra tools `lsp_servers`/`lsp_registry` are named, so the row no longer implies one tool. The shipped-components table was rebuilt in the parent's own pass over the Layout/Shipped-components slice (`partition.py` excludes `layout-of-a-running-system` from the child batches), so only the pre-audit quote lacked them. Its twin A144 asks the identical thing from the other report.",
    "evidence": "docs/MANUAL.md:71 ('… `hover`, `warmup` — plus `lsp_servers` (list the merged registry) and `lsp_registry` (`add`/`remove` an entry, approval-gated)'); components/lsp/main.nim:51 (`warmup` in the ops enum), :1300 `lsp`, :1319 `lsp_servers`, :1324 `lsp_registry`",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A006",
    "slice": "Shipped components",
    "status": "apply",
    "reason": "The table row lists the four store tools without flags, which reads as if the model may call `del`. Adds the flags in one clause: all four are on-demand and `del` is hidden (core-only). This is the row's fix; its duplicate A142 is folded here so the two anchors cannot collide.",
    "evidence": "docs/MANUAL.md:59 (`| \\`store\\` | … document store over the bus (\\`put/get/list/del\\`, rev-based concurrency)…`); components/store-sqlite/main.go:264 (put onDemand + sessionId), :358 (get), :404 (list), :496-498 (del hidden, 'Hidden from the LLM: deletes are made by core'); components/store/main.nim:117, 129 (onDemand), :171 (`hidden`)",
    "old_string": "`put/get/list/del`, rev-based concurrency). Engines register under the same name with identical tools",
    "new_string": "`put/get/list/del`, rev-based concurrency). All four tools are on-demand, and `del` is additionally hidden — core deletes records, the model cannot. Engines register under the same name with identical tools"
  },
  {
    "id": "A007",
    "slice": "Shipped components",
    "status": "apply",
    "reason": "The row names two of the component's nine tools; it now names all nine (the new names cost one line and surface `agent_ask`, which no chapter describes yet) while keeping the pointer to §Fabric and subagents, which is where the behaviours live. A146 is the same finding from the other report and is folded here.",
    "evidence": "docs/MANUAL.md:73 (`… subagent sessions: `agent_run`/`agent_spawn` (…)`); components/agent/main.nim:1058 agent_run, :1203 agent_spawn, :1308 agent_status, :1332 agent_wait, :1370 agent_stop, :1400 agent_steer, :1473 agent_ask, :1538 agent_notices, :1597 agent_list",
    "old_string": "| `agent` | Nim | optional | subagent sessions: `agent_run`/`agent_spawn` (fresh or continued children, background jobs, durable settlement notices — see [Fabric and subagents](#fabric-and-subagents)) |",
    "new_string": "| `agent` | Nim | optional | subagent sessions, nine tools: `agent_run`/`agent_spawn` (fresh or continued children, background jobs, durable settlement notices) plus `agent_status`/`agent_wait`/`agent_stop`/`agent_steer`/`agent_ask`/`agent_notices`/`agent_list` — see [Fabric and subagents](#fabric-and-subagents) |"
  },
  {
    "id": "A009",
    "slice": "Shipped components",
    "status": "already",
    "reason": "Verified row (do not re-audit): the repomap row's facts — opt-in append, the two gates, the mtime-keyed cache, the threshold defaults — all match the code.",
    "evidence": "docs/MANUAL.md:61; components/repomap/main.nim:34-37, 49-52 (defaults 50/800/25/5 behind the `NIF_REPOMAP_MIN_*` rows at MANUAL:437-440), :262 (`onDemand: true`, `effect: \"read\"`), :72-96 (per-file mtime-keyed cache under `var/repomap-tags/`)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A010",
    "slice": "Shipped components",
    "status": "already",
    "reason": "Verified row (do not re-audit): `dialog` still ships in `var/bin/dialog` and is still not autostarted — it has no manifest entry, only a Makefile build rule.",
    "evidence": "docs/MANUAL.md:86; Makefile:297-312 (`var/bin/dialog: components/dialog/dialog.sh`, built into `var/bin` with the components), manifest.yaml has no `dialog` entry (grep count 0)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A011",
    "slice": "Shipped components",
    "status": "already",
    "reason": "Verified row (do not re-audit): the bash row's result contract — `(exit N)` line followed by combined stdout/stderr, the `exit_code`/`cancelled`/`spill` machine fields, 124/130, whole-process-group kill — matches the component, and the `### bash in detail` subsection now carries the rest.",
    "evidence": "docs/MANUAL.md:60, :87-131 (`### bash in detail`, added by this audit's A276); components/bash/main.nim:164-188 (status line, exit_code/cancelled, spill), :139 (`exit_code: 130, cancelled: true`)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A142",
    "slice": "Shipped components (",
    "status": "skip",
    "reason": "Duplicate of A006 (same row, same fix, filed from the other report); the edit is carried once there so the two anchors cannot collide — a second application of the same row text would fail after the first.",
    "evidence": "docs/MANUAL.md:59; components/store-sqlite/main.go:264, 358, 404, 496-498; A006's edit above",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A143",
    "slice": "Shipped components (",
    "status": "already",
    "reason": "Already right in the current row: it names both tools with their flags — `build {lang, name, source, files?, defines?}` (approval-gated, on demand) and `info` (on demand). Updated in the parent's own pass over the Layout/Shipped-components slice (`partition.py`'s MINE set).",
    "evidence": "docs/MANUAL.md:63; components/builder/main.nim:49 (`approval: \"always\"`, `onDemand: true` for build), :203 (`onDemand` for info)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A144",
    "slice": "Shipped components (",
    "status": "already",
    "reason": "Same finding as A004 from the other report, and already right in the lsp row: `warmup` is listed and `lsp_servers`/`lsp_registry` (approval-gated) are named, so the row no longer implies a single `lsp` tool — the row was brought current by the parent's own Layout/Shipped-components pass.",
    "evidence": "docs/MANUAL.md:71; components/lsp/main.nim:51, 1300, 1319, 1324",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A145",
    "slice": "Shipped components (",
    "status": "already",
    "reason": "The row was a delta with no fix: the auditor's own check passed. Re-verified — six tools, all on-demand, none approval-gated, exactly as the row says.",
    "evidence": "docs/MANUAL.md:72; components/git/main.nim:185 git_status, :216 git_diff, :255 git_log, :293 git_show, :323 git_blame, :369-370 review_receipt — the first five carry `parallel: true, onDemand: true`, review_receipt `onDemand: true`, and none carries `approval`",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A146",
    "slice": "Shipped components (",
    "status": "skip",
    "reason": "Duplicate of A007 (same row, same nine-tool fix, filed from the other report); carried once there. Its other allowed shape — 'defer to §Fabric' — is already half-present in the row's pointer, which A007's edit keeps.",
    "evidence": "docs/MANUAL.md:73; components/agent/main.nim:1058-1597 (nine registrations); A007's edit above",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A485",
    "slice": "component: repomap",
    "status": "apply",
    "reason": "Resolved: the row gains the pointer the fix asks for, and the detail it wants stated (budget default 1024 / cap 4096, discover-only, read-effect, approval-free) is carried by the new `### `repomap` in detail` subsection that A486's edit inserts below this table — so the row stays a row instead of growing another paragraph.",
    "evidence": "docs/MANUAL.md:61 (row, ends '…nothing else changes |'); components/repomap/main.nim:34-35 (`MAP_DEFAULT_BUDGET = 1024`, `MAP_MAX_BUDGET = 4096`), :217-218 (clamp), :262 (`onDemand: true`, `effect: \"read\"`, no `approval` key); subsection anchor added by A486 at docs/MANUAL.md:130-132",
    "old_string": "Optional component — absent means no map, nothing else changes |",
    "new_string": "Optional component — absent means no map, nothing else changes. Parameters, budget default and cap, tag tiers and the append payload: [`repomap` in detail](#repomap-in-detail) |"
  },
  {
    "id": "A486",
    "slice": "component: repomap",
    "status": "apply",
    "reason": "Resolved by giving the component the detail section the fix asks for, in the MANUAL's own established shape (`### `bash` in detail` sits in the same place): it carries the language tiers this row wants, the discovered-only/read/approval-free facts and the budget defaults (A485), the append payload (A492), the `focus`/`mentionedIdents` semantics (A493) and the census exclusions/caps (A494) — one coherent addition instead of four anchors into the delivery-gated shipped-table row.",
    "evidence": "docs/MANUAL.md:61 (only repomap mention outside env rows; no chapter exists), :130-132 (insertion point, right after `### `bash` in detail`); components/repomap/tags.nim:11 (40 MB Nim parser), :228-233 (tier list: .go/.py/.ts/.js/.c/.h/.cpp…/.rs/.rb tree-sitter, .nim/.nims native), components/repomap/ts.nim:16-40 (vendored grammars), components/repomap/queries/ (eight `*-tags.scm`); components/repomap/main.nim:64-70 (skip dirs), :115-137 (census caps 5000 files / 5 s), :217-218, :262 (budget + flags), :286-296 (withheld log line), :232-234 (the `No map: …` answer); core/conversation.nim:1035-1056 (drainMap: one user-role message, bracketed preamble, once per conversation, append-only history, compaction may trim it), :2915 (`ev.session.map`, emitted from drainMap's `onEvent(\"map\", …)` with sessionId/workspace/bytes)",
    "old_string": "runs it exclusively, never inside the read concurrency cap.\n\n### Minimal boot profile (`--minimal`)",
    "new_string": "runs it exclusively, never inside the read concurrency cap.\n\n### `repomap` in detail\n\nThe `repomap` row above is the summary. The `repo_map` tool is discover-only\n— absent from a conversation's frozen direct set, reachable through\n`discover` + `invoke` — carries `x-harness.effect: read` and needs no\napproval, so the model asks and nothing is injected; `budget` is in tokens,\n1024 by default and 4096 at most.\n\n`focus` ranks the graph around the files the conversation is working on — and\nomits their own definitions, since it already has them — while\n`mentionedIdents` boosts files whose path or definitions match the symbols the\ntask names.\n\nTag coverage is two tiers: tree-sitter for Go, Python, TypeScript,\nJavaScript, C, C++, Rust and Ruby (grammars vendored under\n`components/repomap/csrc/`, per-language queries under\n`components/repomap/queries/`), and a native Nim tagger for `.nim`/`.nims`,\nbecause the Nim grammar's generated parser is 40 MB. An extension outside\nthose tiers contributes no symbols, so a map of an untaggable language is\nempty rather than wrong.\n\nThe census that feeds the graph walks the workspace once, skipping hidden\ndirectories, `docs/`, `var/`, `logs/`, `vendor/`, `node_modules/`, the build\noutputs (`dist/`, `build/`, `target/`, `nimcache/`, …) and the other junk\ndirectories, and stops after 5000 files or 5 s. A docs-only or otherwise tiny\nworkspace therefore maps to nothing — the append path logs\n`repo map withheld`, and the tool answers `No map: …`.\n\n**What the append injects.** One user-role message per conversation: a\nbracketed preamble naming the workspace and warning that it is a snapshot\ntaken when the conversation started, then the map. It goes into ordinary\nappend-only history — the runner folds it in before the conversation's next\nLLM request, never into the frozen prefix — so a later compaction may trim it\naway and `repo_map` re-creates it on demand. Observers see\n`ev.session.map {sessionId, workspace, bytes}`.\n\n### Minimal boot profile (`--minimal`)"
  },
  {
    "id": "A492",
    "slice": "component: repomap",
    "status": "skip",
    "reason": "Folded into A486's new `### `repomap` in detail` subsection, which documents the append payload ('What the append injects'): one user-role message with the bracketed snapshot preamble, append-only history folded in by the runner, once per conversation, re-creatable with `repo_map`, and the `ev.session.map` event. The row's own home (a proposed repomap chapter) was never adopted, so one anchor carries it.",
    "evidence": "docs/MANUAL.md:61 (no statement about the payload anywhere); core/conversation.nim:1035-1056 (`drainMap`), :2915 (`ev.session.` & kind → `ev.session.map`); A486's edit above",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A493",
    "slice": "component: repomap",
    "status": "skip",
    "reason": "Folded into A486's new subsection, which states both semantics: `focus` ranks around the files being worked on and omits their own definitions; `mentionedIdents` boosts files whose path or definitions match the named symbols. The only prior description was the tool schema's own text (main.nim:247-250), which is LLM-facing, not MANUAL prose.",
    "evidence": "docs/MANUAL.md:61 (schema parameter names only); components/repomap/score.nim:204 (`if rel in focusSet: continue` — focus files' definitions dropped), :111-118 (mention personalization: mentioned files and path components matching a mentioned ident), :150 (`if ident in mentionSet: mul *= 10.0`); components/repomap/main.nim:247-250 (schema wording); A486's edit above",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A494",
    "slice": "component: repomap",
    "status": "skip",
    "reason": "Folded into A486's new subsection, which lists the census exclusions (hidden dirs, `docs/`, `var/`, `logs/`, `vendor/`, `node_modules/`, build outputs and the other junk dirs) and the caps (5000 files or 5 s) plus the two observable consequences (the `repo map withheld` log line and the tool's `No map: …` answer).",
    "evidence": "docs/MANUAL.md:61 (only 'a workspace below the census floor is never built'); components/repomap/main.nim:64-70 (`skipDirs`), :115-121 (`WALK_MAX_FILES = 5000`, `WALK_BUDGET_SECS = 5.0`), :286-296 (withheld log), :232-234 (`No map: …`); A486's edit above",
    "old_string": "",
    "new_string": ""
  }
]
