[
 {
  "id": "A029",
  "slice": "The bus in one screen",
  "status": "skip",
  "reason": "resolved the other way: the omission A029 complains about is fixed by A248 (which also adds llm.cancel.<sessionId>), and the block already opens by linking docs/WIRE.md for the normative tables. Trimming it to six lines would delete the per-subject detail the rest of the MANUAL relies on (svc.session.<id>.map, ev.session.notice, the cancel.<component> row that A270 asks about) — and A153 already recorded that such a trim cannot be anchored on a short unique old_string.",
  "evidence": "docs/MANUAL.md:481-482 (the WIRE.md link), 494-523; batch-2.summary.md (A153)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A030",
  "slice": "The bus in one screen",
  "status": "apply",
  "reason": "the `#` markers are stray: every other continuation line in the block is indented prose (ev.session.notice, ev.workspace.opened, cancel.<component>), so only the two approval entries read as code comments. Dropping them (the auditor's own suggested fix) makes the block internally consistent.",
  "evidence": "docs/MANUAL.md:514-518 vs 492-493,500-503,521-523",
  "old_string": "svc.approval.<name>.request # directed approval to the component driving the\n                           # turn (derived from the call envelope's `caller`);\n                           # driver acks {id, ack: true}, then answers {id, ok}\nev.approval.request    core → UI: {id, tool, args, caller?, fallback?} —\n                       # human gate, broadcast (see Approvals below)",
  "new_string": "svc.approval.<name>.request  directed approval to the component driving the\n                           turn (derived from the call envelope's `caller`);\n                           driver acks {id, ack: true}, then answers {id, ok}\nev.approval.request    core → UI: {id, tool, args, caller?, fallback?} —\n                       human gate, broadcast (see Approvals below)"
 },
 {
  "id": "A086",
  "slice": "Fabric and subagents",
  "status": "skip",
  "reason": "folded into A165's edit of the same two table rows: that edit adds the fresh-child clause ('its model/modelTier/thinking/tools/budgets come from this call and freeze at its first turn, exactly as a continuation's were'). A second edit would need the identical anchor.",
  "evidence": "docs/MANUAL.md:2467 (the row A165 rewrites)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A165",
  "slice": "Fabric and subagents",
  "status": "apply",
  "reason": "`modelTier?` is missing from both driver signatures: both schemas take it (components/agent/main.nim:1033,1178) and it resolves a fresh child's model through NIF_AGENT_MODEL_* clamped to the parent's tier, mutually exclusive with `model` (main.nim:687-693). The same edit carries A086's missing clause: a fresh child's model/thinking/tools/budgets are frozen at its first turn, exactly as a continuation's are (childSessArgs is consulted only on the fresh path, main.nim:1150,1216).",
  "evidence": "docs/MANUAL.md:2467-2468; components/agent/main.nim:1033,1178 (modelTier in both schemas), 693 (mutually exclusive), 860,1125,1275 (childSessArgs, fresh path only)",
  "old_string": "| `agent_run {task, session?, close?, fork?, model?, thinking?, tools?, maxRounds?, maxCalls?, maxTokens?, timeoutMs?}` | Run a task in a subagent session and return its final reply. Without `session` it starts a **fresh** child (own runner, own loop). With `session` (a previously returned `sessionId`) it gives that **existing child another turn** — its conversation, model, thinking, tools and budgets are frozen at its first turn, so the caller's model/thinking/tools/budget arguments are ignored and the result reports the child's `effective` controls; the child must belong to this conversation, must not be closed, and must not be mid-turn (that refuses with `code: \"busy\"` — use `agent_spawn` to queue instead). Optional per-job budgets on fresh runs: `maxRounds` (tool rounds per turn, 1–`NIF_MAX_TURN_ROUNDS`), `maxCalls` (total tool dispatches, 1-500), `maxTokens` (cumulative tokens) — exhaustion ends the turn as a budget-exhausted failure. `close: true` retires the child after this turn (nothing is deleted; later continuations refuse). |\n| `agent_spawn {task, session?, close?, fork?, model?, thinking?, tools?, maxRounds?, maxCalls?, maxTokens?, timeoutMs?}` | Start the same kind of task in the background; returns `{jobId, sessionId}` immediately. Without `session` it starts a fresh child; with `session` it **queues** another turn for an existing child (same frozen-controls rules as `agent_run`, but a mid-turn child is fine — the turn runs next; only the lineage parent may continue). `close: true` retires the child after the queued/background turn settles. `timeoutMs` is the job budget: once exceeded the job is cancelled (agent_stop semantics) the next time it is observed. |",
  "new_string": "| `agent_run {task, session?, close?, fork?, model?, modelTier?, thinking?, tools?, maxRounds?, maxCalls?, maxTokens?, timeoutMs?}` | Run a task in a subagent session and return its final reply. Without `session` it starts a **fresh** child (own runner, own loop): its `model`/`modelTier`/`thinking`/`tools`/budgets come from this call and freeze at its first turn, exactly as a continuation's were. With `session` (a previously returned `sessionId`) it gives that **existing child another turn** — its conversation, model, thinking, tools and budgets are frozen at its first turn, so the caller's model/thinking/tools/budget arguments are ignored and the result reports the child's `effective` controls; the child must belong to this conversation, must not be closed, and must not be mid-turn (that refuses with `code: \"busy\"` — use `agent_spawn` to queue instead). Optional per-job budgets on fresh runs: `maxRounds` (tool rounds per turn, 1–`NIF_MAX_TURN_ROUNDS`), `maxCalls` (total tool dispatches, 1-500), `maxTokens` (cumulative tokens) — exhaustion ends the turn as a budget-exhausted failure. `model` is an exact id, while `modelTier` (`weak`/`medium`/`strong`) resolves through `NIF_AGENT_MODEL_*` and is clamped to the parent's tier — the two are mutually exclusive. `close: true` retires the child after this turn (nothing is deleted; later continuations refuse). |\n| `agent_spawn {task, session?, close?, fork?, model?, modelTier?, thinking?, tools?, maxRounds?, maxCalls?, maxTokens?, timeoutMs?}` | Start the same kind of task in the background; returns `{jobId, sessionId}` immediately. Without `session` it starts a fresh child; with `session` it **queues** another turn for an existing child (same frozen-controls rules as `agent_run`, but a mid-turn child is fine — the turn runs next; only the lineage parent may continue). `close: true` retires the child after the queued/background turn settles. `timeoutMs` is the job budget: once exceeded the job is cancelled (agent_stop semantics) the next time it is observed. |"
 },
 {
  "id": "A166",
  "slice": "Fabric and subagents",
  "status": "apply",
  "reason": "`descendants` recurses the whole tree (the walk loops a frontier down the parent links, main.nim:1621-1637) and `NIF_AGENT_MAX_DEPTH` may exceed the default 1, so '(depth 1 today)' is wrong — depth 1 is what the default `children` scope means.",
  "evidence": "docs/MANUAL.md:2473; components/agent/main.nim:1590-1592,1621-1637; core/dispatch.nim:1551-1560,1688-1703; docs/MANUAL.md:429",
  "old_string": "`scope: \"descendants\"` walks the whole tree (depth 1 today).",
  "new_string": "`scope: \"descendants\"` walks the whole tree below you, while the default `children` is the depth-1 view; real nesting exists once `NIF_AGENT_MAX_DEPTH` is raised above 1."
 },
 {
  "id": "A167",
  "slice": "Fabric and subagents",
  "status": "apply",
  "reason": "`agent_ask` is implemented (approval-gated, 900 s dispatch budget) and named in the Approvals list but has no row in the agent tool table; on an idle child it is a continuation returning the answer, on a mid-turn child it queues as parent mail.",
  "evidence": "docs/MANUAL.md:2473 (row inserted before agent_list); components/agent/main.nim:1461-1479; docs/MANUAL.md:576",
  "old_string": "| `agent_list {scope?}` | The caller's subagent roster,",
  "new_string": "| `agent_ask {session, question, timeoutMs?}` | Ask a child a question and get its answer. An idle child answers directly — this is an ordinary continuation (`timeoutMs` bounds the wait, default 300 s) — while a **mid-turn** child cannot start a second turn, so the question is queued as parent mail and arrives with its next continuation (the result says `queued: true`, `deliveredVia: \"next-turn\"`). The same lineage authorization as `agent_run` applies: unknown, foreign and closed children refuse. Approval-gated. |\n| `agent_list {scope?}` | The caller's subagent roster,"
 },
 {
  "id": "A168",
  "slice": "Fabric and subagents",
  "status": "already",
  "reason": "the auditor's delta row reports the MANUAL matching the code (fabric-exec as a private compiled process + var/fabric-cache, the always-approval gate, fabric_help); re-verified: the executor compiles the guest with the Nim compiler into its own process, so 'no embedded VM' is right — the stale sentence lives in manifest.yaml's comment, not in the MANUAL.",
  "evidence": "docs/MANUAL.md:2465,2577-2583; components/fabric/executor.nim:1,53-78; manifest.yaml:242",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A169",
  "slice": "Fabric and subagents",
  "status": "already",
  "reason": "the auditor's delta row reports continuation/fork matching WIRE.md and the code with no delta; re-verified the load-bearing claims: the fork cut lands on block boundaries and fails closed, usage meters are dropped and summary/error roles skipped, the activation ledger advances with firstActivationAt set once, and `close: true` sets sessionmeta.closed.",
  "evidence": "docs/MANUAL.md:2503-2524,2539-2552; components/agent/main.nim:479-538,774,798-805; docs/WIRE.md:453-534",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A212",
  "slice": "The bus in one screen",
  "status": "already",
  "reason": "already fixed: the Approvals bullet now says the driver must ack within 1.5 s (`ackTimeoutSecs`) before the rebroadcast, and that with no interactive client registered the rebroadcast is skipped and the call is denied at once — exactly what core/approval.nim does.",
  "evidence": "docs/MANUAL.md:597-600; core/approval.nim:50,206-216",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A237",
  "slice": "Fabric and subagents",
  "status": "already",
  "reason": "both halves of the fix are in the current MANUAL: the Layout table's var/ row already lists `var/approval-sources/`, and the Approvals section describes it (digest-named full source, mode 0600).",
  "evidence": "docs/MANUAL.md:49 (the var/ row),617-620 (the approval-manifest paragraph)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A248",
  "slice": "The bus in one screen",
  "status": "apply",
  "reason": "five live event families were absent from the subject block: ev.log.<component> (every SDK), ev.lsp.warm, ev.agent.started/done/notice and the six ev.fabric.* events — plus llm.cancel.<sessionId>, which existed only in the streaming paragraph below. ev.workspace.opened was already listed.",
  "evidence": "docs/MANUAL.md:513 (insertion point in the block); sdk/go/component.go:288-303 (ev.log.<component>), components/lsp/main.nim:1286 (ev.lsp.warm), components/agent/main.nim:237,1012,1291 (ev.agent.*), components/fabric/fabric.nim:292,354,362,368,770,794 (ev.fabric.*)",
  "old_string": "ev.sys.drain           core → components: stop taking calls, finish, exit",
  "new_string": "ev.sys.drain           core → components: stop taking calls, finish, exit\nev.log.<component>     structured SDK logs: {component, level, msg, ctx?, at}\nev.lsp.warm            lsp → bus when a workspace warmup settles: {workspace, warmed, skipped}\nev.agent.started|done|notice  agent: a background job started, a job became\n                       terminal, a settlement was folded into the turn\nev.fabric.started|phase|log|call.started|call.done|done\n                       one fabric program's lifecycle, correlated by `runId`\nllm.cancel.<sessionId> abort an in-flight streaming call (see Streaming below)"
 },
 {
  "id": "A249",
  "slice": "The bus in one screen",
  "status": "already",
  "reason": "verified: all nine listed ev.session.* subjects exist, emitted dynamically from one helper (core/conversation.nim onEvent publishes 'ev.session.' & kind), and the streaming paragraph's ev.llm.token → ev.session.token chain matches the llm adapter. The suggested emitting-component column is an editorial nicety, not a defect — the block's one-line-per-subject form is deliberate.",
  "evidence": "docs/MANUAL.md:494-505,526-538; core/conversation.nim:2913-2915 (onEvent), 839,1104,1226,1336,1383",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A270",
  "slice": "The bus in one screen",
  "status": "already",
  "reason": "the report's placement note marks the block's cancel.<component> row as correct (and its 'FIX: none needed beyond the above' refers to the 30 s cancel-freshness and stashed-cancel rules, which the bash detail section now carries). The only optional extra — that bash polls the cancel probe every 50 ms — is an editorial nicety about promptness, not a contract anyone can act on.",
  "evidence": "docs/MANUAL.md:521-523,122-127; docs/audit/2026-09-18/components/bash.md §2 (cancel.bash) and §4 (MANUAL-placement note)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A340",
  "slice": "Fabric and subagents",
  "status": "apply",
  "reason": "the executor's environment is built from scratch: PATH, FABRIC_CACHE_DIR, HOME and TMPDIR pointing at the private build dir, and deliberately no NIF_* vars (no bus, no credentials) — the MANUAL stops at credentials.",
  "evidence": "docs/MANUAL.md:2587; components/fabric/fabric.nim:120-126",
  "old_string": "  The executor child holds no NATS connection and no credentials.",
  "new_string": "  The executor child holds no NATS connection, no credentials and no\n  inherited `NIF_*` environment: it sees only `PATH`, a scratch\n  `HOME`/`TMPDIR` and the cache path."
 },
 {
  "id": "A341",
  "slice": "Fabric and subagents",
  "status": "apply",
  "reason": "an identical program is cached, but the cache is not unbounded: 64 entries / 128 MB with least-recently-stored eviction (never touching an entry in use), swept at the start of every run.",
  "evidence": "docs/MANUAL.md:2465; components/fabric/fabric.nim:535-536,539-566,779",
  "old_string": "(no embedded VM; an identical program is cached in `var/fabric-cache`)",
  "new_string": "(no embedded VM; an identical program is cached in `var/fabric-cache`, self-bounded at 64 entries / 128 MB, evicting least-recently-stored)"
 },
 {
  "id": "A451",
  "slice": "component: processes",
  "status": "apply",
  "reason": "the claim is false: KEEP_FINISHED = 50 is declared and never referenced, and gProcs has no eviction path (only clear() at drain), so process_list returns every finished entry of the component's lifetime — which is also what process_list's own note says ('registry is per component lifetime'). The MANUAL is corrected to the shipped behaviour; implementing the cap is a separate code question, not a doc one.",
  "evidence": "docs/MANUAL.md:1473-1474; components/processes/main.nim:33-34 (KEEP_FINISHED declared, never used), 76, 421-436 (process_list returns every entry), 487",
  "old_string": "- Cap: 32 concurrent processes; the 50 most recent finished entries stay\n  in the registry.",
  "new_string": "- Cap: 32 concurrent processes. Finished entries are never evicted: every\n  process of this component's lifetime stays in the registry — only\n  *running* ones are written to `registry.json` — so `process_list`\n  keeps reporting a finished child as `exited(code N)` or\n  `killed(signal N)` until the component exits."
 },
 {
  "id": "A456",
  "slice": "component: processes",
  "status": "apply",
  "reason": "the four tool schemas declare per-tool dispatch budgets (start 20 s, poll 30 s, kill 15 s, list 10 s) that the MANUAL never mentions; it documents only the 25 s waitMs cap.",
  "evidence": "docs/MANUAL.md:1471-1472; components/processes/main.nim:500,514,521,526",
  "old_string": "  (default 64 KiB).",
  "new_string": "  (default 64 KiB).\n- Each tool carries its own dispatch budget `x-harness.timeoutMs` —\n  `process_start` 20 s, `process_poll` 30 s, `process_kill` 15 s,\n  `process_list` 10 s — separate from the `waitMs` cap inside\n  `process_poll`."
 },
 {
  "id": "A457",
  "slice": "component: processes",
  "status": "apply",
  "reason": "'default: workspace' holds only for calls that go through core dispatch: applyWorkspace fills a cwdField that is empty, '.' or relative with the conversation workspace, while a direct bus call leaves it empty and the child inherits the component's cwd; a workdir that does not exist is refused with E_BAD_SHAPE.",
  "evidence": "docs/MANUAL.md:1472; components/processes/main.nim:496,289-291; core/dispatch.nim:1457-1515",
  "old_string": "- Crash-safe: children are process-group leaders, so a SIGKILLed component",
  "new_string": "- `workdir` defaults to the conversation workspace: core substitutes it for\n  an empty or `.` value and resolves a relative path against the\n  workspace (`x-harness.workspace`), and a path that does not exist\n  fails with `E_BAD_SHAPE`. A bare bus call (`cli call process_start`)\n  skips that substitution, so the child inherits the component's own\n  cwd (`$NIF_ROOT`).\n- Crash-safe: children are process-group leaders, so a SIGKILLed component"
 },
 {
  "id": "A458",
  "slice": "component: processes",
  "status": "apply",
  "reason": "the two knobs are not validated alike: NIF_PROCESSES_POLL_CHUNK is clamped to [1024, 1048576], while NIF_PROCESSES_SPOOL_CAP is parsed without bounds and feeds the kept tail `min(2 MiB, cap div 2)`, so a cap at or below zero empties a spool on the next poll. The env table listed both as plain defaults.",
  "evidence": "docs/MANUAL.md:389-390; components/processes/main.nim:42-47,49-57,206",
  "old_string": "| `NIF_PROCESSES_SPOOL_CAP` | `processes` spool size before a background process's output file is truncated to its tail on the next poll | `33554432` |\n| `NIF_PROCESSES_POLL_CHUNK` | maximum new bytes one `process_poll` returns per stream (kept below the spool cap so a burst is always split) | `65536` |",
  "new_string": "| `NIF_PROCESSES_SPOOL_CAP` | `processes` spool size before a background process's output file is truncated to its tail on the next poll — the kept tail is `min(2 MiB, cap div 2)` and the value is not validated, so a cap at or below zero empties a spool | `33554432` |\n| `NIF_PROCESSES_POLL_CHUNK` | maximum new bytes one `process_poll` returns per stream (kept below the spool cap so a burst is always split; clamped to 1 KiB-1 MiB) | `65536` |"
 },
 {
  "id": "A459",
  "slice": "component: processes",
  "status": "apply",
  "reason": "the component's caller-visible error codes are undocumented: E_LIMIT at the 32-live cap and on a failed fork, E_NOT_FOUND for an unknown id, E_BAD_SHAPE for an empty command, a missing workdir and an invalid filter regex.",
  "evidence": "docs/MANUAL.md:1490; components/processes/main.nim:256,270,277,283,291,319",
  "old_string": "- `process_list` entries carry `started_at`",
  "new_string": "- Tool errors carry stable codes: `E_BAD_SHAPE` (empty `command`, a\n  missing `workdir`, an invalid `filter` regex), `E_NOT_FOUND` (unknown\n  id — `process_list` shows the registry) and `E_LIMIT` (32 live\n  processes, or a child that could not be forked).\n- `process_list` entries carry `started_at`"
 },
 {
  "id": "A461",
  "slice": "component: processes",
  "status": "skip",
  "reason": "the general rule is already stated and covers this component: the MANUAL's spawn section says replicas are for stateless or externally coordinated components and that single-writer components are never replicated; `processes`'s in-memory registry is the same class, so a per-component sentence would only repeat MANUAL:831-834.",
  "evidence": "docs/MANUAL.md:831-834; manifest.yaml:180-182 (no replicas)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A462",
  "slice": "component: processes",
  "status": "apply",
  "reason": "the example reads as if core rendered it: the badge comes from the niffler-tui plugin (process_list + process_poll {tail} and the 'bg N' status segment); nothing in core or the web UI reads these tools.",
  "evidence": "docs/MANUAL.md:1490-1491; var/plugins/niffler-tui@main/tui/processes.go:54,77,130 (the badge text) and .../tui/main.go:2785",
  "old_string": "  show how long something has been running (`bg 1 (7m)`).",
  "new_string": "  show how long something has been running — the `bg 1 (7m)` badge is the\n  `niffler-tui` plugin's status line, not core's."
 },
 {
  "id": "A463",
  "slice": "component: processes",
  "status": "already",
  "reason": "the auditor's verified row: four tools on-demand, the 32-live cap, drain semantics and filter cursor, the 64 KiB raw tail, per-stream cursors on O_APPEND spools, the exit-notice text and its lanes, E_BACKGROUND fallback, the boot sweep with the pid-reuse guard and the env defaults are all right — re-checked against the component, nothing to change.",
  "evidence": "docs/MANUAL.md:1456-1461,1465-1472,1475-1489,1497-1501; components/processes/main.nim:33-36,93-143,200-262,346,383-392,468-470",
  "old_string": "",
  "new_string": ""
 }
]
