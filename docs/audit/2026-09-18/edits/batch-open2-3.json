[
  {
    "id": "A699",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The paragraph states the ring and probe bounds qualitatively; no number for any of them appears anywhere in the MANUAL. Added the defaults and the ranges the code enforces (ring 2000 / 16 MiB, per-entry 64 KiB, per-probe 2 MiB and 2000 entries, 32 probes) plus the user-visible consequence (the 33rd probe fails until one is removed).",
    "evidence": "docs/MANUAL.md:2405 vs components/observe/main.nim:16-32 (DefaultRingMessages 2000, DefaultRingBytes 16_777_216, DefaultEntryBytes 65_536, DefaultProbeBytes 2_097_152, DefaultMaxProbes 32, MaxProbeEntries 2000), :71-80 (configInt ranges 1..10000, 64 KiB..100 MiB, 1 KiB..1 MiB, 64 KiB..16 MiB, 1..256), :143-147 (a message above the entry cap becomes a base64 preview of three quarters of the cap), :184-195 (per-probe eviction), :410-412 (\"probe limit reached\")",
    "old_string": "The global ring is bounded by both message count and approximate wire bytes.\nEach targeted probe has independent count and byte bounds; the number of probes\nis also capped. Stopped probes remain queryable until `observe_remove` releases\ntheir memory.",
    "new_string": "The global ring is bounded by both message count and approximate wire bytes —\n2 000 messages and ~16 MiB by default (`NIF_OBSERVE_RING`, clamped 1..10000;\n`NIF_OBSERVE_RING_BYTES`, 64 KiB..100 MiB), with a single retained message\ncapped at 64 KiB (`NIF_OBSERVE_ENTRY_BYTES`, 1 KiB..1 MiB — a larger one is kept\nas a base64 preview). Each targeted probe has independent count and byte bounds\n— at most 2 000 entries and 2 MiB (`cap`, clamped 1..2000;\n`NIF_OBSERVE_PROBE_BYTES`, 64 KiB..16 MiB) — and the number of probes is also\ncapped at 32 (`NIF_OBSERVE_MAX_PROBES`, 1..256), so the 33rd\n`observe_listen`/`observe_trace` fails until one is removed. Stopped probes\nremain queryable until `observe_remove` releases their memory."
  },
  {
    "id": "A700",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "\"limited to 30 seconds\" conflates two different bounds: the 100..30000 ms argument clamp and the tool's own 35 s `x-harness.timeoutMs`. Stated both, keeping the row on one line.",
    "evidence": "docs/MANUAL.md:2423 vs components/observe/main.nim:376 (`timeoutMs: 35_000` on the observe_request registration) and :387-393 (fatal unless 100..30000)",
    "old_string": "| `observe_request` | Diagnostic request/reply to a concrete `svc.*.call`; approval-gated and limited to 30 seconds |",
    "new_string": "| `observe_request` | Diagnostic request/reply to a concrete `svc.*.call`; approval-gated. The request wait is clamped to 100–30000 ms (`timeoutMs`), and the tool call itself carries `x-harness.timeoutMs: 35000` |"
  },
  {
    "id": "A701",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The row names the filters without saying what they accept. Added the `kind` enum, the three ways `component` matches, the exactness of `subject`, and the 1..500 clamp on `limit`.",
    "evidence": "docs/MANUAL.md:2418 vs components/observe/main.nim:521-534 (kind must be call|result|event|error), :551 (`min(max(limit, 1), 500)`), :574-579 (component matches `svc.<component>.` prefix, `ev.log.<component>`, or the envelope's own `component` field; subject is exact)",
    "old_string": "| `observe_events` | Query a probe or the global ring, newest first, with time/kind/component/subject/regex filters |",
    "new_string": "| `observe_events` | Query a probe or the global ring, newest first, with time/kind/component/subject/regex filters; `kind` is `call|result|event|error`, `component` matches `svc.<component>.*`, `ev.log.<component>` or an envelope's own `component` field, `subject` is an exact concrete subject, and `limit` is clamped to 1..500 |"
  },
  {
    "id": "A702",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The row omits the probe's own bounds and the shape of the id every later call needs. Added `cap` (default 500, clamped 1..2000) and `probeId` = `pr-<id>`, shared with `observe_trace`.",
    "evidence": "docs/MANUAL.md:2413 vs components/observe/main.nim:415-417 (`cap: int = 500`), :405-406 (`cap: min(max(cap, 1), MaxProbeEntries)`, MaxProbeEntries 2000 at :21), :414 (`\"pr-\" & newId()`), :445-446 (the result carries probeId + cap)",
    "old_string": "| `observe_listen` | Start a bounded capture for a token-correct NATS pattern (`*` and terminal `>`) plus optional regex |",
    "new_string": "| `observe_listen` | Start a bounded capture for a token-correct NATS pattern (`*` and terminal `>`) plus optional regex; `cap` defaults to 500 and is clamped to 1..2000, and the returned `probeId` is `pr-<id>` (same for `observe_trace`) |"
  },
  {
    "id": "A703",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The row's \"one component\" hides the two subject shapes actually watched, and it never mentions `toolRegex`, `cap` or the monotonic `elapsedMs`. All four added.",
    "evidence": "docs/MANUAL.md:2414 vs components/observe/main.nim:206-211 (traceSubjectMatches: `svc.<component>.call` and the four-token `svc.<component>.<id>.call`), :447-459 (toolRegex, `cap` default 500), :243-251 (the pending table is trimmed to the cap), :258-262 (`elapsedMs` from MonoTime arithmetic)",
    "old_string": "| `observe_trace` | Capture calls to one component and correlate result/error inbox replies by envelope id |",
    "new_string": "| `observe_trace` | Capture calls to one component and correlate result/error inbox replies by envelope id — it watches `svc.<component>.call` and the scoped `svc.<component>.<id>.call` form, `toolRegex` filters by tool name, `cap` defaults to 500 (max 2000), and each correlated reply carries `elapsedMs` from a monotonic clock |"
  },
  {
    "id": "A704",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "Three things the row left out, all of them what the tool is read for: the `session-<id>` to `svc.session.<id>.call` mapping, the fixed known-event list, and the census/snapshot facts (top 100, `_INBOX.*` excluded, best-effort 250 ms core snapshot, `*Truncated`/`dropped*` counters). Kept in the row rather than a new paragraph, so neighbouring text is untouched.",
    "evidence": "docs/MANUAL.md:2412 vs components/observe/main.nim:292-295 (serviceSubject: `session-<id>` maps to `svc.session.<id>.call`), :33-37 (KnownEvents, 13 entries), :303-317 (best-effort `catalog` request with a 250 ms deadline), :328-334 (components/componentsTruncated), :346-348 (the top-100 slice) with :358 (observedSubjectsTruncated), :355-359 (droppedComponentNames and droppedSubjectNames in the result), :167-175 (`_INBOX.` excluded, 2000-subject cap with droppedSubjects)",
    "old_string": "| `observe_subjects` | List the authoritative component/service view when core is reachable, known event patterns, and the most frequently observed concrete subjects |",
    "new_string": "| `observe_subjects` | List the authoritative component/service view when core is reachable (a `session-<id>` component maps to `svc.session.<id>.call`), the fixed known-event set (`reg.publish`, `reg.depart`, `ev.sys.drain`, `ev.catalog.updated`, `ev.llm.token`, `ev.session.>`, the three `ev.approval.*` subjects, `svc.approval.>.request`, `ev.log.>`, `ev.models.updated`, `llm.cancel.>`), and the most frequently observed concrete subjects (top 100, `_INBOX.*` excluded); the core view is a best-effort 250 ms `catalog` request and the `*Truncated`/`dropped*` counters say when an answer is partial |"
  },
  {
    "id": "A705",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The row said \"export of one probe\" without the file it writes. Added the generated path and line format, the 0700/0600 modes, the symlink refusal, the prune rule and the failure text; the quota clause that already stood just below the table is left alone.",
    "evidence": "docs/MANUAL.md:2420 vs components/observe/main.nim:688-710 (`captureDir/<probeId>.jsonl`, one JSON object per line, createDir + 0700, file 0600, symlink refused), :652-675 (prune to MaxCaptureFiles 256 and captureByteCap; \"capture directory quota is exhausted\")",
    "old_string": "| `observe_dump` | Approval-gated export of one probe beneath `NIF_OBSERVE_CAPTURE_DIR`; arbitrary output paths are not accepted |",
    "new_string": "| `observe_dump` | Approval-gated export of one probe beneath `NIF_OBSERVE_CAPTURE_DIR` (the file is `<captureDir>/<probeId>.jsonl`, one JSON object per captured entry, in a directory created user-only — 0700, files 0600 — with a symlinked target refused and older captures pruned oldest-first to a byte quota and a 256-file cap; when even pruning cannot fit the dump the tool fails with `capture directory quota is exhausted`); arbitrary output paths are not accepted |"
  },
  {
    "id": "A706",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "One clause for the boundary that matters (`observe_logs` sees the in-memory ring only, `logfile_search` is the persisted view) and one for the item shape and the 1..500 clamp.",
    "evidence": "docs/MANUAL.md:2419 vs components/observe/main.nim:591-651 (ring only, `payload.msg`/`level`/`ctx`, `emittedAt` from the emitter's `at`, limit `min(max(limit, 1), 500)`), components/logfile/main.nim:296-300 (logfile_search is the persisted counterpart)",
    "old_string": "| `observe_logs` | Query recent `ev.log.*` events in memory |",
    "new_string": "| `observe_logs` | Query recent `ev.log.*` events in memory — the ring only, for persisted history use `logfile_search`; items carry `component`, `level`, `msg` plus the emitter's `at` as `emittedAt` and `ctx` when present, and `limit` is clamped to 1..500 |"
  },
  {
    "id": "A716",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "Boundary covers trust but not the single-instance requirement: manifest.yaml sets no `replicas`, and the ring, the probes and the component census are process-local, so replicas would split one view into several. One sentence appended to the trust paragraph (the `**Not an audit trail.**` paragraph added by the A236 pass is untouched).",
    "evidence": "docs/MANUAL.md:2386 vs manifest.yaml:196-203 (no `replicas` key), components/observe/main.nim:87-93 (ring, probes, components and subjectCounts are component-process globals; `grep -c 'storePut\\|onIdle\\|selftest' components/observe/main.nim` is 0)",
    "old_string": "current trust model is a single trusted user/admin; do not expose the observe\nservice or capture directories to untrusted bus clients.",
    "new_string": "current trust model is a single trusted user/admin; do not expose the observe\nservice or capture directories to untrusted bus clients. Nor can it be\nreplicated: the ring, the probes and the component census are process-local, so\nreplicas would split one view into several and multiply exactly that exposure —\nthe manifest leaves `replicas` unset for this reason."
  },
  {
    "id": "A679",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The search paragraph documents the scan but not the tool contract. Added one paragraph: the flags (on demand, no `x-harness.effect`, so the fabric batch host treats a read as a write), the argument set with its clamps, and the two surprising bits — an invalid argument arrives inside a *successful* result, and the schema carries no `timeoutMs`, so core's 120 s default deadline applies.",
    "evidence": "docs/MANUAL.md:2482 vs components/logfile/main.nim:292 (`{\"onDemand\": true}` only — no effect, no timeoutMs), :301-323 (component max 64 chars, level enum, regex <=1024 bytes, `since > until` refused, limit clamped 1..500), :307-322 (every invalid argument returns `%*{\"error\": ...}` from a handler the SDK still wraps as a result), sdk/niffler/sdk.nim:721-729 (a returned node becomes a result envelope, only a raise becomes an error), core/dispatch.nim:171-172 (120000 ms default deadline), components/fabric/fabric.nim:218-239 and :327 (an undeclared effect schedules as a write)",
    "old_string": "Directory enumeration is capped by `NIF_LOGFILE_DIRECTORY_ENTRIES` and reports\n`directoryTruncated` when more files exist; searches still inspect the bounded\nsubset.",
    "new_string": "Directory enumeration is capped by `NIF_LOGFILE_DIRECTORY_ENTRIES` and reports\n`directoryTruncated` when more files exist; searches still inspect the bounded\nsubset.\n\nBoth read tools are `onDemand` and declare no `x-harness.effect`, so the fabric\nbatch host schedules even a read as a write. `logfile_search` takes\n`{component?, level? (debug|info|warn|error), regex? (≤1024 bytes), since?,\nuntil? (epoch seconds, `since ≤ until`), limit? (default 100, cap 500)}`; an\ninvalid argument comes back as `{\"error\": …}` inside a *successful* result\nrather than as an error envelope, and with no `timeoutMs` on the schema the call\nruns under core's 120 s default deadline."
  },
  {
    "id": "A680",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The row named three of the fields the tool returns and said nothing about the directory it owns. Added the 500-file cap, `maxBytes`/`keep`/`subjects` and the component-file count, and corrected \"Capture directories\" to the **log directory** with its 0700/0600 modes and the symlink refusal. The on-demand/no-effect clause both rows asked for sits in the A679 paragraph immediately above rather than being duplicated here.",
    "evidence": "docs/MANUAL.md:2486 vs components/logfile/main.nim:448 (`{\"onDemand\": true}`), :463-491 (MaxPathItems 500, then the health keys maxBytes, keep, maxComponentFiles, componentFiles, subjects, totalFiles, truncated, directoryTruncated), :156 (directory 0700), :174 (file 0600), :160-161 (a symlinked log file is refused)",
    "old_string": "`logfile_paths` reports a bounded retained-file list plus `writeErrors`,\n`lastError`, and `lastErrorAt`. Filesystem failures also go to stderr. Capture\ndirectories are user-only where the platform permits; active symlink targets\nare rejected.",
    "new_string": "`logfile_paths` reports up to 500 retained files plus `writeErrors`,\n`lastError`, `lastErrorAt`, `maxBytes`, `keep`, `subjects` and the component-file\ncount. Filesystem failures also go to stderr. The **log directory** is created\nuser-only (0700, files 0600) where the platform permits, and an active symlink\nat a log path is refused."
  },
  {
    "id": "A681",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The rotation paragraph never names the generations or the boot prune. Added both — including the consequence that lowering `NIF_LOGFILE_KEEP` destroys history at the next start — plus the re-seeding of the file registry from `.jsonl` files already on disk.",
    "evidence": "docs/MANUAL.md:2472 vs components/logfile/main.nim:105-120 (rotate shifts `<file>.<keepN>` down and renames the live file to `<file>.1`), :122-136 (pruneRotations deletes `*.jsonl.<n>` with n above keep), :194 (called at boot), :195-201 (the boot registry seeded from existing `.jsonl`, bounded by maxComponentFiles)",
    "old_string": "file and rotated before the next record. `NIF_LOGFILE_KEEP=0` retains no rotated\ngeneration.",
    "new_string": "file and rotated before the next record. `NIF_LOGFILE_KEEP=0` retains no rotated\ngeneration. Rotated generations are named `<file>.1` … `<file>.<KEEP>`, and\nevery boot deletes any generation numbered above the current\n`NIF_LOGFILE_KEEP` — lowering the knob destroys history at the next start.\n`.jsonl` files already on disk at boot also count against\n`NIF_LOGFILE_MAX_FILES`."
  },
  {
    "id": "A682",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "The paragraph explains what is written but not that the pattern list is validated fatally at boot, nor which subscription the tap ends up taking. Both appended.",
    "evidence": "docs/MANUAL.md:2458 vs components/logfile/main.nim:204-227 (an invalid or over-512-byte pattern, more than 64 unique patterns, or an empty result raises before `comp.run()`; `>` collapses the list; one pattern is tapped as itself, several patterns tap `>` and filter locally), :25-26 (MaxPatterns 64, MaxPatternBytes 512), :53-59 (validComponentName) and :61-69 (validSubjectPattern)",
    "old_string": "`bus.jsonl`. Multiple configured patterns are treated as one locally filtered\nunion, so overlapping patterns persist each matching publication exactly once.",
    "new_string": "`bus.jsonl`. Multiple configured patterns are treated as one locally filtered\nunion, so overlapping patterns persist each matching publication exactly once.\nThe list is validated at boot: a malformed or over-512-byte pattern, more than\n64 unique patterns, or an empty result makes the component exit non-zero. With\nseveral patterns the tap is `>` and matching happens locally; a single pattern\nis subscribed as itself."
  },
  {
    "id": "A683",
    "slice": "Observation and logs",
    "status": "apply",
    "reason": "\"A valid component name\" never said which names are valid or where the file lands. Replaced it with the name pattern and `NIF_LOGFILE_DIR`'s absolute-versus-root-relative rule.",
    "evidence": "docs/MANUAL.md:2446 vs components/logfile/main.nim:91-99 (`ev.log.<name>` with `validComponentName` = `[a-z0-9-]{1,64}` becomes `<name>.jsonl`, everything else `bus.jsonl`), :32-36 (`NIF_LOGFILE_DIR`: empty -> `$NIF_ROOT/var/logs`, absolute -> as-is, relative -> resolved against the harness root), :53-59 (the name predicate)",
    "old_string": "Default input is `ev.log.>`. A valid component name gets one file:",
    "new_string": "Default input is `ev.log.>`. One file per component name matching\n`[a-z0-9-]{1,64}`, under `NIF_LOGFILE_DIR` (`$NIF_ROOT/var/logs` by default; an\nabsolute value is used as-is, a relative one is resolved against the harness\nroot):"
  },
  {
    "id": "A587",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified row (wave-2 console report, \"FIX: fix: none — verified\"). The sentence it audits is the one the A236 pass added and it is still true: `console` subscribes and renders with no ring, no file and no state, and hooks keep nothing. Nothing for the MANUAL. (The pass that fixed it: the batch-open-1..6 consolidation, row A236.)",
    "evidence": "docs/MANUAL.md:2392 vs components/console/main.nim:1-11 (subscribe and render only), components/hooks/main.nim:85-114 (runHook runs the command, logs a failure to stderr, persists nothing)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A677",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "The row's own FIX is \"none — [verified]\": the sentence belongs to the two components this section documents, and every numeric `NIF_OBSERVE_*` / `NIF_LOGFILE_*` knob goes through `configInt` and exits non-zero outside its range. `hooks` deliberately clamps instead, which its own environment row states; the sentence is not about it.",
    "evidence": "docs/MANUAL.md:2597 vs components/observe/main.nim:71-80 and components/logfile/main.nim:37-45 (all numeric knobs via configInt), sdk/niffler/sdk.nim:268-284 (configInt raises ValueError outside the range)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A684",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified (\"FIX: none — [verified]\"): the at-most-once framing matches a component that publishes nothing and calls nothing — no `comp.emit`, no `publishEnvelope`, no store, no idle seam.",
    "evidence": "docs/MANUAL.md:2442 vs components/logfile/main.nim:1-7 (header) and the file as a whole (the only `emit`-like hit is the `emittedAt` field read at :406-407)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A685",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: `since`/`until` are compared against the sink's own `receivedAt`, and the emitter's `at` is only echoed as `emittedAt`.",
    "evidence": "docs/MANUAL.md:2480 vs components/logfile/main.nim:381-384 (`receivedAt` is the filter key) and :406-407 (emitter `at` echoed as `emittedAt`)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A686",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: enumeration stops at `NIF_LOGFILE_DIRECTORY_ENTRIES` and sets `directoryTruncated`; the accepted range 100..100000 belongs to the environment-table row, which another batch owns.",
    "evidence": "docs/MANUAL.md:2482 vs components/logfile/main.nim:44-45 (range 100..100000), :330-335 (search enumeration cap), :458-460 (paths enumeration cap)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A697",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: the test is named and its coverage list matches the file's scope (rotation, zero retention, exact-once overlapping patterns, response bounds, sink health, invalid configuration).",
    "evidence": "docs/MANUAL.md:2608 vs tests/t_logfile.nim:1-374 and Makefile:514 (`test-logfile`)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A707",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: the subject must be concrete and must start with `ev.` or `llm.cancel.`, and the published envelope is always `kind: event` (`comp.emit`) — which is exactly what makes the \"cannot send call/result/error envelopes or registrations\" sentence true.",
    "evidence": "docs/MANUAL.md:2422 vs components/observe/main.nim:361-375 (validSubject with wildcards off, the ev./llm.cancel. prefix check, `comp.emit`), sdk/niffler/sdk.nim:175-177 (emit builds a kind: event envelope)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A708",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: exactly those four tools carry `x-harness.approval: always`; the other eight only read in-memory state.",
    "evidence": "docs/MANUAL.md:2426 vs components/observe/main.nim:361 (observe_send), :376 (observe_request), :677 (observe_dump), :714 (observe_monitor)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A709",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: `prepareCapture` prunes oldest-first against `NIF_OBSERVE_CAPTURE_BYTES` (default 67108864, clamped 64 KiB..1 GiB) and `MaxCaptureFiles = 256`.",
    "evidence": "docs/MANUAL.md:2431 vs components/observe/main.nim:652-675 (the prune loop) and :31-32, :79-80 (the two constants)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A710",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: `MaxPendingSeconds = 60.0`, pruned on every capture into a trace probe and by `observe_probes`.",
    "evidence": "docs/MANUAL.md:2433 vs components/observe/main.nim:22 and :197-204 (prunePending), called from :226 (captureFor) and :481 (observe_probes)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A711",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: 512/256/1024-byte input limits, the `dropped` counter for an oversized probe entry, the 60000-byte response budget with `truncated`, and the `valueTruncated`/`valueBytes` metadata for an oversized diagnostic reply.",
    "evidence": "docs/MANUAL.md:2434 vs components/observe/main.nim:25-29 (the limits), :186-188 (`inc pr.dropped` above the probe byte cap), :23-24 (the response budget), :584-590 (observe_events truncation), :398-408 (valueTruncated/valueBytes)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A712",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: one `discard comp.tap(\">\", onBus)`, and `wireMessage` preserves the original JSON node or the raw bytes.",
    "evidence": "docs/MANUAL.md:2399 vs components/observe/main.nim:290 (the single tap), :142-157 (wireMessage), :278-287 (reg.publish/reg.depart read off the tap)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A713",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified (\"FIX: none — [verified]\"): a fresh HttpClient per request, `/subsz?subs=1&limit=1024` plus `/connz`, a 3 s client timeout, and a plain error naming `NIF_OBSERVE_MONITOR_URL` when no endpoint resolves. The extras the auditor listed as \"plus\" (limit, timeout, error text) are precision, not a contradiction — the MANUAL's two sentences stay true, so no edit.",
    "evidence": "docs/MANUAL.md:2590 vs components/observe/main.nim:719-725 (env, else the discovery file, else \"no NATS monitoring endpoint\"), :727-738 (fresh client, timeout 3000, `/subsz?subs=1&limit=1024`, `/connz`), :753-767 (the reported counts and the truncation flag)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A714",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: every numeric `NIF_OBSERVE_*` goes through `configInt`, which raises outside its range, so the process exits non-zero.",
    "evidence": "docs/MANUAL.md:2597 vs components/observe/main.nim:71-80, sdk/niffler/sdk.nim:268-284",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A715",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified against the current table: all eight `NIF_OBSERVE_*` rows and all seven `NIF_LOGFILE_*` rows are present with the right defaults, and `NIF_LOG_LEVEL` is documented in the Environment variables section.",
    "evidence": "docs/MANUAL.md:2594 vs docs/MANUAL.md:444-458 (the fifteen rows) and components/observe/main.nim:71-85, :719, components/logfile/main.nim:32-45",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A717",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "The row's FIX is \"none — [delta] note only\": the two error conventions (raised argument checks the SDK turns into an error envelope, versus `errResult` for a bad regex or an unknown probe) both reach the caller with the right text, and the difference earns a clause only if the tool table ever gains a parameters column. No MANUAL claim is false, so no edit.",
    "evidence": "docs/MANUAL.md:2429 vs components/observe/main.nim:387-393, :429-436, :443, :468, :531, :557 (the raises) and the errResult returns beside them; sdk/niffler/sdk.nim:721-729 (a returned node is a result envelope, only a raise is an error)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A728",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified: the test is named and its coverage list matches the file's scope.",
    "evidence": "docs/MANUAL.md:2602 vs tests/t_observe.nim:1-485 and Makefile:513 (`test-observe`)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A782",
    "slice": "Observation and logs",
    "status": "already",
    "reason": "Verified accurate: `natsServerBinary` returns `getAppDir()/nats-server` when that file exists and otherwise the PATH `nats-server`, with the `ours` flag (only the component build understands `--max_payload`) choosing between flag and config file. The MANUAL sentence says exactly that.",
    "evidence": "docs/MANUAL.md:2576 vs core/niffler.nim:51-60 (natsServerBinary), :82 (the ours flag decides the payload route), :414-425 (nats-url first, then nats-monitor-url only when there is a monitor endpoint — a reused or remote bus leaves none)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A781",
    "slice": "Common tasks",
    "status": "apply",
    "reason": "The bullet claimed core \"reuses a live bus on 127.0.0.1:4222\" and only spawns when none answers. It reuses only a bus whose answering core serves **this root**; a foreign harness or a bare nats-server makes core warn and spawn an isolated bus (a leftover `var/nats-pid` naming one of this root's own buses is reclaimed first). Corrected in place, same bullet.",
    "evidence": "docs/MANUAL.md:3048 vs core/niffler.nim:138-166 (probeBus: bkFree / bkOurs when `catalog` answers `root == ours` / bkForeignCore / bkBareNats), :354-383 (only bkOurs reuses; bkForeignCore and bkBareNats print a WARNING and spawn on port -1), :364 (bkOurs branch), :374-376 (bkBareNats -> reclaimOwnNats else isolated), :177-201 (reclaimOwnNats verifies pid + /proc comm)",
    "old_string": "- **Attach to any bus**: `NIF_NATS_URL=nats://host:4222` (even remote), or\n  start your own nats-server on the default port before core — core reuses\n  a live bus on `127.0.0.1:4222` and only spawns its own (the built\n  `var/bin/nats-server` component) when none answers.",
    "new_string": "- **Attach to any bus**: `NIF_NATS_URL=nats://host:4222` (even remote), or\n  start your own nats-server on the default port before core — core reuses\n  a bus on `127.0.0.1:4222` only when the core answering it serves **this\n  root**; a foreign harness, or a bare nats-server with no core on it, makes\n  core warn and spawn an isolated bus instead (a leftover `var/nats-pid`\n  naming one of this root's own buses is reclaimed first). To force a bus\n  deliberately, set `NIF_NATS_URL`."
  },
  {
    "id": "A658",
    "slice": "Common tasks",
    "status": "apply",
    "reason": "The `make install` comment reads as if a *host* grep/git binary were what PATH must not shadow. Naming the component's own binary in the same parenthetical removes the ambiguity: it is `var/bin/grep` (which shells out to `rg`) that stays off PATH. The other half of the finding — a clause in a `grep` chapter — has no home: the MANUAL has no grep section, so the clarification lands on the line the finding quotes.",
    "evidence": "docs/MANUAL.md:3025 vs manifest.yaml:117-118 (the grep entry's `binary: var/bin/grep`), components/grep/main.nim:26-32 (runRg shells out to `rg`, never to the host `grep`), scripts/install.sh:110-114 (\"plain symlinks — niffler-prefixed names only\": niffler, niffler-cli, niffler-console)",
    "old_string": "make install        # PATH entries (niffler, niffler-cli, niffler-console,\n                    # + niffler-tui wrapper on request — never component\n                    # binaries, so PATH cannot shadow grep/git/...)",
    "new_string": "make install        # PATH entries (niffler, niffler-cli, niffler-console,\n                    # + niffler-tui wrapper on request — never component\n                    # binaries such as the `grep` tool (`var/bin/grep`, which\n                    # shells out to `rg`), so PATH cannot shadow grep/git/...)"
  },
  {
    "id": "A589",
    "slice": "Common tasks",
    "status": "already",
    "reason": "Verified row (wave-2 console report, \"FIX: fix: none — verified\"): `make install` really does put `niffler-console` on PATH, and the MANUAL block lists it. Nothing to write.",
    "evidence": "docs/MANUAL.md:3025 vs scripts/install.sh:111 (`link niffler-console console`) with the linker at :108-114, :84 (uninstall loop) and Makefile:374 (`install: build`)",
    "old_string": "",
    "new_string": ""
  },
  {
    "id": "A575",
    "slice": "Model catalog (models)",
    "status": "already",
    "reason": "Verified row (wave-2 cli report, \"fix: none — verified\"): the cli's tool index is built from the `catalog {op: components}` snapshot, i.e. every tool core accepted, on-demand or hidden, so \"Components and `cli call` address them directly by name\" is accurate as written. The finding's one residual note (the same mechanism reaches `hidden` tools, a sentence the cli component's own report files elsewhere) does not make this sentence false, so no edit.",
    "evidence": "docs/MANUAL.md:2179 vs components/cli/main.nim:38-69 (toolIndex rebuilt from the catalog snapshot, `owners[tname] = name` for every accepted tool), :105-119 (callTool dispatches by that index) — contrast the on-demand paragraph it sits in, which is about the *conversation's* frozen set",
    "old_string": "",
    "new_string": ""
  }
]
