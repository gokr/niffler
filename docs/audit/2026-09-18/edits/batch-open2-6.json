[
 {
  "id": "A562",
  "slice": "component-cli",
  "status": "apply",
  "reason": "the cli paragraph never says that `cli` is a plain bus client with no core gate: its calls skip the approval gate, the workspace/session injection, a tool's `x-harness.timeoutMs` and the hidden/on-demand filter",
  "evidence": "components/cli/main.nim:103-124 (`callTool` requests `svc.<component>.call` directly; only `refreshCatalog` uses `svc.core.call`), core/dispatch.nim:1439-1457 (`__workspace` injection), :1620-1630 (the approval gate and the per-tool timeout live in core's dispatch)",
  "old_string": "Name-based verification\ndoes not distinguish an existing accepted component from a newly started\nprocess with the same name.",
  "new_string": "Name-based verification\ndoes not distinguish an existing accepted component from a newly started\nprocess with the same name.\n\nIt is a plain bus client: `cli call` dispatches straight to\n`svc.<component>.call` (only the catalog read goes through core), so its calls\nbypass the approval gate, the workspace and session injection, a tool's\n`x-harness.timeoutMs` and the hidden/on-demand filter — `cli call del …` and\n`cli call chat …` work. Treat it as an operator tool: give it the trust you\ngive the shell you started the harness from."
 },
 {
  "id": "A569",
  "slice": "component-cli",
  "status": "already",
  "reason": "no edit: the sentence matches the code today — `plugin_install` is called with a 600 s timeout, a 60 s wait for `plugins` and a 60 s registration wait per spawned component, interactive components are verified by their build, and the two outcomes are `INSTALL OK`/`INSTALL FAILED`. Only the three budgets are unstated, which does not make the prose wrong",
  "evidence": "docs/MANUAL.md:625-628; components/cli/main.nim:158-207",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A570",
  "slice": "component-cli",
  "status": "already",
  "reason": "no edit: verified — both indexes are replaced on every read from core's snapshot and `call` refuses a tool core has not accepted, so a component core rejected is unreachable through `cli` even while it answers on the bus",
  "evidence": "docs/MANUAL.md:628-629; components/cli/main.nim:34-72, 149-153",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A573",
  "slice": "component-cli",
  "status": "already",
  "reason": "no edit: verified — `cli` sends no `__session`/`sessionId`, so its calls are unattributed; this sentence is the MANUAL's own statement of it",
  "evidence": "docs/MANUAL.md:1703-1706; components/cli/main.nim:103-124",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A576",
  "slice": "testing",
  "status": "already",
  "reason": "no edit: the hand-list of targets is gone and `make test-cli` exists (`t_cli` + `t_cli_catalog`), so the sentence is right",
  "evidence": "docs/MANUAL.md:2926; Makefile:521-523; tests/t_cli.nim, tests/t_cli_catalog.nim",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A590",
  "slice": "testing",
  "status": "already",
  "reason": "no edit: `make test-ui` is exactly 'lib unit tests + `npm run typecheck`' and `make test-console` exists separately (`t_console`)",
  "evidence": "docs/MANUAL.md:2925; Makefile:482-486, 505; tests/t_console.nim",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A625",
  "slice": "testing",
  "status": "apply",
  "reason": "the Testing section never names the compaction verification lane: the end-to-end fixtures, the contract-v1 conformance runner a third-party summarizer is accepted with, and the opt-in live gate",
  "evidence": "Makefile:544 (`test-compaction`), :548 (`test-conformance`), :552 (`live-smoke`); tests/t_compaction_conformance.nim:5-16, 99-104 (`--bin:`/`--tool:`, defaulting to the shipped `var/bin/compaction`); tests/compaction_live_smoke.nim:1-5 (no `t_` prefix, hence outside `test-server`); Makefile:448,472-476 (the wildcard `t_*` suite that `make test-server` runs)",
  "old_string": "its process-group path and then proves the timeout kill at a 1 s budget,\nexpecting exit 124.",
  "new_string": "its process-group path and then proves the timeout kill at a 1 s budget,\nexpecting exit 124.\n\nThe compaction contract has a verification lane of its own:\n`make test-compaction` runs the end-to-end propose/commit/restart/reload\nfixtures, and `make test-conformance --bin:<path> --tool:<name>` points the\nsame contract-v1 suite at a replacement compactor — the acceptance test for a\nthird-party summarizer (both ride the wildcard `t_*` suite, so\n`make test-server` runs them too). `make live-smoke` is the opt-in live gate:\nreal summarization against a real provider, `SYNTHETIC_API_KEY=...`, outside\nthe suite.\n"
 },
 {
  "id": "A659",
  "slice": "testing",
  "status": "code",
  "reason": "the fix belongs in code, not prose: the wording the report proposes is not true of the current core — a component that registers no `selftest` is not reported as 'not implementing one', it is simply left out of the fan-out and the check reports how many components were probed. Register a hidden `selftest` tool in `components/grep` (the SDK's `Component.selfTest`, docs/WIRE.md 'Self tests') and `/doctor deep` covers it; no MANUAL edit follows from this row",
  "evidence": "core/dispatch.nim:844-877 (only components registering `selftest` are probed; `selftestComponents` counts those), core/catalog.nim:120, docs/WIRE.md:741-769; `grep -c selftest components/grep/main.nim` = 0; tests/t_grep.nim covers the bus contract itself",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A678",
  "slice": "testing",
  "status": "code",
  "reason": "the fix belongs in code, not prose: the wording the report proposes is not true of the current core — a component that registers no `selftest` is not reported as 'not implementing one', it is simply left out of the fan-out and the check reports how many components were probed. Register a hidden `selftest` tool in `components/hooks` (the SDK's `Component.selfTest`, docs/WIRE.md 'Self tests') and `/doctor deep` covers it; no MANUAL edit follows from this row",
  "evidence": "core/dispatch.nim:844-877 (only components registering `selftest` are probed; `selftestComponents` counts those), core/catalog.nim:120, docs/WIRE.md:741-769; `grep -c selftest components/hooks/main.nim` = 0; tests/t_hooks.nim covers the bus contract itself",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A698",
  "slice": "testing",
  "status": "code",
  "reason": "the fix belongs in code, not prose: the wording the report proposes is not true of the current core — a component that registers no `selftest` is not reported as 'not implementing one', it is simply left out of the fan-out and the check reports how many components were probed. Register a hidden `selftest` tool in `components/logfile` (the SDK's `Component.selfTest`, docs/WIRE.md 'Self tests') and `/doctor deep` covers it; no MANUAL edit follows from this row",
  "evidence": "core/dispatch.nim:844-877 (only components registering `selftest` are probed; `selftestComponents` counts those), core/catalog.nim:120, docs/WIRE.md:741-769; `grep -c selftest components/logfile/main.nim` = 0; tests/t_logfile.nim covers the bus contract itself",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A729",
  "slice": "testing",
  "status": "code",
  "reason": "the fix belongs in code, not prose: the wording the report proposes is not true of the current core — a component that registers no `selftest` is not reported as 'not implementing one', it is simply left out of the fan-out and the check reports how many components were probed. Register a hidden `selftest` tool in `components/observe` (the SDK's `Component.selfTest`, docs/WIRE.md 'Self tests') and `/doctor deep` covers it; no MANUAL edit follows from this row",
  "evidence": "core/dispatch.nim:844-877 (only components registering `selftest` are probed; `selftestComponents` counts those), core/catalog.nim:120, docs/WIRE.md:741-769; `grep -c selftest components/observe/main.nim` = 0; tests/t_observe.nim covers the bus contract itself",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A770",
  "slice": "troubleshooting",
  "status": "already",
  "reason": "no edit: verified — the file-backed engines refuse a second owner (flock) and the row's advice (a temp `NIF_ROOT`) is the shipped answer",
  "evidence": "docs/MANUAL.md:3067; components/store/main.nim:44-51, components/store-sqlite/main.go:32-38",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A771",
  "slice": "troubleshooting",
  "status": "already",
  "reason": "no edit: verified — the boot refusal the row quotes is the string core prints, together with the `niffler-store-migrate` instructions and the barrel escape hatch",
  "evidence": "docs/MANUAL.md:3068; core/niffler.nim:455-470",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A783",
  "slice": "troubleshooting",
  "status": "already",
  "reason": "no edit: verified — the three causes are the real ones (a manually started nats-server, a non-Linux host, a stale `var/nats-pid` that core re-validates by pid + comm before signalling)",
  "evidence": "docs/MANUAL.md:3069; components/nats/pdeathsig_linux.go:12-21, components/nats/pdeathsig_other.go:4-6, core/niffler.nim:176-201",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A778",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the bus payload budget is absent from the MANUAL: the spawned bus is raised to 8 MiB, a PATH nats-server gets it via a generated config file, and core warns at boot when an attached bus is below it — the failure it prevents looks like a component hang",
  "evidence": "core/niffler.nim:46-49 (`natsMaxPayload = 8388608`), :63-72 (`writeNatsConfig` for a PATH nats-server), :89-95 (`--max_payload` for the bundled binary), :400-411 (boot warning from `natsConnection_GetMaxPayload`)",
  "old_string": "A reused or remote bus has no discoverable HTTP endpoint; configure\n`NIF_OBSERVE_MONITOR_URL` explicitly. `NIF_NATS_SPAWN=1` forces an isolated\ncore-owned bus on a random port (primarily for tests and diagnostics) —\nnever 4222; an explicit `NIF_NATS_URL` in the environment wins.",
  "new_string": "A reused or remote bus has no discoverable HTTP endpoint; configure\n`NIF_OBSERVE_MONITOR_URL` explicitly. `NIF_NATS_SPAWN=1` forces an isolated\ncore-owned bus on a random port (primarily for tests and diagnostics) —\nnever 4222; an explicit `NIF_NATS_URL` in the environment wins.\n\nA request carries a whole conversation, so the bundled bus is started with\n`max_payload: 8388608` (8 MiB — about 2M tokens of JSON; above that\nnats-server only warns): the bundled `var/bin/nats-server` takes it as an\nextra flag, while a PATH `nats-server` gets it in a generated config file\ninstead. When core attaches to a bus it did not spawn, it reads the server's\nreal cap and warns at boot when it is below 8 MiB — such a bus rejects a large\nreply at publish time and the caller waits out its full timeout, which looks\nlike a component hang rather than a bus limit."
 },
 {
  "id": "A779",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "how the spawned bus picks its ports is undocumented: NATS' own `--ports_file_dir` handshake and which port core asks for (the home port when it is free, `-1` for an isolated bus)",
  "evidence": "core/niffler.nim:74-116 (`--ports_file_dir`, the `*.ports` poll bounded at 200 x 20 ms, the client + monitoring ports read back), :348 (`NIF_NATS_SPAWN=1` -> `[\"-1\"]`), :358-381 (`bkFree` -> the home port; a foreign core or a bare nats-server -> `[\"-1\"]`)",
  "old_string": "The monitor discovery file is written only after the client connection succeeds.",
  "new_string": "The monitor discovery file is written only after the client connection succeeds.\nNATS allocates the ports itself: core passes `--ports_file_dir <tmp>` and\nreads the client and monitoring ports back from the `*.ports` file it writes\n(a bounded 4 s wait), so concurrent harnesses cannot win a bind-close-start\nrace. Core passes the home port when it is free and `-1` (a random port) when\nit starts an isolated bus."
 },
 {
  "id": "A784",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the test-only fixture components and stub LLMs are named nowhere in the MANUAL, yet they are what several tests actually run against. Cross-batch note: A547 (batch-open2-4) rewrites this same sentence and does not keep its 'Repository build writes are …' wording, so this row anchors on the sentence's tail ('use sandbox-local Nim caches.'), which A547 keeps verbatim — the paragraph lands after the rewritten sentence in either application order, and the anchor also validates against the MANUAL as it stands now",
  "evidence": "components/ctxtest/main.nim:1-12 (test-only, 'not in the manifest; compiled and started by the test itself'; hidden `chat` stub; the `ctxecho` sessionContext probe), :784-823 (collision, republish, effect-read and scalar outputSchema fixtures), components/ctxtest/sink.nim:1-15 (component `ctxsink`, `sawSession`), tests/mock_llm.nim:1, tests/mock_parallel_llm.nim:1, tests/t_nested.nim:26-46 (both fixtures compiled into the sandbox); `grep -rn ctxtest Makefile niffler.nimble manifest.yaml` = only tests/t_nested.nim",
  "old_string": "use sandbox-local Nim caches.\n",
  "new_string": "use sandbox-local Nim caches.\n\nSeveral tests carry their own fixtures instead of a real model:\n`components/ctxtest/` is a stub-LLM component (a hidden `chat` that plays a\nscripted turn per session id) that also supplies the contract fixtures a real\ntool cannot produce on demand — parameter-name mangling, a schema collision,\ncatalog republish churn, an `effect: \"read\"` item for the fabric batch host, a\nscalar `outputSchema` — plus `ctxecho`, the `sessionContext` probe that proves\nharness-private context is stripped from nested calls;\n`components/ctxtest/sink.nim` registers a second component (`ctxsink`, which\nreports `sawSession`) for the same check. `tests/mock_llm.nim` and\n`tests/mock_parallel_llm.nim` are the equivalent stand-ins for the `llm`\nbinary. None of them is in `manifest.yaml` or built by `make build`: each test\ncompiles the one it needs into its private sandbox `NIF_ROOT` and spawns it\nthere.\n\n"
 },
 {
  "id": "A785",
  "slice": "component-infra-and-examples",
  "status": "already",
  "reason": "no edit: the sentence is right as it stands. The sandbox's generated `config.nims` (sandbox-local `nimcache`) is read by compiles that run inside the sandbox — the builder's agent-built components — while a test compiling its own fixture (`nim c <repo>/components/ctxtest/...`) reads the *repo* config, so the proposed rewording ('test-only components … compiled with a sandbox-local nimcache') would be false",
  "evidence": "docs/MANUAL.md:2933-2935; tests/helpers.nim:316-341; `nim check --hints:on components/ctxtest/main.nim` from the repo root reports the only config file used as `/home/gokr/git/nifflerprod/config.nims`, not a sandbox one; components/builder passes no `--nimcache`, so it inherits the root it runs in",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A790",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the per-directory candidate list omits `AGENTS.local.md`, which is additive (it never shadows the primary file) and is collected for every directory of the walk",
  "evidence": "components/systemprompt/main.nim:46 (`localCandidate`), :62-72 (`loadLocalContextFileFromDir`, explicitly not a shadowing candidate), :210-219 (called for every directory, after that directory's primary file)",
  "old_string": "   - per directory, first hit wins: `AGENTS.override.md`, `AGENTS.md`,\n     `AGENTS.MD`, `CLAUDE.md`, `CLAUDE.MD` (one file per directory —\n     `AGENTS.md` shadows a `CLAUDE.md` next to it; symlinks are followed);",
  "new_string": "   - per directory, first hit wins: `AGENTS.override.md`, `AGENTS.md`,\n     `AGENTS.MD`, `CLAUDE.md`, `CLAUDE.MD` (one file per directory —\n     `AGENTS.md` shadows a `CLAUDE.md` next to it; symlinks are followed;\n     `AGENTS.local.md` is added in addition, never as a candidate: it never\n     shadows the primary file, and it is collected for every directory of the\n     walk, not only lazily);"
 },
 {
  "id": "A791",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "`prompt_hint` and the three prompt slots — the extension seam plugins contribute through — appear nowhere in the MANUAL",
  "evidence": "components/systemprompt/main.nim:122-158 (the hidden `prompt_hint` tool and its validation), :86-110 (`PromptHint`, `renderPromptSlot`, sort by `source\\x1fkey`), :240-246 (the three slots rendered as `<prompt_slot name=\"…\">`); exercised by tests/t_systemprompt.nim",
  "old_string": "- **Agent pre-fetch.** The `agent` component requests the prompt for\n  subagent children before their first turn and passes it via the session\n  call's `systemPrompt` field (best effort — the runner's own fallback\n  covers a missing component).",
  "new_string": "- **Agent pre-fetch.** The `agent` component requests the prompt for\n  subagent children before their first turn and passes it via the session\n  call's `systemPrompt` field (best effort — the runner's own fallback\n  covers a missing component).\n- **Prompt slots (extension seam).** Components and plugins contribute\n  fragments through the hidden `prompt_hint {slot, content, source?, key?,\n  mode?}` tool: the named slots (`tool_usage`, `efficient_tools`,\n  `after_instructions`) are rendered into `<prompt_slot name=\"…\">` blocks in a\n  deterministic order (by `source`, then `key`); `mode: aggregate` (default)\n  keeps every contribution and `mode: singleton` keeps only the last one\n  registered for that slot, while a slot with no contributions renders\n  nothing. Registration is component-local state and only affects prompts\n  composed *after* it — a frozen conversation is never rewritten.\n  `prompt_hint` is `x-harness.hidden` too, so neither it nor `systemprompt`\n  ever appears in an LLM toolset."
 },
 {
  "id": "A792",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the two component-side caps (16 context files, 200 000 bytes) and the fact that they are compile-time constants are unstated",
  "evidence": "components/systemprompt/main.nim:29 (`maxPromptLen = 200_000`), :33 (`maxFiles = 16`), :220-225 (the loop stops at the file cap), :268-270 (the component's truncation marker), :120 (the file's only `getEnv` is `NIF_ROOT`); core/conversation.nim:101-103 (core's own cap and marker)",
  "old_string": "- **Cap.** Answers are truncated at 200 KB (both sides).",
  "new_string": "- **Cap.** Answers are truncated at 200 KB (both sides) — the component stops\n  at 200 000 bytes itself (marker `[systemprompt: truncated at 200000 bytes]`;\n  core adds its own) and collects at most 16 context files. Both numbers are\n  compile-time constants with no env knob: a different cap means rebuilding\n  the component."
 },
 {
  "id": "A793",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the replace-the-constitution recipe is incomplete in a way that breaks a real attempt: the replacement must register under the same name, and the swap is undone at the next boot because the manifest entry is restored first and a stored record for that name is skipped",
  "evidence": "core/dispatch.nim:311-345 (`spawn` refuses 'component already supervised: <name>'; `kill` removes any supervised child, manifest included), core/niffler.nim:596-604 (a stored `component` record whose name is already supervised is skipped with no message), sdk/niffler/sdk.nim:817-819 (the call subject is derived from the component name)",
  "old_string": "Replacing the constitution is a normal Niffler operation:\nwrite a component that answers on the same subject, `build` it,\n`kill` the old one, `spawn` yours. The agent can do this to\nitself.",
  "new_string": "Replacing the constitution is a normal Niffler operation: write a\ncomponent that answers on the same subject — the subject is derived from\nthe component name, so it must register as `systemprompt` — `build` it,\n`kill` the old one (`core.spawn` refuses a name that is already\nsupervised), then `spawn` yours. The swap lasts only until the next\nboot, though: a component declared in `manifest.yaml` is restored\nfirst and a stored record for the same name is skipped, so a permanent\nreplacement means editing `manifest.yaml`. The agent can do this to\nitself."
 },
 {
  "id": "A794",
  "slice": "component-infra-and-examples",
  "status": "already",
  "reason": "no edit: verified — the `agent` component asks for the prompt with the child's `{cwd}` before the child's first turn and passes the answer as the session call's `systemPrompt` (best effort)",
  "evidence": "docs/MANUAL.md:2323-2326; components/agent/main.nim:867-885",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A796",
  "slice": "component-infra-and-examples",
  "status": "already",
  "reason": "no edit: verified — the 500 ms probe runs first and the 8 s budget is granted only when the catalog says `systemprompt` is registered, else core's baked-in fallback",
  "evidence": "docs/MANUAL.md:2319-2321; core/conversation.nim:72, 96-105",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A797",
  "slice": "component-infra-and-examples",
  "status": "already",
  "reason": "no edit: verified — the worktree shadow rule reads `<root>/.git` for a `gitdir:` pointer, matches `/.git/worktrees/`, and shadows only the same candidate filename in the main root. The unstated `cwd`-inside-root precondition adds nothing: outside the root the walk never reaches the main root, so the rule cannot apply there",
  "evidence": "docs/MANUAL.md:2343-2345; components/systemprompt/main.nim:174-204; :205-219 (`stopAbove` = cwd when the workspace is outside the root)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A798",
  "slice": "component-infra-and-examples",
  "status": "code",
  "reason": "the fix belongs in code: `components/systemprompt/main.nim:210` has a dead `let f = loadContextFileFromDir(dir)` — the same directory is re-read one line later as `primary`, and the outer `f` is shadowed by the loop variable. Delete the line: it costs one redundant `readFile` per directory of the ancestor walk and duplicates the 'systemprompt: unreadable context file …' stderr line for an unreadable file. Nothing in the MANUAL describes this, so no prose edit follows",
  "evidence": "components/systemprompt/main.nim:205-219; `--hints:off` in the build is what hides the unused-symbol warning",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A799",
  "slice": "component-infra-and-examples",
  "status": "skip",
  "reason": "not a second anchor: the row asks for exactly the statement A792 adds ('compile-time constants with no env knob … a different cap means rebuilding the component'). The component's only `getEnv` is `NIF_ROOT` (the candidate list, slot list and timeouts are constants), so one clause in the Cap bullet covers it — a second edit would only restate it",
  "evidence": "components/systemprompt/main.nim:29, 33, 44-46, 120, 129, 164, 240-245; docs/MANUAL.md:2322 (A792's clause)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A801",
  "slice": "component-infra-and-examples",
  "status": "code",
  "reason": "the fix belongs in code: core's boot restore silently `continue`s when a stored `component` record's name is already supervised (i.e. declared in `manifest.yaml`), unlike the sibling 'core: WARNING stored component … has missing binary' two dozen lines below. Echo one `core: WARNING stored component <name> skipped: the manifest declares it` so a replaced-and-forgotten component is diagnosable instead of invisible. The MANUAL's new caution in A793 already describes the behaviour the warning would surface",
  "evidence": "core/niffler.nim:596-604 (`if alreadyManifest: continue`) vs :618-620 (the sibling warning)",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A805",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the example adapter's own contract is unstated: which `NIF_OPENAI_*` variables it reads and that its output cap is hardcoded. Cross-batch note: A802 (batch-open2-1) appends a clause right after 'swap it in via `manifest.yaml`', so this row inserts its parenthetical before that phrase instead — the two land in the same cell whatever the order, and the sentence reads through (verified in the whole-round simulation)",
  "evidence": "components/llm-openai/main.go:48 (`NIF_OPENAI_CONTEXT`), :70 (`NIF_OPENAI_MODEL`), :75 (`NIF_OPENAI_BASE_URL`), :79 (`NIF_OPENAI_API_KEY`), :91 (`max_tokens: 32768`, no arg and no env); no `NIF_OPENAI_PROVIDER` read anywhere in the file",
  "old_string": "is the minimal non-streaming example, swap it in via `manifest.yaml`",
  "new_string": "is the minimal non-streaming example (it reads only `NIF_OPENAI_API_KEY`, `NIF_OPENAI_BASE_URL`, `NIF_OPENAI_MODEL` and `NIF_OPENAI_CONTEXT` — not `NIF_OPENAI_PROVIDER` — and always sends `max_tokens: 32768`, a hardcoded value with no knob); swap it in via `manifest.yaml`"
 },
 {
  "id": "A808",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the stdio environment allowlist in the MANUAL is wrong in both directions: it lists `SHELL` and `TERM` (not passed) and omits the locale/TLS/cache variables that are (npx/uvx need the cache vars, TLS needs the cert paths)",
  "evidence": "components/mcp-bridge/transport.go:157-179 (`mergedEnv`: PATH, HOME, USER, LOGNAME, TMPDIR, TMP, TEMP, LANG, LC_ALL, SYSTEMROOT, SSL_CERT_FILE, SSL_CERT_DIR, XDG_CACHE_HOME, XDG_CONFIG_HOME, UV_CACHE_DIR, NPM_CONFIG_CACHE, then the record's `env`)",
  "old_string": "  inherit a fixed environment allowlist (PATH, HOME, TMPDIR, USER, SHELL,\n  LANG, TERM) — `NIF_*` variables and secrets in the harness environment\n  never reach them.",
  "new_string": "  inherit a fixed environment allowlist — `PATH`, `HOME`, `USER`, `LOGNAME`,\n  `TMPDIR`/`TMP`/`TEMP`, `LANG`/`LC_ALL`, `SYSTEMROOT`,\n  `SSL_CERT_FILE`/`SSL_CERT_DIR`, `XDG_CACHE_HOME`/`XDG_CONFIG_HOME`,\n  `UV_CACHE_DIR`, `NPM_CONFIG_CACHE` — plus whatever the record's `env` adds.\n  `SHELL` and `TERM` are *not* passed, and `NIF_*` variables and secrets in\n  the harness environment never reach them."
 },
 {
  "id": "A809",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the sentence contradicts the `### Prompts` bullet and the code: the bridge rewrites both caches (`tools` **and** `prompts`) when the server drifts",
  "evidence": "components/mcp-bridge/main.go:256-275 (`acceptContract` compares tools + prompts), :277-306 (`persistContract` writes `record.Tools` and `record.Prompts`), components/mcp/types.go:30-31 (both are caches in the record); docs/MANUAL.md:1818-1821 (the prompts bullet)",
  "old_string": "manager owns every field except `tools` — the bridge rewrites only that cache\nwhen the server drifts.",
  "new_string": "manager owns every field except the caches: the bridge rewrites `tools`\n**and** `prompts` when the server drifts."
 },
 {
  "id": "A810",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the bridge's hidden manager helper (`mcp_<server>_bridge_status`) is undocumented, so the live state `mcp_servers` shows and what `mcp_refresh` drives have no described source",
  "evidence": "components/mcp-bridge/operations.go:411 (registered with `x-harness.hidden`), main.go:390-427 (`status`/`refresh`; the status fields), components/mcp/main.go:250-258 (`mcp_servers` live state via a 1 s status call), :765-779 (`mcp_refresh` → `bridge_status` refresh)",
  "old_string": "The manager rejects servers whose generated tool names\n  collide with another server's or with a catalog tool.",
  "new_string": "The manager rejects servers whose generated tool names\n  collide with another server's or with a catalog tool.\n  Each bridge also registers one hidden helper, `mcp_<server>_bridge_status\n  {op: status|refresh}` (invisible to the LLM), which `mcp_servers` calls for\n  its live state and `mcp_refresh` for the reconnect; `status` carries\n  `connected`, tool and prompt counts, `retiring`, `activeCalls`, `lastError`,\n  `startedAt`, `lastUsed` and `idleMs`."
 },
 {
  "id": "A811",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the Drift bullet is accurate; the one detail worth keeping is that the exit-3 restart is deferred until in-flight calls finish, so a drift cannot truncate a call",
  "evidence": "components/mcp-bridge/main.go:150-154 (`maybeRestartLocked` closes `restart` only when `restartWanted && len(active) == 0`), :120-132 (the call wrapper re-checks on completion), :520-526 (`code = driftExitCode`)",
  "old_string": "rev-retried) and exits 3, so the supervisor restarts it announcing the\n  current truth.",
  "new_string": "rev-retried) and exits 3, so the supervisor restarts it announcing the\n  current truth.\n  The exit waits for in-flight calls to finish, so a drift never truncates a\n  call."
 },
 {
  "id": "A815",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the spilled-result contract is described only loosely: the tool returns a structured pointer (16 KiB of text plus a line naming the file), and non-text/structured content is serialized into `text`",
  "evidence": "components/mcp-bridge/operations.go:19 (`inlineLimit = 64 * 1024`), :140-180 (`bound`: `$NIF_ROOT/var/mcp-results/result-*.json`, `truncateUTF8(preview, 16*1024)` + the pointer line, `{text, spill: {path, bytes}, truncated: true}`), :112-128 (`projectResult`: text parts joined, other parts and structured content JSON-serialized)",
  "old_string": "- **Result size**: MCP results ≤64 KiB are returned inline; larger results\n  are spilled to `$NIF_ROOT/var/mcp-results/result-*.json` and the tool\n  returns a short preview plus the file path (readable with `read`, `grep` or\n  bash) instead of blowing up the context window.",
  "new_string": "- **Result size**: MCP results ≤64 KiB are returned inline; larger results are\n  spilled to `$NIF_ROOT/var/mcp-results/result-*.json` and the tool gets a\n  machine-readable pointer instead — `{text: <the first 16 KiB of the text\n  plus a line naming the file>, spill: {path, bytes}, truncated: true}`\n  (readable with `read`, `grep` or `bash`), so a big result never blows up the\n  context window. Non-text content parts and structured content are\n  JSON-serialized into `text` rather than dropped."
 },
 {
  "id": "A818",
  "slice": "component-infra-and-examples",
  "status": "skip",
  "reason": "an internal invariant of two Go modules (`components/mcp/types.go` and `components/mcp-bridge/types.go` are duplicates to keep identical) with no user-visible behaviour — the MANUAL is not the place for it, and the row itself says so ('fix: none')",
  "evidence": "components/mcp-bridge/types.go:1-3; components/mcp/types.go:1-3",
  "old_string": "",
  "new_string": ""
 },
 {
  "id": "A822",
  "slice": "component-infra-and-examples",
  "status": "apply",
  "reason": "the env row names only the manager as reader; the bridge's own `--probe` mode reads it too, so a hand-run probe honours it",
  "evidence": "components/mcp-bridge/main.go:435-442 (`runProbe` reads `NIF_MCP_PROBE_TIMEOUT_MS`), components/mcp/main.go:466-476 (the manager reads it for its own probe deadline and passes its environment on with `cmd.Env = os.Environ()`)",
  "old_string": "timeout for one real-connect probe in `mcp_add`/`mcp_edit`: when set (positive) it wins over both the 30 s default and the server's own `timeoutMs`",
  "new_string": "timeout for one real-connect probe in `mcp_add`/`mcp_edit` (the bridge's own `--probe` mode reads it too, so a hand-run probe honours it): when set (positive) it wins over both the 30 s default and the server's own `timeoutMs`"
 }
]
