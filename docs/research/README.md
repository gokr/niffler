# Research

Design-history documents: the ideas, reviews and deep-dives behind what
shipped. Not operating documentation — for that, see
[../MANUAL.md](../MANUAL.md) and [../ARCHITECTURE.md](../ARCHITECTURE.md).

Cited code lives in the sibling clone shelf `~/git/harnesses/` — all 20
checkouts, their pinned commits and which note read each one are indexed in
`~/git/harnesses/README.md`.

| Document | What it is |
|---|---|
| [REBOOT.md](REBOOT.md) | The original design rationale (2026-08): processes instead of plugins, NATS as the single backbone, the SDK as the real product, payload/serialization and contract decisions |
| [EXPERT.md](EXPERT.md) | Design of the `expert` advisory peer: bounded current-turn observation, the stateless LLM judge over a cache-stable knowledge prefix, and the turn-bound `svc.session.<id>.advise` surface |
| [FABRIC.md](FABRIC.md) | Full design of the `fabric` programmable tool calling and `agent` subagents: architecture, threat model, shipped scope, tests, deferred work |
| [FABRIC_FEEDBACK.md](FABRIC_FEEDBACK.md) | Historical: the external review that reshaped the fabric design before it shipped (its findings are resolved in FABRIC.md) |
| [MCP.md](MCP.md) | Historical MCP client plan; MCP is now shipped as the `mcp` manager plus per-server `mcp-bridge` components — see [MANUAL.md](../MANUAL.md#external-mcp-servers-mcp) |
| [CODEWHALE.md](CODEWHALE.md) | Prior-art analysis of the CodeWhale coding agent (`../CodeWhale`): what to borrow, mapped to Niffler components — pinned prompt-cache prefix, user memory, subagent postures, typed permission rules, shell sandboxing, replay aliases; plus a 2026-09 delta re-sweep (worktree write-claims, eval-harness contract, external-memory cutline, RLM sessions, aux-model policy, modes) |
| [CODEWHALE-TUI.md](CODEWHALE-TUI.md) | Prior-art analysis of CodeWhale's **terminal UI** (swept 2026-09-17 at `06b44cca5`, two subagent runs, citations spot-checked): its crate's UI contracts (one owner per fact, typed state instead of prose, semantic motion, hitbox/focus parity, PTY evidence over render internals), then the borrowable mechanisms — truthful live surfaces (`Option`-not-`0`, watermark dismissal, live-vs-settled projection, blocked-on-what chips), help generated from the binding table so prose cannot drift, declared width-degradation tiers, approval whose decision risk and presentation stakes are separate axes, gate/cancel/file-mutation receipts, a context inspector that decomposes the prompt layer by layer, revision-keyed render caches, a glyph/colour capability ladder, `@`-mentions as bounded typed attachments — each mapped to a Niffler surface with an effort estimate, plus parity, deliberate non-borrows (ambience, audio, fleet views, the control socket) and a work order |
| [DEEPSEEK-HARNESS.md](DEEPSEEK-HARNESS.md) | Research notes (2026-07) on DeepSeek's open-source agent harness (`dsh`, ~170k LOC TypeScript on the Cordis DI framework): the event-sourced Surface with provable deletions, locked cache-aware compaction, spill budgets, the guarded tool pipeline, Landlock sandbox, subagents, approvals — ending in the five priority steals for Niffler; plus a 2026-09 file-tools slice comparing our `edit` component to dsh's `tool-fs` suite |
| [DSH-STEAL.md](DSH-STEAL.md) | Design record for four dsh orchestration ideas: continuable subagents and forked children are shipped, on-demand `fabric {api: true}` declarations and `team` teammates remain proposals; each is sieved against the AGENTS.md invariants with its prompt-prefix effect stated |
| [DSH-STEALS-PLAN.md](DSH-STEALS-PLAN.md) | Historical runbook: phases A (context/compaction) and B (continuable subagents/forks) landed; phase C (`fabric {api: true}` declarations) remains open. The original files, records, cache-effect statements, tests and acceptance are retained as design history |
| [ESCALATION.md](ESCALATION.md) | Proposal (nothing implemented): automatic escalation — thinking, then model — on consecutive failures, a dsh-goal-style give-up gate, and de-escalation back to a cheap route; what Niffler can already do, the cross-harness prior art (pi, Claude Code, Gemini CLI, CodeWhale), dsh's model-selection and goal machinery in detail, the `escalate` component design, and a seven-item steal list |
| [SANDBOX.md](SANDBOX.md) | Historical sandbox proposal and cross-harness survey; implementation decisions and overbroad security claims are superseded by SANDBOX-PLAN.md |
| [SANDBOX-PLAN.md](SANDBOX-PLAN.md) | Revised plan for `feat/sandbox`: opt-in per-command shell write protection, Landlock/Seatbelt launcher, sanitized environment, explicit failure reporting and one-shot path grants; shared-component and bus-trust limitations, optional component ceilings, and simpler wrapper/VM/container alternatives |
| [PI_EFFICIENCY_FINDINGS.md](PI_EFFICIENCY_FINDINGS.md) | Findings: what pi (github.com/earendil-works/pi) does that Niffler lacks on token consumption and wall-clock execution, plus the concurrency deep-dive (NATS fan-out vs. Nim task pools) |
| [PI_EFFICIENCY_PLAN.md](PI_EFFICIENCY_PLAN.md) | Plan: ordered improvements (parallel tool fan-out → LLM compaction → retry/cache/accounting → session tree), with wire-spec implications and effort |
| [PI_EFFICIENCY_B1B_THREADS.md](PI_EFFICIENCY_B1B_THREADS.md) | Decision record: same-component bottleneck, Nim mechanism evaluation (`std/threads` + `std/locks` preferred), deferred worker-aware pump, process replicas, and concurrent-safe Go tools |
| [PI_FEATURE_SCAN.md](PI_FEATURE_SCAN.md) | Feature-level scan of pi (basis `6aedd1066`): durable tool lifecycle (replay + checkpoints), deferred tool loading, project trust, session tree/lanes, prompt templates, model policy, evals, export — tiered with Niffler-shaped mappings and an effort order |
| [NATSNIM.md](NATSNIM.md) | The pure-Nim NATS client ([gokr/natsnim](https://github.com/gokr/natsnim), a translation of `nats-io/nats.go`) — now the **default** bus client: why it replaces the `nats.c` binding, how the flagged migration kept call sites byte-identical, what P7 found (a 1 ms `NextMsg` that never read the socket, and missing no-responders), and the measured head-to-head against `nats.go` |
| [STORE_V2.md](STORE_V2.md) | Implementation record (M3–M5, M7 landed): three interchangeable stores (SQLite now the default, barrel + TiDB via `NIF_STORE_BACKEND`), `niffler-store-migrate` for moving a root between engines, DuckDB as a bus observer, SDK expansion + duplication ledger |
| [COMPACTION.md](COMPACTION.md) | Shipped context design and implementation record: replaceable `compaction_propose` / `context_recall`, runner-owned validation and persistence, SQLite-backed stores, deterministic pruning, recall links and bounded provider-overflow recovery |
| [CONTEXT-REVIEW.md](CONTEXT-REVIEW.md) | Cross-plan review (2026-09-13): compaction contract blockers, read-visibility invalidation, crash-tail repair, evidence and auxiliary-call seams; corrections to cache, forks, teams and escalation assumptions, with a proposed delivery order |
| [REMOTE.md](REMOTE.md) | Survey (2026-09-13): remote execution vs presentation vs runtime vs fleet across the tracked harnesses (only OpenHands' Agent Server does remote runtimes); then two Niffler paths — first-class Chetter support (MCP floor, runner adapter, dedicated component) and a native fleet component over NATS leaf nodes + a scoped shared TiDB store |
| [AIDER.md](AIDER.md) | Steal list (2026-09-13) from the dormant Aider classic: the PageRank repo map (attention allocation as an on-demand, append-only tool), the reflection loop (lint/test/edit failures as typed retries with a cap), weak/editor model split for auxiliary calls, git-first auto-commit discipline, per-message cost meter; parity notes and what not to steal |
| [OPENHANDS.md](OPENHANDS.md) | Steal list (2026-09-13) from OpenHands Agent Canvas (`28464621d`, frontend-only checkout): per-conversation cost meters and budgets, a distinct `stuck` terminal state, the `/goal` completion loop, LLM compaction with a manual trigger, `PreToolUse`-style blocking hooks, typed tool visualizers + event grouping, client tools (the UI as tool executor), approval risk classes, git-control-bar prompt macros, MCP OAuth2/health verdicts, worktree-per-conversation, schema-driven settings, key-gated remote access (`--public` + one ingress port), and machine-enforced architecture guard tests; plus the four-repo split, REST control plane and settings-blob protocol as explicit non-steals |
| [SUBAGENTS.md](SUBAGENTS.md) | Historical comparison + gap list (2026-09-14) for Niffler's `agent` component versus DSH. Continuation, fork, settlement notices and `agent_list` subsequently shipped; the remaining comparison gaps are intentionally retained as research context |
| [SUBAGENTS-PLAN.md](SUBAGENTS-PLAN.md) | Implementation runbook (2026-09-14): the subagent proposals phased P0–P4 — settlement notices + `agent_list` (P0, **shipped**), continuation + fork (P1, **shipped**), parallel delegation + `NIF_AGENT_MAX_DEPTH` (P2), context-sensitive descriptions, steer durability, `agent_ask`, fork×compaction (P3), and `session_info` lineage + UI + bench scenarios (P4). Per-phase design, contract tests, implementation notes ("found while building it"), and open questions with recorded resolutions |
| [REPOMAP.md](REPOMAP.md) | Verified algorithm study + port record of the Aider repo map (2026-09): `aider/repomap.py` and its tree-sitter tag queries read line by line, the personalized-PageRank ranking derived, then the Niffler port — the tags seam, the scoring core and the `repomap` component, its A/B results (full30 + Multi10), and why the workspace-open auto-append ships **off by default** with `repo_map` remaining an onDemand tool instead |
| [REPOMAP-GATES.md](REPOMAP-GATES.md) | Decision record (2026-09-16) for the two admission gates on the repomap auto-append, agreed after the three A/Bs left the sign regime-dependent: the **content gate** (reject stub maps — the high A/B's anti-map cells were partly measuring a 154-byte/pre-C-tier jq map) and the **size floor** (micro repos have nothing to orient; full30 taxed +41% for zero effect). Calibrated thresholds (≥800 B, ≥25 symbols, ≥5 files, ≥50 census files) with the measurement table, what the gates deliberately do not solve (the high-thinking tail, jq/redis variance), implementation sketch and the post-implementation verification plan |
| [REASONIX.md](REASONIX.md) | Prior-art analysis (2026-09-07) of DeepSeek-Reasonix (Go, single binary; shelf commit `6c2e845b8`): each item a candidate plan, ending in the ten highest-leverage items for Niffler |
| [COMPACTION_LIVE_SMOKE.md](COMPACTION_LIVE_SMOKE.md) | Live smoke record (2026-09-15): compaction against a real provider with an artificially small admission window — model, token accounting, and what actually happened |
| [FAST-APPLY.md](FAST-APPLY.md) | Survey (2026-09-16): the four "fast apply" families across the harness shelf (deterministic forgiving apply, patch-dialect envelope, dedicated apply model, repair hook) — a per-harness ladder table, whether Codex/Claude Code use it (Codex *is* `apply_patch`; Claude Code is exact-match + stale recovery, no apply model), why the literal Morph-style "Fast Apply" needs a model but not a *local* one, dsh's deliberate fail-closed counter-position, the token economics that matter (output tokens per accepted edit, rounds-to-green — not tok/s), and where Niffler's `edit` 0.3.0 sits: candidate-line ambiguity errors, a repair tier behind the existing hook seam, and an opt-in `fastapply` plugin component as the AGENTS.md-legal shape for a merge provider |
