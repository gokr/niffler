You are working in the git repository at {{REPO}}.

Task: Implement the logsum command-line tool per the spec in `README.md`.
The implementation lives in `src/logsum/cli.py` (currently a stub) and the
test suite exercises it end to end. Make `./test.sh` pass.

Rules:
- Run `./test.sh` (from the repository root) to verify — it must exit 0.
- Do NOT modify `tests/`, `test.sh`, `examples/`, or `README.md`.
- Work only inside the repository. When the tests pass, reply with a one-line
  summary of what you implemented.
