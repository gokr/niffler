You are working in the git repository at {{REPO}}.

Task: Produce `todos-report.md` listing every TODO and FIXME comment in the
repository, in the exact format specified in `README.md` (and enforced by
`check.py`, which you may read as the normative spec). There are many
files — use whatever your harness provides for mechanical, repetitive work.

Rules:
- Run `./test.sh` (from the repository root) to verify — it must exit 0.
- Do NOT modify `check.py`, `test.sh`, `src/**`, `notes.txt`, or `README.md`.
- Work only inside the repository. When the check passes, reply with a
  one-line summary of what you produced.
