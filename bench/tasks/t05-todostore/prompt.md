You are working in the git repository at {{REPO}}.

Task: Implement the `TodoStore` class in `lib/todos.mjs` exactly per the API
contract documented at the top of that file, so the full test suite passes.

Rules:
- Run `./test.sh` (from the repository root) to verify — it must exit 0.
- Do NOT modify `tests/`, `test.sh`, `package.json`, or `README.md`.
- Keep the public API exactly as documented. No external dependencies.
- Work only inside the repository. When the tests pass, reply with a one-line summary.
