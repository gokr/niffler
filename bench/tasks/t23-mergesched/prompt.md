You are working in the git repository at {{REPO}}.

Task: Implement `Merge` and `MinRooms` in `sched.go` exactly as specified in
`README.md` (adjacency is not overlap, priority = max, zero-length rules,
sorted output, no input mutation).

Rules:
- Run `./test.sh` (from the repository root) to verify — it must exit 0.
- Do NOT modify `sched_test.go`, `test.sh`, `go.mod`, or `README.md`.
- Work only inside the repository. When the tests pass, reply with a one-line
  summary.
