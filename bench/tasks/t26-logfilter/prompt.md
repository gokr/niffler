You are working in the git repository at {{REPO}}.

Task: Implement the query language in `logfilter.py` (tokeniser, recursive-
descent parser with precedence, evaluator) per `README.md`. Error positions
are asserted by the tests.

Rules:
- Run `./test.sh` (from the repository root) to verify — it must exit 0.
- Do NOT modify `test_logfilter.py`, `test.sh`, or `README.md`.
- Work only inside the repository. When the tests pass, reply with a one-line
  summary.
